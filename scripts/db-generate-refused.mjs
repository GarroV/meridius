#!/usr/bin/env node
// Генератор миграций намеренно выключен (D089, задача T144).
//
// Снимки схемы в src/blocks/data/migrations/meta отстали с 0006: миграции
// 0006 и 0007 написаны руками — с комментариями и обратным файлом, которых
// генератор не пишет. Поэтому `drizzle-kit generate` сегодня выдал бы
// миграцию, заново создающую store_shift_modes, submissions.mode и checks.
// Накат от этого не страдает (Drizzle читает журнал и сами .sql), ломается
// именно генерация — молча и правдоподобно, а «правдоподобно» здесь хуже
// всего: такую миграцию легко накатить, не заметив.
//
// Практика проекта: миграции пишутся руками, парой файлов <имя>.sql и
// <имя>.down.sql, и вносятся в meta/_journal.json.
process.stderr.write(
  [
    "Генератор миграций выключен намеренно (D089).",
    "",
    "Миграции этого проекта пишутся руками: снимки схемы в migrations/meta",
    "отстают с 0006, и generate выдал бы миграцию, заново создающую уже",
    "существующие таблицы. Накат от этого не страдает — ломается генерация.",
    "",
    "Как добавить миграцию:",
    "  1. src/blocks/data/migrations/000N_имя.sql — сама правка",
    "  2. src/blocks/data/migrations/000N_имя.down.sql — откат",
    "  3. запись в src/blocks/data/migrations/meta/_journal.json",
    "  4. npm run db:migrate — накатить, npm run db:rollback — откатить",
    "",
    "Проверять накат следует на ПУСТОЙ базе, а не на своей: только с нуля",
    "видно, что история схемы цела.",
    "",
  ].join("\n"),
);
process.exit(1);
