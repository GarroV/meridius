/**
 * Заголовок запроса, которым посредник называет корневой разметке: это вкладка планшета.
 *
 * Зачем он нужен. Язык привязанной вкладки принадлежит пиццерии (D122), как и у заполнения
 * по наклейке, а знает пиццерию только база. Корневая разметка рендерится РАНЬШЕ страницы и
 * спросить её не может, поэтому язык документа считает сама — по куке планшета. Но кука на
 * любом другом маршруте продукта ничего не значит, и ходить в базу за ней на каждой
 * странице кабинета незачем: признак ставит посредник ровно на `/station`.
 *
 * Модуль намеренно пустой, кроме этой строки: его импортирует посредник (`src/proxy.ts`),
 * который исполняется в окружении без базы. Приедь сюда что-нибудь из слоя данных — в
 * сборку посредника поехал бы драйвер Postgres. Тот же приём, что у `fill/params.ts`.
 *
 * Значение клиента затирается своим в самом посреднике: заголовок, пришедший снаружи,
 * доверия не имеет.
 */
export const DEVICE_TAB_HEADER = "x-meridius-device-tab";
