-- Обратный ход 0013: привязки планшетов и живые пины уходят целиком.
--
-- Историей эти строки не являются (принцип 3 их не защищает): привязка — это право
-- показывать чек-лист сейчас, а не запись о прошлом. Заполнения, сделанные с планшета,
-- на эти таблицы не ссылаются вовсе — они анонимны (D001), — поэтому откат ничего из
-- истории не задевает.
DROP TABLE IF EXISTS "device_pairings";
DROP TABLE IF EXISTS "devices";
