// Табличный пункт на экране редактора: предел колонок, известный и кнопке, и разбору.
//
// Ни одного ввоза — сознательно, ровно как в `schedule-field.ts`: этот файл уезжает в
// браузер вместе со строкой пункта, а вход блока `data` тянет за собой драйвер `pg`,
// которого в браузере нет. Число живёт здесь одно: кнопка «добавить колонку» обязана
// гаснуть на том же месте, где разбор начинает отказывать, иначе два предела разъедутся
// молча — методист заведёт тринадцатую колонку и получит отказ уже на сохранении.

/**
 * Колонок у одного табличного пункта (D074). В исходных вкладках `Dough mixing` их
 * восемь-десять; предел стоит не от жадности, а потому что каждая колонка — это ещё
 * одно поле в карточке строки на экране телефона.
 */
export const MAX_COLUMNS = 12;
