<!-- managed by forge: do not change the row format below — forge-build and forge-status parse this file -->
# Tasks

<!-- id: Tnnn (T001, T002, ...). status: todo | in_progress | done | failed | blocked:Qnnn (Qnnn — the id of an open question in questions.md). "depends on" — ids separated by commas, or "—". block: the name of a product block from plan.md, or chores for housekeeping tasks that belong to no block (topping up research, obtaining access, applying an owner's answer) — those are never handed to block agents. issue: the task's number in the project tracker (#N), or "—" if it has none. The issue column is what makes reconciling the graph against the tracker mechanical: without it the link lives in commit text, and the graph falls behind the tracker silently — on a live project that is how the build stood still for a month and a half.

stage: the number of the wave in which the task is done (1, 2, 3…), or "—" if the product is built in one pass. A stage is something that must work end to end — "the month is calculated in the browser" — not "block api is finished". Why it has to be machine-readable: without it a graph passes the integrity check and is still unreachable — the goal of the first wave depends on a task from the fourth, and only a person holding the waves in their head can see it. The rule that then gets checked: a task may not depend on a task from a later stage. Use the same number as the stage marks on the Definition of Done items in the block description. -->

| id | block | depends on | status | task | issue | stage |
|---|---|---|---|---|---|---|
<!-- | T001 | api | — | todo | Example: design the database schema | #12 | 1 | -->
| T001 | core | — | done | Поднять каркас: Next.js 16.3.4 (App Router), TypeScript 6.0.3, Tailwind 4.3.3; страница отвечает на http://localhost:3100 | — | 1 |
| T002 | core | — | done | Docker Compose с PostgreSQL 17 на порту 5433; `psql "$DATABASE_URL" -c 'select 1'` отвечает; `.env.example` с именами переменных | — | 1 |
| T003 | core | T001 | done | Поставить конфиги шести проверок: prettier, eslint 10 flat с typescript-eslint strictTypeChecked, tsconfig со strict и noUncheckedIndexedAccess, vitest с покрытием, knip, dependency-cruiser; `.npmrc` с min-release-age=7 | — | 1 |
| T004 | core | T003 | done | Написать исполняемый `scripts/check`: запускает весь набор и пишет reports/vitest.junit.xml и reports/playwright.junit.xml | — | 1 |
| T005 | core | T004 | done | Проверить каждую из шести проверок на испорченной копии: неформатированный файл, нарушение линта, ошибка типов, падающий тест, неиспользуемый экспорт, импорт мимо графа — каждая падает и печатает причину | — | 1 |
| T006 | core | T004 | done | Поставить хук .githooks/pre-push, включить core.hooksPath; хук падает, когда инструмента нет, и останавливает пуш при красной проверке | — | 1 |
| T007 | core | T001 | done | Смоук связок из рисков: Tailwind 4 с токеном из `@theme inline` под Turbopack и next-intl с выбором языка по заголовку браузера без cookie | — | 1 |
| T008 | data | T002,T003 | done | Схема Drizzle: countries, stores, stations, checklists, checklist_versions, blocks, submissions; типы Item, Section, Answer через .$type<T>() | — | 1 |
| T009 | data | T008 | done | Миграции применяются с нуля и откатываются; частичный уникальный индекс «одна опубликованная версия на чек-лист» | — | 1 |
| T010 | data | T009 | done | publishVersion: публикация вставляет строку версии и не трогает предыдущую — тест сравнивает содержимое прежней версии до и после | — | 1 |
| T011 | data | T009 | done | getPublishedVersionForStation(stationCode, at): утренний чек-лист в 09:00, вечерний в 21:00, null в 15:00 без подходящего окна, null на неизвестном коде | — | 1 |
| T012 | data | T009 | done | saveSubmission, listSubmissions, getSubmission; серверные отметки времени берутся из базы, а не от клиента | — | 1 |
| T013 | auth | T001 | done | Вход в админку: хэш пароля из ADMIN_PASSWORD_HASH, сравнение постоянного времени, подписанная httpOnly-cookie на 30 дней | — | 1 |
| T014 | auth | T013 | done | requireAdmin() и защита всех маршрутов /admin/*; сквозной тест: без сессии ни один экран админки не отдаёт данные | — | 1 |
| T015 | catalog | T012,T014 | done | Справочник стран, пиццерий и станций: создание, правка, удаление с подтверждением и без висячих станций | — | 1 |
| T016 | catalog | T015 | done | Код станции: 10 символов криптографически случайно, без похожих знаков; перевыпуск ломает старый код сразу | — | 1 |
| T017 | catalog | T015 | done | Привязка чек-листа к станции; станция без назначенного чек-листа помечена явно | — | 1 |
| T018 | catalog | T015 | done | Экран справочника по эталону docs/furca/design/screens/catalog.html | — | 1 |
| T019 | editor | T012,T014 | done | Черновик чек-листа: создание, сохранение секций и пунктов, окно времени, привязка к станции | — | 1 |
| T020 | editor | T019 | done | Клавиатурная работа: Enter создаёт следующий пункт и ставит курсор, Alt+стрелки переставляют | — | 1 |
| T021 | editor | T019 | done | parsePastedList: вставка 20 строк из буфера даёт 20 пунктов за одно действие, маркеры и нумерация отброшены, пустые строки пропущены | — | 1 |
| T022 | editor | T019 | done | Типы ответов (да/нет, число с диапазоном, текст) и признак критичности; по умолчанию да/нет | — | 1 |
| T023 | editor | T010,T019 | done | Публикация версии из черновика: разные действия «сохранить черновик» и «опубликовать» | — | 1 |
| T024 | editor | T019 | done | Дублирование чек-листа целиком, включая перенос на другую станцию; история заполнений не копируется | — | 1 |
| T025 | editor | T019 | done | Предпросмотр «как это увидит сотрудник»: тот же экран заполнения с данными черновика | — | 1 |
| T026 | editor | T020,T021,T022 | done | Экран редактора по эталону docs/furca/design/screens/editor.html | — | 1 |
| T027 | library | T019 | done | Библиотека блоков: создание, правка, список; блок хранит название и пункты | — | 1 |
| T028 | library | T023,T027 | done | Вставка блока в черновик ссылкой; при публикации ссылка разворачивается в снимок содержимого | — | 1 |
| T029 | library | T028 | done | Тест: правка блока приходит во все черновики и не меняет ни одной опубликованной версии | — | 1 |
| T030 | library | T028 | done | «Где используется»: список чек-листов со ссылками и предупреждение, сколько черновиков и опубликованных версий затронет правка | — | 1 |
| T031 | library | T027 | done | Экран библиотеки по эталону docs/furca/design/screens/library.html | — | 1 |
| T032 | qr | T016 | done | Генерация QR станции в SVG без внешних запросов | — | 1 |
| T033 | qr | T032 | done | Лист A4 для печати: наклейки всех станций пиццерии, при печати не выводятся меню, кнопки и фон | — | 1 |
| T034 | qr | T032 | done | Полноэкранный QR для планшета станции; после перевыпуска кода показывает новый без ручного обновления | — | 1 |
| T035 | qr | T033 | done | Проверить распечатанный лист камерой телефона: код читается с бумаги, а не только с экрана | — | 1 |
| T036 | qr | T033 | done | Экран QR по эталону docs/furca/design/screens/qr-sheet.html | — | 1 |
| T037 | fill | T011,T023 | done | Маршрут /s/<код станции>: отдаёт опубликованную версию по станции и окну времени и ничего сверх этого | — | 1 |
| T038 | fill | T037 | done | Язык интерфейса и пунктов по языку устройства с откатом на язык страны и далее на русский | — | 1 |
| T039 | fill | T037 | done | Экран заполнения: секции, прогресс, кнопка с числом оставшихся пунктов, зоны нажатия от 44 px на ширине 375 px | — | 1 |
| T040 | fill | T039 | done | Проваленный критичный пункт требует комментарий сразу под собой и не даёт отправить без него | — | 1 |
| T041 | fill | T012,T039 | done | Отправка заполнения пишется на ту версию, что была отдана клиенту — тест на гонку с публикацией новой версии во время заполнения | — | 1 |
| T042 | fill | T037 | done | Довести защиту публичного маршрута до требований раздела «Правила безопасности» принципов проекта; критерии — в описании блока fill | — | 1 |
| T043 | fill | T039 | done | Состояния экрана сотрудника: отправлено, ссылка не действует, для станции нет чек-листа, обрыв связи без потери введённого | — | 1 |
| T044 | feed | T012,T014 | done | Лента заполнений с фильтрами по стране, пиццерии, станции и периоду | — | 1 |
| T045 | feed | T044 | done | Три показателя за выбранный день считаются по тем же данным, что и лента | — | 1 |
| T046 | feed | T044 | done | Карточка заполнения: ответ и время по каждому пункту, комментарии к проваленным, пункты снимком той версии, по которой заполняли | — | 1 |
| T047 | feed | T046 | done | Сквозной тест: правка и публикация новой версии чек-листа не меняют ранее сохранённую карточку | — | 1 |
| T048 | feed | T044,T046 | done | Экраны ленты и карточки по эталонам feed.html и submission.html | — | 1 |
| T049 | demo | T018,T026,T039,T048 | done | Идемпотентный сид демонстрационного контура на английском: страна, две пиццерии, станции с кодами, три чек-листа с секциями и переиспользуемым блоком, десяток заполнений с одним проваленным критичным пунктом | — | 1 |
| T050 | demo | T049 | done | Продукт поднимается на локальной площадке одной командой, адрес открывается в браузере | — | 1 |
| T051 | demo | T050 | done | Сквозной смоук MVP на поднятом продукте: завёл страну и пиццерию, создал чек-лист, напечатал QR, заполнил с телефона, увидел заполнение в ленте | — | 1 |
| T052 | demo | T039,T048 | done | Проверка доступности прогоном: axe внутри сценариев Playwright на экране заполнения и экранах админки | — | 1 |
| T053 | chores | — | done | Зеркалирование графа в Issues доведено: у каждой незакрытой задачи есть номер в колонке issue. Репозиторий с тех пор переименован — задача называла `GarroV/dodo_qr_service`, сейчас это `GarroV/meridius` (старое имя ведёт на него редиректом). Формулировки, раскрывающие незакрытые слабости, в публичный трекер не переносились (T071) | — | 1 |
| T054 | chores | — | done | Перепроверить версии стека перед стартом стройки: точные патч-версии живут пару недель, а TypeScript 7 и статус Drizzle 1.0 стоит сверить заново | — | 1 |
| T055 | chores | — | done | Разобраться с 4 moderate-уязвимостями npm audit в esbuild внутри drizzle-kit: только для разработки, лечится откатом drizzle-kit до 0.18 — решить, терпим или откатываем | — | 1 |
| T056 | data | T010 | done | Закрыть гонку со станцией: зафиксировать привязку станции в момент публикации версии, а не читать её из мутируемой checklists.stationId при сохранении заполнения. Тест на гонку: перевязать чек-лист на другую станцию между выдачей версии и сохранением — заполнение обязано остаться на станции, где его заполняли | — | 1 |
| T057 | data | T009 | done | Ограничение в базе на равные границы окна времени: чек-лист с window_start = window_end никогда не отдаётся ни на одной станции и делает это молча. Тест: вставка с равными границами отвергается базой | — | 1 |
| T058 | data | T009 | done | Ограничения размера JSONB в базе для публично записываемых данных: верхняя граница на pg_column_size(answers) и pg_column_size(snapshot) в submissions и на sections в checklist_versions. Тест: заведомо огромный JSON отвергается базой, а не только кодом | — | 1 |
| T059 | data | T012 | done | Устойчивый порядок ленты: вторичный ключ сортировки (submitted_at, id) и составной индекс (station_id, submitted_at desc) под фильтр ленты по станции и периоду. Тест: 500 заполнений с одинаковой отметкой времени отдаются в одном и том же порядке при повторных запросах | — | 1 |
| T060 | data | T008 | done | Таймауты пула соединений: statement_timeout и connectionTimeoutMillis, явный max под публичную точку записи. Без них всплеск нагрузки копит зависшие запросы вместо быстрого отказа | — | 1 |
| T061 | data | T011 | done | Тест на пересечение окон двух чек-листов одной станции: сейчас правило «берётся начинающийся раньше» живёт только комментарием в коде и ни разу не запускалось | — | 1 |
| T062 | catalog | T015 | done | Проверка часового пояса при создании и правке пиццерии: опечатка в названии зоны сейчас проходит запись и роняет публичный маршрут всех станций этой пиццерии при каждом сканировании | — | 1 |
| T063 | auth | T014 | done | Сквозные сценарии не должны зависеть от .env: сейчас файл, скопированный из .env.example по инструкции проекта, перекрывает переменные, которые playwright.config.ts передаёт тестовому серверу, и 6 сценариев входа падают. Без .env те же 23 сценария зелёные. Починить так, чтобы прогон из свежего клона проходил и с .env, и без него | — | 1 |
| T064 | core | T001 | done | Подключить Golos Text (эталон грузит его из Google Fonts, в приложении шрифта нет вовсе) и базовый размер текста 13px/18px по эталону: сейчас браузерные 16px, и каждый следующий экран будет расходиться с эталоном | — | 1 |
| T065 | core | T001 | done | Заголовки безопасности в next.config.ts: Content-Security-Policy и X-Frame-Options. Сейчас их нет ни на одной странице, включая публичный маршрут заполнения | — | 1 |
| T066 | auth | T063 | done | Ограничение частоты попыток входа: пароль один на всю сеть и админка смотрит в интернет, перебор ничем не ограничен | — | 1 |
| T067 | chores | — | done | Дополнить экранный эталон недостающими состояниями входа: сообщение «Неверный пароль» и автофокус поля — в продукте они есть, в эталоне их нет | — | 1 |
| T068 | data | T060 | done | Нестабильный тест: при прогоне с покрытием один тест из 146 упал, при повторном прогоне все зелёные. Инструментация покрытия замедляет выполнение, и тест на таймауты пула чувствителен ко времени. Мигающий тест в CI хуже отсутствующего — сделать проверку независимой от скорости машины | — | 1 |
| T069 | core | T004 | done | playwright.config.ts жёстко занимает порт 3101 при reuseExistingServer: true. Если приёмка и чей-то прогон стартуют одновременно, сквозные сценарии молча проверят чужую сборку и покажут зелёный результат по чужому коду | — | 1 |
| T070 | core | T008 | done | Тестовая база одна на все копии репозитория: два одновременных прогона сносят друг другу схему, и упавший тест выглядит случайным. Развести адрес тестовой базы по копиям — как разведены порты и имена compose-проектов | — | 1 |
| T071 | fill | T065 | done | Ужесточить политику безопасности контента на публичном маршруте до уровня админки; подробности — в описании блока fill | — | 1 |
| T072 | core | T064 | done | Считать покрытие раздельно: логика (.ts) под относительным порогом, разметка (.tsx) проверяется сквозными сценариями и в порог не входит. Сейчас появление разметки уронило общий показатель с 89% до 69,7% при том, что логика выросла до 91,5% — метрика перестала показывать деградацию, ради которой заведена | — | 1 |
| T073 | catalog | T018 | done | DetailCards.tsx — 460 строк при пределе 400 в принципах проекта. Разнести на карточки по сущностям | — | 1 |
| T078 | demo | T050 | done | Продукт развёрнут на MUSPELHEIM: своя папка `C:\projects\dodo_qr_service` из `_template`, своё имя compose-проекта `dodo-qr`, приложение на свободном порту 3100, база наружу НЕ публикуется (5433 на сервере занят чужим проектом), метка `backup.pgdump=true` на контейнере базы для ночного бэкапа, свои `SESSION_SECRET`, `ADMIN_PASSWORD_HASH` и `POSTGRES_PASSWORD` на площадке вместо демонстрационных из `.env.example`. Переезд после волны 1 (D035), раскатка — только по явному «да» владельца в разговоре и в ночное окно | — | 1 |
| T079 | demo | T078 | done | Страница заполнения открывается с телефона сотрудника на кухне через `tailscale funnel` на своём порту (D034): корень `:443` на площадке отдаёт чужой продукт. Проверяется с устройства вне Tailscale — без этого QR со стены никуда не ведёт | — | 1 |
| T076 | chores | — | done | СНЯТА решением владельца (D037): чистка истории от PDF концепции не требуется, репозиторий переведён в публичные как есть 06.09.2026 | — | 1 |
| T077 | demo | T078 | done | Повторяемый смоук после раскатки одной командой (`scripts/smoke`): открывает продукт по внешнему адресу площадки, проходит вход и главный сценарий заполнения. Проверка глазами один раз не годится — нужно уметь за минуту узнать, что именно отвалилось | — | 1 |
| T074 | core | T026 | done | Каркас админки (боковое меню, шапка) продублирован между блоками catalog и editor: границы модулей запрещают им импортировать друг у друга, и каждый сделал свой. Перенести в core, откуда его берут все | #3 | 1 |
| T075 | editor | T026 | done | Список чек-листов без фильтров: эталон templates.html показывает выбор страны, пиццерии и станции, построен минимум как вход в редактор | — | 1 |
| T080 | qr | T032 | done | Экранировать значения, попадающие в разметку картинки кода станции: сейчас произвольный текст в базовом адресе проходит в вывод без обработки. Проверено пробой — тест на экранирование падает | — | 1 |
| T081 | fill | T042 | done | Ключ ограничения по клиенту берётся из заголовка запроса, который клиент задаёт сам. Доверять заголовку только от известного посредника, иначе брать адрес соединения; поведение за туннелем задокументировать | — | 1 |
| T082 | fill | T041 | done | Тест отправки читает последнюю запись без отбора по своим данным и падает при повторном прогоне на непересозданной базе: `expected 'bd0da5ae…' to be 'cb27688d…'` в src/blocks/fill/submit.test.ts:110. Проверка, которая зависит от порядка прогонов, ловит не то, ради чего заведена | — | 1 |
| T083 | core | T064 | done | Контраст текста не проходит порог доступности на всех экранах: axe даёт `color-contrast` на `span, label, p` — падают 9 сквозных проверок из блока demo. Цвет взят из дизайн-системы (D013), поэтому починка означает либо свой токен вторичного текста, либо согласованное отступление от эталона | #4 | 1 |
| T084 | core | T004 | done | Обязательные переменные входа не проверялись при старте: `.env`, скопированный из примера с пустыми значениями, поднимал продукт молча, а вход падал ошибкой сервера в момент нажатия кнопки. Проверка добавлена в `src/startup-checks.ts` — теперь при старте пишется строка о ненастроенном входе | #5 | 1 |
| T085 | demo | T078 | done | Продукт опубликован наружу по адресу с путём (D045): сборка с BASE_PATH, запуск на площадке, funnel на своём пути, смоук по внешнему адресу | — | 1 |
| T086 | demo | T052 | done | Мигает проверка доступности главной админки (e2e/a11y.spec.ts:271): в общем прогоне падает примерно через раз, отдельно проходит всегда. Похоже на зависимость от данных, которые параллельные сценарии создают в общей базе. Мигающая проверка останавливает пуши по причине, не связанной с правками | #6 | 1 |
| T087 | editor | T026 | done | Введённое до гидратации теряется молча: свойства чек-листа (станция, заголовок) живут в состоянии React, а уезжают скрытыми полями — изменённое в первую секунду видно на экране, но сохранение стирает привязку. Найдено фактом на площадке: версия опубликована, а checklists.station_id пустой, по QR «Nothing to fill in right now». Состояние подхватывает разметку при монтировании | #7 | 1 |
| T088 | core | T026 | done | Базовый путь доведён не до конца: переходы между экранами админки сделаны обычными `<a href>`, которым Next префикс не добавляет — они уводят на корень адреса. Пока это так, продукт публикуется на корне своего адреса (D046), а включённый BASE_PATH предупреждает о себе при старте | #8 | 1 |
| T089 | demo | T085 | done | Сквозной смоук не убирает за собой: данные прогона (`Smokeland …`) остаются в базе, а уборка по имени страны не забирает чек-листы — `checklists.station_id` объявлен `ON DELETE SET NULL`, и они выпадают из выборки. Накопилось 8 лишних чек-листов и 11 версий при трёх настоящих; видно только ручным пересчётом. Смоук должен убирать за собой сам, включая падение посередине, и проверять, что после прогона в базе ровно демо-контур | #9 | 1 |
| T090 | core | T064 | done | Первый экран после входа — тупик: ни одной ссылки в готовые разделы, попасть в них можно было только зная адрес наизусть. Вторая заглушка того же рода, что была на корне. Экран ведёт в разделы, проверка `e2e/admin-home.spec.ts` ходит от входа только по ссылкам | #11 | 1 |
| T091 | core | T090 | done | Боковая навигация разъехалась: четыре копии списка разделов, «Заполнения» и «QR-коды» показывались надписью «Раздел ещё не готов», хотя работали, а из справочника нельзя было уйти никуда. Адреса и готовность сведены в `src/blocks/core/admin-sections.ts`, связность держит `e2e/admin-nav.spec.ts` | #11 | 1 |
| T092 | editor | T026 | done | Чек-лист нельзя удалить: кнопки нет ни в списке, ни в редакторе, пробные копятся в списке. Удаление с экраном подтверждения; без заполнений — стирается целиком, с заполнениями — снимается с работы (`archived_at`, миграция 0005) и уходит из списка и со станции, история цела | #12 | 1 |
| T093 | qr | T035 | done | Код станции нельзя скачать: он только показывается на экране и печатается листом. Кнопка «Скачать» отдаёт наклейку файлом SVG с печатным размером 42 мм — вектор, масштабируется без потери качества | #13 | 1 |
| T094 | data | T011 | done | Уровни пунктов и режимы смены — ядро: `severity.ts` (три уровня, матрица, `severityOf`/`sectionsForMode`/`requiresCommentOnFailure`), `shift-modes.ts` (`getShiftMode`/`setShiftMode`/`listShiftModeChanges`), `grading.ts` (`countUnansweredCritical`), `timezones.ts`, миграция `0006_shift_modes`, `submissions.mode`. Backfill: построено и слито (D051-D056) вне графа задач, заведено задним числом по `docs/furca/specs/2026-09-07-shift-modes-design.md` | — | 2 |
| T095 | fill | T037,T094 | done | Уровень вместо булева признака во всей цепочке заполнения; комментарий обязателен на двух верхних уровнях; режим смены читается на сервере при отправке; переключатель режима в шапке экрана заполнения. Backfill — см. T094 | — | 2 |
| T096 | feed | T044,T094 | done | Уровень и режим в ленте и карточке; `alarms.ts` + `AlarmStrip.tsx` — три вида тревог (провал критичного, критичный без ответа, незаполненный чек-лист); сетевая выборка тревог переведена на `pg_timezone_names`, сломанный часовой пояс одной пиццерии не роняет весь запрос. Backfill — см. T094 | — | 2 |
| T097 | editor | T019,T094 | done | Переключатель уровня пункта на три положения; разбор устаревшего признака `critical` из черновиков; предпросмотр в выбранном режиме смены. Backfill — см. T094 | — | 2 |
| T098 | demo | T049,T095,T096,T097 | done | Сид демо переведён на три уровня, сокращённую смену и заполнение в критичную смену; время демо-заполнений переведено на местные сутки пиццерии. Backfill — см. T094 | — | 2 |
| T099 | demo | T098 | done | Демо не показывает тревогу «критичный пункт без ответа»: все показательные заполнения полные, из трёх видов тревог демонстрируются два (`docs/furca/specs/2026-09-07-shift-modes-design.md`, «Не сделано» п.4) | — | 2 |
| T100 | data | T012 | done | `SubmissionRow` не отдаёт `failedCount` (только `failedCriticalCount`) — лента дочитывает провалы вторым запросом мимо слоя доступа (`src/blocks/feed/failures.ts`). Добавить `failedCount` в `SubmissionRow` там же, где считается `failedCriticalCount`, убрать обходной путь из feed | #2 | 2 |
| T101 | core | T070 | done | `DB_PORT` не задан в `.env.example` — все копии репозитория по умолчанию просят порт 5433 хоста, различает их только имя compose-проекта, не порт. Проверки одной копии могут молча уехать в чужую базу или снести тестовую схему другой копии. Развести порт по умолчанию по стенду или проверять при старте, что подключённая база — своя | #10 | 1 |
| T102 | catalog | T037,T094 | done | По D060: сломанный часовой пояс пиццерии ловится при сохранении в справочнике (проверка против `pg_timezone_names`, внятный отказ на форме на двух языках), публичный маршрут заполнения остаётся с громким отказом — второй режим отказа не заводится. Блок сменён с fill на catalog: по решению работа в справочнике, а не на публичном маршруте | #16 | 2 |
| T103 | demo | T085 | done | По D062: публичный адрес показа переводится на `tailscale funnel` — Chrome помечает `*.trycloudflare.com` как Dangerous из-за репутации случайного домена. Своего домена у владельца нет, поэтому именованный туннель Cloudflare отпадает | #14 | 1 |
| T104 | auth | T066 | done | Проверка «неверный пароль отвечает не быстрее верного» (`src/blocks/auth/password.test.ts`) мигает на общей машине: меряет настенные часы вокруг scrypt, а не саму работу — под покрытием и параллельными файлами задержка планировщика съедает запас. Найдено при разборе T068 (мигал не тот тест, на который указывала формулировка). Проверять целиком scrypt + `timingSafeEqual`, а не время | #19 | 1 |
| T105 | core | T074 | done | Ребрендинг: бренд-токен в каркасе и текстах говорит MERIDIUS; слово «чек-лист» как предмет остаётся. Заведено при сверке графа с трекером 11.09 — задача жила только в трекере | #15 | 2 |
| T106 | feed | T047 | done | Сквозной сценарий ленты падает через раз: ожидание смены адреса после выбора фильтра. Тот же класс, что T086 — проверка ждёт разметку, а не законченный переход. Заведено при сверке графа с трекером 11.09 | #18 | 1 |
| T107 | catalog | T032,T074 | done | Справочник: кнопки «QR-коды станций» и «QR» в строках станций остались неактивными, хотя раздел QR давно готов. Неактивных пунктов в кабинете больше быть не должно — библиотека была последним. Заведено при сверке графа с трекером 11.09 | #20 | 1 |
| T108 | chores | — | todo | ЗА ГОРИЗОНТОМ MVP (этап 3), не считается в готовность MVP. Разбор 13.09 показал, что это не «нарисовать эталон», а проектирование нового функционала: привязать чек-лист пиццерии к вкладке планшета (сотруднику нельзя личный телефон на смене) и уведомления о непокрытой тревоге. Спека помечает уведомления как nice и прямо исключает их (раздел «Не делаем»), а экран QR на планшете (must) уже сделан в `/admin/qr/screen`. Упирается в развилку владельца: у тревоги нет адресата — кто на смене, продукт не знает по D010/D014, и опознание противоречит D001. Развилка РАЗРЕШЕНА 14.09: адресат сигнала — станция, а не человек (D077, дизайн `docs/furca/specs/2026-09-14-rounds-and-expiry-design.md`), уведомления разложены на T134—T141. За этой задачей остаётся только привязка чек-листа пиццерии к вкладке планшета | #17 | 3 |
| T109 | chores | T074 | todo | Сверка экранов кабинета с эталоном (библиотека блоков, фильтры чек-листов, меню во всех пяти разделах): роль сверки входит в кабинет на отдельном стенде с заведомо тестовым паролем — способ выбран решением D059, Q008 закрыт, задача НЕ заблокирована | #30 | 2 |
| T110 | chores | — | todo | Подтянуть патчи стека после сверки с реестром (T054): next 16.3.3 → 16.3.4, next-intl 4.14.1 → 4.14.4. Мажоры (typescript 7, vitest 5) сюда не входят — отдельным решением после стройки | #31 | 2 |
| T111 | chores | — | done | `scripts/check` зовёт сквозные сценарии без `E2E_PORT`, то есть поднимает сервер на общем 3101 — вне диапазонов блоков. Два блок-агента с полным прогоном одновременно перехватывают сервер друг у друга и получают ложные падения. Порт должен браться из настроек копии. Найдено блоком demo 11.09 | #21 | 1 |
| T112 | core | T074 | done | Главная кабинета (`src/app/admin/page.tsx`) идёт без каркаса `AdminShell` — без меню и верхней полосы, в отличие от остальных экранов; рядом с общим `core/ui/AdminShell.tsx` живут ещё две копии, `catalog/ui/AdminShell.tsx` и `editor/ui/AdminShell.tsx`. Сведение каркаса (T074) доведено не до конца. Найдено блоком demo 11.09 Блок catalog подтвердил находку со своей стороны: его копия каркаса живёт без пропа `active`, меню уже разъезжалось однажды (#11). | #22 | 1 |
| T113 | chores | — | done | В `docs/furca/specs/2026-09-07-shift-modes-design.md` пункт 4 списка «Не сделано» больше не верен: тревога в демо показана (T099). Привести документ в соответствие. Найдено блоком demo 11.09 | #24 | 1 |
| T114 | feed | T047 | done | По D063 визуально три вида тревоги НЕ различаем — владелец сказал «хватит текста». Остаётся проверить обратное: что сами три формулировки на ленте различимы и на русском, и на английском (провален критичный пункт / критичный оставлен без ответа / чек-лист не заполнен, окно закрылось), и что эталон ленты, где полосы тревог нет вовсе, приведён в соответствие записью | #23 | 2 |
| T115 | editor | T031 | done | Тот же протухший запрет, что был в справочнике: `editor/ui/SectionCard.tsx:108-117` держит пункт «Открыть блок» с `aria-disabled` и комментарием «раздела библиотеки ещё нет», хотя библиотека построена и `ADMIN_SECTIONS.library.ready = true`. Рядом `PreviewScreen.tsx:252` — серая кнопка в футере предпросмотра, похоже на имитацию экрана заполнения, нужно решение ведущего блок. Найдено блоком catalog 11.09 | #25 | 1 |
| T116 | qr | T032 | done | `qr/ui/view.ts` держит путь раздела строкой `QR_PATH = "/admin/qr"`, хотя тот же факт лежит в `core/admin-sections`, откуда его теперь берёт справочник. Один факт — одно место. Найдено блоком catalog 11.09 | #26 | 1 |
| T117 | catalog | T107 | done | Поле «Страна» в карточке пиццерии (`catalog/ui/StoreCard.tsx:74`) отрисовано как `disabled`: по сути только для чтения, но серое поле читается как «сломано» или «нет прав». Показывать значением. Найдено блоком catalog 11.09 | #27 | 1 |
| T118 | feed | T116 | done | `feed/ui/FeedEmpty.tsx:17` держит собственный `QR_PATH = "/admin/qr"` — тот же дубль, что убран из блока qr. Найдено блоком qr 11.09 | #28 | 1 |
| T119 | catalog | T116 | done | `catalog/ui/view.ts:11` держит `CATALOG_PATH = "/admin/catalog"` мимо `core/admin-sections` — тот же класс дубля. Найдено блоком qr 11.09 | #28 | 1 |
| T120 | chores | — | done | Префикс публичного заполнения `"/s/"` записан дважды: `src/security-headers.ts:7` и `src/blocks/qr/scan-url.ts:12`. В отличие от адресов кабинета, канонического владельца здесь нет, и выбор стороны — решение, а не механическая правка: корневой файл лежит вне границ блока qr. Найдено блоком qr 11.09 | #29 | 2 |
| T121 | editor | T115 | done | Сквозной сценарий редактора «из секции вставленного блока методист уходит в сам блок библиотеки» падает в параллельном прогоне и проходит в одиночном (2 из 2 при полном наборе, 8/8 при прогоне одного файла): помощники сценария жмут кнопки до того, как страница ожила, клик уходит в пустоту, и блок не заводится. Тот же класс, что T086 и T106 — чинится постусловием «страница ожила», а не таймаутом. Туда же находка блока feed: `editor/ui/ChecklistFilterSelects.tsx` устроен так же (`requestSubmit()` в `onChange`), а `checklists-filter.spec.ts` ждёт только смену адреса — не мигает пока случайно, потому что в каждом тесте ровно один выбор сразу после перехода | #32 | 1 |
| T122 | fill | T079 | done | В `src/messages/ru.json` ключ `fill.left` содержит латинскую `c` внутри кириллического слова («осталcя»). Это строка настоящего экрана заполнения — её видит сотрудник на кухне. Найдено блоком editor 11.09; больше смешанных слов в обоих словарях нет | #33 | 1 |
| T123 | chores | — | done | Нет сторожа «ключ, названный в коде, есть в словаре». Проверено фактом: при удалении секции словаря next-intl нарисовал сам ключ вместо текста, в журнал сервера ушла одна строка MISSING_MESSAGE, и ВСЕ 127 сквозных сценариев остались зелёными. Тихий отказ, который не ловит ни один гейт. Найдено блоком core 11.09 | #34 | 1 |
| T124 | core | T112 | done | Из раздела в главную кабинета вернуться нечем, кроме кнопки браузера: бренд в меню не ссылка. С появлением каркаса на главной асимметрия стала заметнее. Найдено блоком core 11.09 | #35 | 2 |
| T125 | chores | — | done | След переименования каталога: старое имя каталога (`docs/` + `forge`) стояло в 55 местах 37 файлов, хотя каталог давно `docs/furca`. Правка задевает файлы всех блоков сразу, поэтому делается вне блочной работы. Найдено блоками core и qr 11.09 | #36 | 2 |
| T126 | feed | T106 | done | Полоса тревог не держит фильтр станции: при порче view.ts, выбрасывающей stationId, сценарий полосы остался зелёным. Найдено блоком feed 11.09 отрицательным прогоном | #37 | 1 |
| T127 | editor | T115 | done | Сторож мёртвых элементов управления (editor/dead-controls.test.ts) накрывает только разметку редактора. После T117 поле «Страна» перестало быть отключённым, то есть препятствие снято — сторож можно расширить на весь кабинет одной проверкой. Найдено блоком editor 11.09 | #38 | 2 |
| T128 | editor | T123 | done | Третья копия приёма снятия комментариев живёт в `editor/dead-controls.test.ts`. Общий помощник заведён (`core/source-text.ts`), `admin-links.test.ts` переведён; эту копию не трогали, потому что блок был в работе. Перевести на общий | #39 | 1 |
| T129 | editor | T121 | done | Осталась ТОЛЬКО таблица отрицательного прогона в журнале блока: сама починка сделана и принята 13.09 (окно смены отправляет сам список, `cb317e3`), заголовок и введение к таблице написаны, самой таблицы нет — агент оборвался на ней дважды подряд. Нужно прогнать порчи классом (три ломают опоры починки, четыре ломают продукт, одна ставится сбоку — в соседний экран) и записать, что сказала каждая проверка | #40 | 1 |
| T130 | library | T121 | done | `e2e/library.spec.ts:33` — помощник того же устройства, что был в редакторе до T121: считает экран готовым по разметке, а не по тому, что он ожил и принадлежит нужному блоку. Зелен сегодня по случайности. Найдено блоком editor 12.09 | #41 | 1 |
| T131 | core | T121 | todo | Приём «продукт сам говорит, что ожил» разошёлся: свой вариант в feed, `use-live.tsx` в editor, атрибуты в library. Свести можно только через core — это решение о месте хука. Найдено блоком editor 12.09 | #42 | 2 |
| T132 | chores | — | done | Карты соответствия `docs/furca/design/map.md` (модуль эталона → блок → задачи) у проекта нет: десять экранов эталона, и проверить, что каждому сопоставлен блок, нечем. Модуль без блока стройка не увидит вовсе. Заведено при продолжении стройки 13.09 — требование новее проекта, проверка не делалась ни разу | #43 | 2 |
| T133 | editor | T128 | done | Четвёртая копия приёма снятия комментариев: `editor/routes.test.ts` объявляет свою `withoutComments`, хотя общий помощник заведён в T128 и `dead-controls.test.ts` уже на него переведён. Найдено при приёмке волны 13.09 | #44 | 2 |
| T134 | data | T012 | done | Расписание периодических проверок в модели: `Item.schedule` набором отрезков `[{from, to, everyMinutes}]` на самом пункте (D075), вывод интервалов местных суток с обрезкой по окну чек-листа и поштучно по режиму смены (D067). Нет поля — пункт обычный, миграции данных нет: опубликованное до сегодня читается как прежде (принцип 3, D002). Местное время считает PostgreSQL по `stores.timezone` (D026), второго календаря не заводим | #47 | 3 |
| T135 | data | T134 | done | Отметки периодических проверок таблицей `checks` (только пополняется): `station_id`, `checklist_version_id`, `item_id`, `interval_start`, `at`, `value`, `comment`. Не заполнением: заполнение несёт снимок всех секций, и класть его на каждое нажатие значило бы десятки копий документа за смену; снимок отметке не нужен, версии неизменяемы. Главное правило D066 — отметка всегда встаёт в ТЕКУЩИЙ интервал, пропуски остаются пропусками и нигде не хранятся, а выводятся из расписания при чтении, как тревоги сегодня (D053) | #48 | 3 |
| T136 | fill | T079,T135 | done | Экран станции: состояние проверки вместо сетки («до 12:00» / «пропущено 2 · проверить сейчас» / «сделано в 11:05»), одна свёрнутая строка с ближайшим временем обхода (D076), разворот отметок за текущие местные сутки БЕЗ ВХОДА в кабинет (D077). Правит первую меру D021 узко: только отметки обхода, только текущие сутки, только чек-лист этой станции, без комментариев; ограничение частоты отправок и ротация кода (D006) не трогаются. Отрицательным прогоном проверить, что по коду станции не достать ни чужую станцию, ни вчерашний день | #49 | 3 |
| T137 | editor | T031,T134 | done | Редактор: чип регулярности в строке пункта открывает окно настройки — набор отрезков с кнопкой «добавить отрезок», частота напоминания при просрочке (10/20/60 минут или молчать, D068), кнопка «применить ко всей секции». Секция носителем расписания НЕ становится: перенос настройки на семь строк — работа редактора, а не сущность модели (D075). Предпросмотр показывает состояние пункта, а не сетку | #50 | 3 |
| T138 | fill | T136,T137 | todo | Сигналы о просроченной проверке: поднимается по закрытию интервала без отметки (до закрытия станция молчит и показывает отсчёт), повторяется с настроенной частотой, гаснет с отметкой. Звонит СТАНЦИЯ, а не пункт: три просрочки — один звонок по самой частой из настроек. Адресат — место, а не человек, этим снимается развилка T108 без отхода от D001; эскалации наружу нет (D073). Ограничение «звук работает, пока вкладка открыта» назвать в интерфейсе, а не замолчать | #51 | 3 |
| T139 | fill | T136 | done | Будильники блоком чек-листа (D070): сотрудник вручную вносит время и подпись, планшет звонит. Разовый, живёт до конца местных суток станции; хранится строкой на станцию и сутки, потому что переживать перезагрузку планшета обязан — это записка на сегодня, а не история, и правило неизменяемости на него не распространяется. Справочника продуктов, журнала линии и учёта списаний НЕ заводим (D069). В редакторе — переключатель блока, поверхность маленькая | #52 | 3 |
| T140 | feed | T047,T135 | done | Отчёт сеткой «пункты × интервалы» с пропусками: сетка часов переезжает туда, где нужна (D065). Единственное место, где таблица отвечает на настоящий вопрос — в какие часы обход сыпется, — и разговор получается про нагрузку и расстановку, а не про то, кто виноват. Фильтры те же, что у ленты | #53 | 3 |
| T141 | editor | T137 | todo | Табличный пункт для замеса теста: третий род пункта — таблица с нормами по колонкам, колонки задаёт методист, строки заводит сотрудник. Нужен в пилоте, но без углубления (D074): клонирование замесов и вынос живут в отдельном сервисе. Источник — вкладки `Dough mixing, production` и `Dough mixing, fridge` присланного файла; отдельного файла не будет, владелец подтвердил 14.09 | #54 | 3 |
| T142 | chores | T146 | done | Остаток снятия костыля (D079): 104 почасовых чек-листа свёрнуты 14.09 в 8 обычных с расписанием на пунктах, зоны легли пятью пунктами вместо пятнадцати часов. Не снято: 8 станций-дублей `*-round` со своими наклейками — они держатся дефектом #60 (станция отдаёт один чек-лист, и обход молча съел бы приём смены у менеджера и закрытие у кассы). Убирать после T146 | #56 | 3 |
| T143 | feed | T047 | done | Тревога `criticalUnanswered` поднимается в момент отправки, а не когда дозаполнить уже нельзя: неполное заполнение принимается сознательно (`matchAnswersToSnapshot`), и каждый неотвеченный критичный пункт сразу даёт тревогу о работе, которую ещё не бросили. Поднимать по закрытию окна чек-листа за эти сутки — тем же правилом, по которому считается `missed` (D054). Указал владелец 14.09: «ты подсвечиваешь нашу недоработку, а не то что мы не можем это сделать» | #57 | 3 |
| T144 | chores | — | done | Снимки схемы в `migrations/meta` отстают с 0006, и `npm run db:generate` сегодня выдаст миграцию, которая заново создаёт `store_shift_modes`, `submissions.mode` и `checks`. Накат от этого не страдает (Drizzle читает журнал и сами `.sql`), ломается генерация — молча и правдоподобно. Решить записью: либо признать, что миграции пишутся руками (так и есть с 0006: комментарии и обратный файл генератор не пишет) и убрать `db:generate` из `package.json`, либо восстановить снимки и держать их в согласии | #58 | 3 |
| T145 | chores | T136 | done | Эталон `docs/furca/design/screens/fill.html` не знает панели обходов: заголовок секции, строка состояния («Проверить до 12:00» / «Сделано в 11:05» / «Обходы закончены»), вторая строка «Пропущено 2 · Следующий в 12:00», кнопки «Порядок»/«Непорядок» для да/нет и поле для числа и текста, свёрнутый список обходов за смену. Эталон выглядит убедительно и врёт: следующий, кто сверяет экран, увидит «лишний» блок и снесёт его | #59 | 3 |
| T146 | data | — | done | Станция отдаёт ВСЕ подходящие по времени чек-листы, а не первый по началу окна (`getPublishedVersionForStation` с `.limit(1)`). На боевом пакете пересекаются: обход менеджера накрывает приём смены целиком, обход кассы — закрытие с 21:00, обход тестоцеха — открытие с 08:00. Экран станции показывает список, если подходящих несколько, и сам чек-лист, если он один. Отдельно подтвердить прогоном, что тревога о незаполненном чек-листе считает по чек-листам, а не «по одному на станцию» | #60 | 3 |
| T147 | chores | — | todo | Кольцо фокуса нарисовано тенью, поданной как цвет обводки: `outline-[var(--focus-ring)]` в шести местах (`fill/ui/RoundsPanel.tsx`, `fill/ui/ShiftModeBar.tsx`, `feed/ui/AlarmStrip.tsx`, `editor/ui/ItemRow.tsx`), а `--focus-ring` в токенах — тень `0 0 0 2px …, 0 0 0 4px …`. Браузер объявление отбрасывает, и фокус рисуется цветом по умолчанию — молча и по-разному в светлой и тёмной теме. Решить сознательно: рисовать кольцо тенью, как задумано токеном, или завести отдельный токен цвета обводки (вопрос к эталону, а не к коду) | #62 | 3 |
| T148 | chores | — | done | Импорт печатает ссылки станций без базового пути площадки: после заливки пакета на стенд показа (`/qr`, D045) напечатанная «ссылка из QR» ведёт в 502, тогда как та же ссылка с префиксом открывается. Единственное место, где человек видит ссылку сразу после заливки, и выглядит она убедительно. Передавать `publicBasePath` в `stationScanUrl` либо печатать так же, как строит экран QR | #63 | 3 |
| T149 | library | — | done | Блоки библиотеки из боевого пакета (задача владельца 15.09: «подготовь логически блоки заранее из того что у нас уже есть»). Замер: дословных повторов между станциями НОЛЬ, 175 пунктов написаны каждый под свою станцию — механическим поиском одинаковых строк блок не собрать. Пересечения по смыслу: уборка (71 пункт, 9 станций), оборудование (31/7), склад и запасы (28/9), допуск к смене (20/6), персонал и смена (17/5), температуры (15/6), деньги и касса (12/4), сроки годности (5/3). Сделано 15.09: шесть блоков заведены импортом и ни в один чек-лист не вставлены (D085). Температуры блоком не делаются сознательно — у каждой точки своя норма. Вставка блока — решение методиста | — | 3 |
| T150 | chores | — | done | Раскатать на стенд показа то, что уже в main: правку обрезанного заголовка секции (7433319) и удаление старых демо-чек-листов. Сделано 15.09: стенд на main, в списке ровно девять чек-листов пилота, у каждого `v1 published` со станцией. Три старых чек-листа не удалились, а ушли в архив — по ним есть заполнения, и продукт бережёт историю (принцип 3, D002); из списка методиста они пропали, чего и добивались | — | 3 |
| T152 | editor | — | done | Метка `DRAFT` в списке чек-листов горит по признаку «строка черновика существует» (`has_draft`, `listing.ts:105`), а `publishVersion` оставляет черновик жить после публикации (`checklists.ts:213`) — значит метка горит ВСЕГДА. Проверено на стенде: у всех девяти чек-листов пилота `draft.sections = published.sections` побайтово, метка горит у всех девяти. Методист читает её как «есть неопубликованные правки»; постоянный шум неотличим от настоящего расхождения. Показывать по различию содержимого, а не по наличию строки | #64 | 3 |
| T151 | catalog | — | done | Демо-пиццерии «Demoland, Central Square» и «Demoland, Riverside» снесены целиком по решению владельца (D087/D088) и локально, и на стенде показа: в каталоге осталась одна «Demoland, Pilot» с девятью станциями, сверено запросом к обеим базам. Демо-заполнения сняты руками (14 строк локально, 13 на стенде), сам снос сделан штатным `deleteStore` с подтверждением. По ходу вскрылся дефект #66: пиццерию держала строка `store_shift_modes`, а отказ называл причиной заполнения | — | 3 |
| T153 | chores | — | done | Импорт печатает список ссылок станций русским именем (`scripts/import-checklists.mjs:497`, `s.name.ru`), хотя страна показа английская и в базе имя английское. Человек раскладывает напечатанные QR по одному имени, а сотрудник видит на экране другое. Печатать по локали страны с запасным `en`. Рядом та же строка — #63 (ссылка без базового пути площадки), чинить одним заходом | #65 | 3 |
| T154 | catalog | — | done | Пиццерию с заданным режимом смены нельзя удалить никогда: `store_shift_modes` держит её внешним ключом `restrict`, а `deleteStore` про режим не знает. Хуже отказа — его текст: `asHistoryConflict` переводит ЛЮБОЙ конфликт ключа в «ссылаются заполнения», и человек ищет историю, которой нет (проверено на базе с нулём `submissions`). Снимать режим в той же транзакции и различать конфликты по имени ограничения | #66 | 3 |
| T155 | chores | — | done | У публичного репозитория нет README: по ссылке видно список файлов вместо ответа «что это и как посмотреть». Короткий README: что за продукт, две поверхности (кабинет методиста и заполнение по QR), как поднять одной командой, где демо | #45 | 3 |
| T156 | core | — | done | Локальный сервер живёт сутками и молча отдаёт код недельной давности: на 3100 висел `next-server` от 11.09 и ничего не знал ни о пакете, ни об обходах. Дважды поймало людей, оба раза причину искали в данных. Нужен сторож: продукт называет свою сборку (коммит и время запуска) так, чтобы расхождение было видно без расследования | #61 | 3 |
| T157 | chores | — | todo | Сверка построенного со спекой, итерация 1: свежий взгляд проходит сценарии и критерии готовности MVP из `docs/furca/spec.md` как пользователь, а не чтением кода — включая пути отказа (пустой ввод, недоступная зависимость, повторный запрос, отменённая операция) и права (чужое от лица того, кому оно не принадлежит, и неавторизованного). Ни один из тринадцати критериев готовности в спеке не отмечен, то есть фаза не закрывалась ни разу | — | 3 |
| T158 | chores | — | done | Гейт `pre-push` (`.githooks/pre-push` = полный `./scripts/check`) отказывает в пуше ложно: под нагрузкой модульные проверки с базой падают по `Test timed out in 5000ms`, сквозные при этом зелёные. Гейт существует, чтобы сломанное не уезжало наружу, а на деле держит состояние на одной машине — 16.09 `main` ушёл на 36 коммитов вперёд `origin/main`. Дешевле всего `--maxWorkers=2` в пуш-гейте (проверено, идёт чисто) | #67 | 3 |
| T159 | fill | T139 | done | Будильник живёт до конца окна времени чек-листа, а не до местной полуночи (D090). Окно, переходящее через полночь, допускает время после полуночи: пиццерия работает до 02:00 — в 23:40 будильник на 00:30 ставится и звонит. Прошедшее время внутри окна по-прежнему отвергается вслух, «значит, завтра» не выдумываем. Существенно по хранению: записи ключуются `localDate` станции (`src/blocks/fill/alarms.ts`), а будильник на 00:30 принадлежит уже следующей дате — выборка «на сегодня» обязана захватывать хвост окна, иначе поставленный в 23:40 исчезнет из панели в 00:00 | #68 | 3 |
| T160 | data | T137 | done | Окно «без ограничения» (`00:00`–`24:00`) молча отключает все обходы периодического пункта: `TIME_PATTERN` в `src/blocks/data/schedule.ts:19` час 24 не разбирает, `windowLength` отдаёт `null`, `intervalsForItem` превращает его в пустой список — ни отказа, ни следа. Редактор при этом `24:00` для окна разрешает явно. Ловушка: общий `parseLocalTime` читает ещё границы отрезков и время отметки, где час 24 бессмыслен, — граница суток законна только как КОНЕЦ окна | #69 | 3 |
| T161 | data | T137 | done | Пересекающиеся отрезки расписания не отклоняет никто, и это даёт ЛОЖНЫЙ пропуск в отчёте: отметка встаёт в интервал одного отрезка, интервал второго закрывается без отметки и считается пропущенным. Выбрать и записать решением одно из двух: пересечения запрещены (отклонять в `assertValidSchedule` и гасить кнопку в окне настройки — предел должен быть один) либо разрешены (тогда отметка обязана закрывать ВСЕ активные в этот миг интервалы). Первое проще и честнее | #70 | 3 |
| T162 | fill | T159 | todo | Переход на летнее время не покрыт ничем в расчёте окна будильника: `windowOpensAt`/`windowClosesAt`/`sameClockOn` (`src/blocks/fill/alarms.ts:206-233`) строят ГИПОТЕТИЧЕСКУЮ местную отметку и переводят её через `at time zone`, а такая точка в ночь перевода либо не существует, либо случается дважды — Postgres молча вернёт один момент, не обязательно тот. До миграции 0009 приводился реальный существующий момент, и неоднозначным это не бывало. Тест честно объявляет пояс без DST выбранным намеренно. Для пилота по России риск нулевой, но контур заведён под другие страны | #72 | 3 |
| T163 | core | — | todo | `npm run dev` и `npm run start` прибиты к порту 3100 литералом и `PORT` копии игнорируют, при том что `scripts/up` его уважает — два пути запуска расходятся молча. Блок-агент со своим диапазоном портов получает сервер на общем 3100: либо выбивает чужой стенд, либо цепляется к чужому серверу и проверяет не свою сборку. Второе ловило людей дважды (дефект #61). Тот же класс, что починенный T111/#21 для playwright: брать порт из одного источника на все пути запуска | #71 | 3 |
| T164 | chores | — | todo | Погашенные кнопки по всему продукту гаснут до 60% при эталонных 45% (`.btn:disabled { opacity: .45 }`): расхождение общее, восемь блоков, ровно один экран сделан по эталону (`fill/ui/FillForm.tsx`). Разница на глаз небольшая, но она ровно про то, ради чего гашение существует — кнопка должна читаться недоступной с первого взгляда, а не «какой-то бледной» | #74 | 3 |
| T165 | feed | T140 | done | Круглосуточное окно (`00:00`–`24:00`) молча выпадает из отчёта по обходам: `rounds-days.ts:96-102` несёт дословную копию старой сломанной `windowLength`, `parseLocalTime` час 24 не разбирает, `passDatesOf` отдаёт пустой список. Регрессия только что закрытого #69 в новом файле того же дифа, и причина структурная: починка #69 завела `parseWindowEnd` приватной, поэтому сломанный вариант остался тем, что получается, если написать очевидное. Чинить не копию, а возможность её завести: вынести разбор конца окна и `windowLength` в `data` единственным публичным источником, копию УДАЛИТЬ, проверку на `24:00` в отчёте написать до починки | #75 | 3 |
| T166 | data | T165 | done | Третья копия разбора конца окна через общий `parseLocalTime` — `checks.ts:215-220`, `passLocalDate`. Сегодня НЕ врёт, и это совпадение: на круглосуточном окне `end` равен `null`, функция уходит в запасную ветку и возвращает тот же ответ, что дала бы верная. Оставленная строка — заряженная мина: следующая правка переставит порядок условий, запасная ветка перестанет совпадать с верной, и сломается так же тихо, как дважды до этого. Проверка обязана показывать, что выбрана ВЕРНАЯ ветка, а не что ответ совпал. Заодно пройти блок `data` целиком в поисках четвёртой копии | #76 | 3 |
| T167 | data | — | todo | Неразобранное начало окна молча становится полуночью: `checks.ts:284` и `fill/rounds-view.ts:66` пишут `parseLocalTime(window.start) ?? 0`. Сегодня не врёт — началом окна «24:00» быть не может, — но приём тот же, что трижды выстрелил 16.09: тихое умолчание вместо отказа там, где разбор может не удаться, и разница между «не врёт» и «верно» держится на внешнем обстоятельстве. Либо ронять понятную ошибку, как уже сделано для конца окна в `passLocalDate`, либо объяснить в комментарии, почему полночь здесь безопасна, и закрепить проверкой | #77 | 3 |
| T168 | fill | T159 | todo | Поле времени будильника не названо на экране ничем: у обоих полей `<label>` несёт `sr-only`, но у поля подписи есть хотя бы серый пример внутри, а у поля времени `placeholder` пустой — только нативные прочерки и иконка часов. Замерено живьём 16.09 на 375 px в обоих языках. Эталон свой единственный ввод подписывает видимым `.field__label`; панели будильников в эталоне нет, поэтому расхождение с общим языком эталона, а не с конкретным экраном. Попутно: поле времени рисует нативную иконку часов, а ни один ввод эталона иконок не несёт. Решение владельца D093: вернуть видимые надписи «Во сколько» и «Зачем» над полями, подсказка внутри поля достаточной не признана; форма растёт на строку — принято. Указание D094 тем же заходом: добавить к полям пиктограммы и показать владельцу — это отступление от эталона, решается после показа | #78 | 3 |
| T169 | chores | — | todo | Два отступления блока `feed` от эталона не записаны признанными в `docs/furca/design/map.md`, в отличие от полосы тревог: (1) ссылка «Отчёт по обходам» в шапке ленты — экран отчёта достижим быть обязан, значит нужна либо правка эталона, либо признание отступлением; (2) пятая ячейка «режим смены» (требует D055) встаёт одна во втором ряду сетки фактов на четыре колонки — эталон старше решения. Найдено сверкой 16.09; пока не записано, каждая следующая сверка будет находить это заново как расхождение | — | 3 |
| T170 | chores | T168 | todo | Показать владельцу панель будильника с возвращёнными надписями и пиктограммами (D094) и закрыть отступление: пиктограммы у вводов эталон не несёт нигде, поэтому после показа либо правится эталон, либо отступление записывается признанным в `docs/furca/design/map.md`. Пока не закрыто — у продукта есть видимый элемент, которого эталон не объясняет | — | 3 |
