<!-- managed by forge: do not change the row format below — forge-build and forge-status parse this file -->
# Questions for the owner

<!-- id: Qnnn (Q001, Q002, ...). status: open (waiting for an answer) | answered (the answer is in the "answer" column) | auto (decided autonomously by the dispatcher; the "answer" column holds a reference of the form "→ Dnnn" into decisions.md). "blocks" — task ids from tasks.md, comma-separated, or "—". -->
<!-- The last line of the "question" column carries the note "sent YYYY-MM-DD". Without it "the question is written down" and "the question was asked" are indistinguishable, and the next session either asks it a second time or waits for an answer the owner never promised, because the owner never saw the question. -->

| id | status | blocks | question | answer |
|---|---|---|---|---|
<!-- | Q001 | open | T004 | Example question for the owner? | — | -->
| Q001 | open | — | Есть ли дата или событие, к которому продукт нужен? (спрошено 2026-09-05) | — |
| Q002 | answered | T053 | Имя репозитория на GitHub и его владелец: личный аккаунт GarroV или организация Dodo? (спрошено 2026-09-05) | Личный аккаунт GarroV, публичный репозиторий; имя `dodo_qr_service` по образцу соседних проектов → D039, порядок публикации → D036 |
| Q003 | answered | — | Где продукт живёт после пилота, в контуре компании: какая площадка, кто её даёт? (спрошено 2026-09-05) | Продукт ставим на MUSPELHEIM, домашний сервер владельца → D038. Контур компании остаётся за горизонтом MVP (D008) |
| Q004 | answered | — | Есть ли бюджет на внешние сервисы после переезда в контур компании? (спрошено 2026-09-05) | Бюджета нет, строим MVP → D022 |
| Q005 | answered | T079 | Продукт живёт на MUSPELHEIM, а чек-лист заполняют с телефона на кухне: без Tailscale телефон адреса сервера не видит. Публикуем страницу заполнения наружу через `tailscale funnel` или сотрудники ставят Tailscale? (спрошено 2026-09-06) | `tailscale funnel` на своём порту → D034 |
| Q006 | answered | T102 | Публичный маршрут заполнения падает целиком, если PostgreSQL не признаёт часовой пояс пиццерии (issue #16) — лента тревог такой отказ уже сделала мягким (пиццерия выпадает из выборки, а не роняет весь запрос, `pg_timezone_names`). Переводить ли на тот же мягкий отказ и публичный маршрут (сканирование QR не отказывает, но сломанный пояс отдаёт «нет чек-листа» молча), или оставить громкий отказ как сейчас и ловить сломанный пояс отдельно, в справочнике пиццерий? Рекомендация автора issue — второй вариант: тихий отказ на кухне хуже громкого (спрошено 2026-09-09, отправлено 2026-09-09; владелец 2026-09-10 ответил встречным вопросом, разъяснение с рекомендацией отправлено 2026-09-11) | Владелец 11.09 ответил «ок» на рекомендацию: проверять пояс при сохранении пиццерии, публичный маршрут оставить с громким отказом → D060 |
| Q007 | answered | T103 | Публичный адрес показа (Cloudflare quick tunnel, `*.trycloudflare.com`) Chrome помечает красным «Dangerous» — репутация случайного домена, не проблема сертификата (issue #14). Взять именованный туннель Cloudflare на домене владельца, или вернуться на `tailscale funnel` (D045) на отдельном порту? Если именованный туннель — на каком домене? (спрошено 2026-09-09, отправлено 2026-09-09; владелец 2026-09-10 ответил встречным вопросом, разъяснение с рекомендацией отправлено 2026-09-11; ответ «2. а» от 11.09 сопоставить достоверно не удалось, вопрос задан заново буквами 2026-09-13) | Владелец 13.09: «Домена нет на данный момент, делаем через туннель» → D062, остаёмся на tailscale funnel |
| Q008 | auto | T109 | Сверку экранов с эталоном делает отдельная роль на живом стенде, и она упирается во вход в кабинет: пароль основной копии лежит в её `.env`, читать его значение нельзя, а две безопасные обходные попытки (временно подставить в копию e2e-хэш из `e2e/admin-credentials.ts`, поднять отдельный стенд на своём порту с тем же хэшем) автоматический сторож отклонил как ослабление безопасности. Как давать проверяющему доступ: (а) выдать пароль локального кабинета разово, (б) разрешить отдельный стенд сверки с заведомо тестовым паролем (тот же, что уже используют сквозные сценарии), (в) сверять только на стенде, который поднимает Playwright, и научить сверку им пользоваться? Рекомендация: (б) — пароль тестовый и уже лежит в репозитории открытым текстом, отдельный стенд не трогает настройки рабочей копии. Дополнено 11.09: то же самое упирается в приёмочный смоук — `scripts/mvp-smoke.mjs` требует `--password`, и без него приёмка блока опирается на прогон в копии блока, а не в основной копии (спрошено 2026-09-11, отправлено 2026-09-11) | Владелец 11.09: «реши сам эту проблему, мне надо чтобы был готовый продукт» → принят вариант (б), D059 |
| Q009 | answered | T114 | Лента показывает три разных вида тревоги (критичный пункт не выполнен; критичный пункт оставлен без ответа; чек-лист не заполнен, окно закрылось), но на экране они различаются только фразой — стиль один красный на все три, а в эталоне ленты полосы тревог нет вовсе. Различать ли их визуально (тогда нужен третий «плохой» цвет тега — сейчас в дизайн-системе их два), или достаточно текста? Рекомендация: различать — «не заполнили вовсе» и «сделали, но плохо» требуют разной реакции управляющего (спрошено 2026-09-11, отправлено 2026-09-13 — записан 11.09, но отправлен не был: ровно тот разрыв, из-за которого «вопрос есть» и «вопрос задан» неразличимы) | — |
| Q010 | answered | — | Обходы: на бумаге почти каждую сетку часов подписывают двое — сотрудник сделал, менеджер проверил («Supervisor checked ✅», в туалете две подписи). При отказе от опознания (D001) «проверил менеджер» — это ещё одно нажатие неизвестно кем. (а) отказаться от второй подписи; (б) принимать её отдельным заполнением того же слота с пометкой «проверка», анонимно; (в) завести менеджеру короткий код и этим отойти от D001. Рекомендация: (б) — анонимная вторая отметка всё равно показывает, обошёл менеджер станцию или нет, а код тянет за собой администрирование, которого пилот не окупает (спрошено 2026-09-14, отправлено 2026-09-14) | Владелец 14.09: «по поводу подписи это плевать» → D072, второй подписи не будет |
| Q011 | answered | — | Круглосуточные пиццерии: семь вкладок пакета устроены как один документ с осью смены (Night/Morning/Evening и знак Х там, где пункт в эту смену не делается). Сегодняшними средствами это три чек-листа с разными окнами — дублирование пунктов, для которого в редакторе есть кнопка. Заводить ли вторую ось поверх матрицы режимов (D056), или дублирования достаточно? Рекомендация: дублирование — вторая ось умножается на три режима и даёт девять состояний пункта, которые методист держать в голове не сможет (спрошено 2026-09-14, отправлено 2026-09-14) | Владелец 14.09: «мы же можем по времени выставлять, когда чек-лист работает» → D071, оси смены не заводим |
| Q012 | answered | — | Эскалация: будильник станции (дизайн от 14.09) звонит в место, а не человеку, и этим снимает развилку T108 для линии. Но управляющему он молчит, а у управляющего адресат есть — учётная запись. Нужна ли эскалация пропуска и просрочки в канал (почта, Telegram) до пилота, или полосы тревог в кабинете достаточно? Канал наружу — внешняя служба, которой в MVP нет, и фоновая работа, которой нет в архитектуре. Рекомендация: полосы достаточно до первого пилота, эскалацию заводить по жалобе, а не заранее (спрошено 2026-09-14, отправлено 2026-09-14) | Владелец 14.09: «звонит будильник на месте, в отчётах это всё собирается» → D073, эскалации наружу нет |
| Q013 | answered | — | Журналы-таблицы: четыре вкладки пакета — таблицы, строки которых заводит сотрудник, а не методист (замесы теста 1…14 с нормами над колонками, свободный список списаний). Нужны ли они в пилоте? Это новый тип пункта и отдельный заход. Рекомендация: списания закрываются журналом линии начинения бесплатно, а замес теста отложить — иначе тестоцех оцифровывается наполовину, и это надо сказать вслух тем, кто его ведёт (спрошено 2026-09-14, отправлено 2026-09-14) | Владелец 14.09: «замес теста также необходим… сильно не акцентироваться» → D074; свободный список списаний выпадает вместе с D069 |
| Q014 | answered | — | Справочник продуктов со сроками жизни на линии начинения — новая справочная сущность. По D051 она заводится совместимой с пространствами стран (D050). Кто её ведёт: страна (срок годности — вещь страновая и регуляторная, один список на сеть заведомо неверен), или управляющая компания одним списком с правом страны отклоняться? Рекомендация: страна (спрошено 2026-09-14, отправлено 2026-09-14) | Владелец 14.09: «это сервис только про чек-листы в онлайне, всё, не больше» → D069, справочника продуктов и учёта не будет |
| Q015 | answered | T139 | Будильник станции нельзя поставить на время после полуночи: решение D070 говорит «живёт до конца местных суток станции», и блок понял это буквально — прошедшее время отвергается вслух, а не уезжает на завтра. Пиццерии, работающей до 02:00, это помешает: в 23:40 поставить будильник на 00:30 будет нельзя. Варианты: (а) оставить как есть — записка строго на сегодня, просто и предсказуемо; (б) разрешить время до конца рабочей смены, а не суток — сложнее, надо знать смену; (в) считать, что время меньше текущего означает завтра — привычно по будильникам телефона, но тихо меняет смысл «до конца суток». Рекомендую (в): именно так работают будильники, к которым человек привык, и сюрприза не будет (спрошено 2026-09-16, отправлен 2026-09-16, отвечен 2026-09-16) | D090 |
| Q016 | answered | T140,T109 | Обязан ли кабинет методиста работать с телефона? Сверка с эталоном нашла: боковое меню не схлопывается ниже 768 px (`app.css:24` — `grid-template-columns: 208px 1fr`, ни одного `@media` во всём файле), и на 375 px под содержимое остаётся полоса ~165–170 px из 375: заголовок в три строки, крошки в пять, значения в списках обрезаны. При этом адаптив внутри блока `feed` сделан верно — фильтры встают в столбец как надо, — то есть нечитаемым экран делает каркас, и всякая правка внутри блоков будет чинить симптом. Эталон границу объявляет (`tokens.css:113`, `--page-fold: 768px`, «ниже — только сценарии управляющего»), но не говорит, обязан ли кабинет ниже неё работать, а комментарий читается двусмысленно: кабинет и есть экран управляющего. Варианты: (а) да — каркасу нужен настоящий адаптив со схлопывающимся меню, работа блока `core`; (б) нет — снять ожидание: перестать снимать кабинет на 375 px и записать границу решением, чтобы следующий не чинил то, чего от продукта не требуют. Рекомендую (б) для пилота и (а) потом: заполняют с планшета и телефона сотрудники, а методист садится за стол — но снимок отчёта на 375 px сняли не просто так, поэтому спрашиваю, а не решаю (спрошено 2026-09-16, отвечен 2026-09-16) | D092 |
| Q017 | answered | T139 | У полей будильника нет видимого микро-лейбла, только подсказка внутри поля, — эталон на этом экране лейбл держит. Нашла сверка панели будильников с эталоном. Варианты: (а) вернуть лейблы и вырастить форму на строку; (б) дорисовать эталон без них, признав подсказку внутри поля достаточной. Рекомендую (а): подсказка внутри поля исчезает, как только человек начал печатать, и на маленькой форме из двух полей «время» и «подпись» перепутать их легко, а панель живёт на планшете в шумной кухне. **Переспрошен 2026-09-16 простыми словами** — первая формулировка была непонятна («нихуя не понял»), и это моя вина: спрашивал терминами вёрстки вместо картинки экрана. Заодно в тот же вопрос добавлена вторая половина того же экрана: при нескольких открытых чек-листах шапка показывает часы одного из них, а отказ будильника — объединённые рабочие часы станции, и это разные числа на одном экране (спрошено 2026-09-16, переспрошен 2026-09-16) | — Ответ владельца 16.09: вариант (а), см. D093; там же D094 про пиктограммы. |
| Q018 | answered | T198 | Чип регулярности (расписание пункта) есть в редакторе чек-листа и отсутствует на экране правки блока библиотеки — потому что у блока нет своего окна времени: один и тот же блок живёт сразу в нескольких чек-листах с разными окнами, и от какого из них считать первый отрезок, продукт не знает. Варианты: (а) так и оставить — расписание принадлежит чек-листу, а блок несёт только набор пунктов, и на экране блока это сказано словами, а не пустым местом; (б) дать блоку собственное окно по умолчанию, от которого считается расписание, а чек-лист его переопределяет; (в) показывать расписание в блоке относительными отрезками, без привязки к часам. Рекомендую (а): это то, что продукт делает уже сейчас, и оно единственное не создаёт двух источников правды об окне. отправлен 2026-09-17 | Ответ владельца 2026-09-17: чип должен быть, блок в админке выглядит как блок в чек-листе → D096; способ отображения (относительный отрезок, своё окно не заводим) → D097 |
| Q019 | answered | — | Окно времени чек-листа: методист выбирает из трёх готовых («Утро 06:00–11:00», «Вечер 20:00–00:00», «Без ограничения»), а в демонстрационных данных есть 05:00–17:00 и 06:00–23:00 — то есть окна, которые руками завести было нельзя. Блок сделал то, что не связывает руки: список остался, рядом появились два поля времени, и своё окно сильнее списка, когда заполнены обе границы. Вопрос в том, что оставить надолго: (а) так и оставить — список для частых случаев плюс свои поля для остальных; (б) убрать список вовсе, оставить только ввод времени — меньше элементов на экране, но частый случай набирают руками каждый раз; (в) оставить только список, но дополнить его недостающими окнами — тогда нужен их перечень от вас. Рекомендую (а): она уже работает, покрывает оба случая и ничего не отнимает. Пока не решено, расхождение с экранным эталоном записано временным. отправлен 2026-09-17 | Вариант (а): список остаётся, свои поля времени рядом. Решение D098 |
| Q020 | answered | T170 | Пиктограммы у полей панели будильника (D094): у ввода «Во сколько» стоят часы, у ввода «Зачем» — записка. Экранный эталон пиктограмм у вводов не несёт нигде — ни в одном модуле, проверено грепом. То есть у продукта есть видимый элемент, которого эталон не объясняет, и закрыть это можно только вашим взглядом: либо эталон дорисовывается под продукт, либо пиктограммы снимаются. Снимок панели приложен. Отправлено 2026-09-17 | Оставить как есть, к оформлению панели вернуться позже. Решение D099 — владелец ответил на панель будильника в целом, не дожидаясь этого вопроса |
| Q021 | answered | — | Накопилось пять служебных дел, и по правилу я их молча не беру — решай, что из этого стоит волны. (1) Эталон редактора отстал от продукта по четырём пунктам сразу: чип регулярности, три уровня важности вместо тумблера, блок «своё окно», мобильная раскладка; пока он отстаёт, на него опираются пять незакрытых сверок экранов (T197, T201, T205, T216, T218). (2) T225 — планка числа выполненных тестов: по новой парадигме это крайнее средство, а не норма, и я считаю её лишней. (3) T226 (сверить доступы у целей прогона) лежит в той же неслитой ветке feat/core-w4 и сама по себе полезна. (4) T228 — файлы блоков 3061 строка при потолке 120. (5) T229 — удалить тесты-налог. Плюс шестое, мелкое: сквозной смоук волны 5 не прошёл одним проходом — стык между «управляющий создал чек-лист» и «сотрудник его заполнил» держится фикстурой. Рекомендую: (1) взять отдельной волной, потому что он разблокирует пять сверок; (3) вытащить из ветки отдельным коммитом; (2) снять как задачу; (4) и (5) отложить до конца MVP. (спрошено 2026-09-17) | Делать по рекомендации целиком — ответ владельца дословно: «Сделай ка креоуиендуешь». Разложено в D104 |
| Q022 | auto | — | Три расхождения с эталоном на новых экранах отказа — сверка `norma` их нашла, блок не стал решать сам, и я тоже не стал: эталон на этот случай молчит. (1) На экране отказа кабинета шапка говорит «Кабинет», а упавший раздел в меню не подсвечен — общая граница ошибки не знает, какой раздел упал; чинится либо своей границей на каждый раздел, либо тем, что так и остаётся. (2) Карточка отказа в кабинете смешивает два вида из эталона: заголовок сверху и одна кнопка — это вид «пусто», а плашка без заголовка и ряд из двух кнопок — вид «ошибка». (3) Прямого образца «база недоступна» для сотрудника в `states.html` нет вовсе — ближайший блок «Нет сети при отправке» рисуется без цветной плашки, а мы плашку поставили. Рекомендую: (1) оставить как есть до жалобы — подсветка требует границы на раздел, это дороже пользы; (2) и (3) считать пробелом эталона, а не продукта, и закрыть их той же волной, которой будешь править эталон редактора (см. Q021). (спрошено 2026-09-17, отправлен 2026-09-18) | → D107 |
| Q023 | answered | T216 | Тёмной темы в продукте нет вовсе, а задача T216 требует её сверить — сверять нечего. Факты: эталон `docs/furca/design/reference/tokens.css` описывает тёмный набор токенов и включает его атрибутом `[data-theme="dark"]`, то есть переключателем, а не системной настройкой; решение D044 отдельно подбирало значение `--ink-3` «в тёмной» ради доступности. В продукте (`src/app/globals.css` и весь `src/`) нет ни `prefers-color-scheme`, ни `data-theme`: снимки светлой и тёмной версии одного экрана совпали побайтово. Развилка настоящая, дефолтом не закрывается: смена на кухне идёт 17:00–05:00, то есть продуктом пользуются ночью, но тёмная тема — это работа по всем экранам сразу плюс переключатель, которого в интерфейсе тоже нет. Варианты: (а) тёмная тема за горизонтом MVP — тогда пункт про неё из T216 снимается, а тёмные токены остаются заделом; (б) берём в MVP отдельной волной после сверок; (в) делаем автоматически по системной настройке, без переключателя — дешевле, но расходится с эталоном, который включает её атрибутом. Рекомендую (а). (спрошено 2026-09-17, отправлен 2026-09-18) | → D106 |
| Q024 | answered | T239 | Единицы измерения у числового пункта нет в данных вовсе, а экранный эталон рисует её рядом с полем («°C · within range»). Волна 7 показала вместо неё границы и вердикт («-22…-10», после ввода «-22…-10 · вне диапазона») — задача это прямо разрешала, и сотруднику стало понятно, чего от него ждут. Но расхождение с эталоном осталось, и проверяющий назвал его заметным: в эталоне ожидаемые значения стоят в подсказке под названием, а у поля — единица и вердикт; у нас наоборот, и слот подсказки занят текстом про важность пункта. Развилка настоящая, дефолтом не закрывается, потому что цена разная. Варианты: (а) завести единицу измерения полем в данных — это миграция и правка редактора, зато экран сойдётся с эталоном и «-22…-10 °C» читается однозначно; (б) признать нынешний вид (границы и вердикт у поля, без единицы) и поправить эталон под него — дешевле, но единицу сотрудник не увидит никогда, а методист не сможет её задать; (в) оставить как есть и вернуться после MVP. Рекомендую (б): границы у поля решают ту задачу, ради которой пункт и заводился, а единица без поля в данных всё равно неоткуда взяться. (спрошено 2026-09-18) | Вариант (а) с расширением: единица заводится полем в данных И должна настраиваться методистом. Дословно: «Значит давай добавим единицы тоже, но надо уметь их настраивать, чтобы поле было гибким» (2026-09-18) → D110; заведены T240, T241, T242 |
| Q025 | answered | T220 | Поштучные отметки времени по пунктам остаются клиентскими: `clampAnswerTimes` зажимает каждую независимо в отрезок смены и не проверяет ни монотонность, ни порядок пунктов. Карточка заполнения при этом показывает их управляющему как время отметки пункта — то есть как значащие. Общая длительность достоверна, оба её конца серверные. Варианты: (а) убрать поштучные отметки с карточки, оставив общую длительность — дешевле всего и ничего не врёт; (б) показывать их с явной оговоркой о природе (время устройства сотрудника); (в) сделать их серверными — дороже всего, нужен обмен на каждую отметку. Рекомендую (а): управляющий смотрит карточку, чтобы понять, обошёл ли сотрудник станцию, а не чтобы восстановить поминутный ход. (спрошено в терминале 2026-09-18) | Владелец на вариантах не остановился и поднял встречный вопрос, который меняет развилку, дословно: «ну погоди, а как тогда обходы сверять? как понять что пункты вообще вовремя отмечались?». То есть поштучное время нужно продукту не как украшение карточки, а как доказательство обхода — и тогда вариант (а) отпадает, а (б) закрепляет недоказательство. Ответ диспетчера: сейчас продукт не проверяет своевременность отметок НИКАК, клиентское время даёт только видимость (сотрудник может проставить всё разом — времена будут правдоподобными); доказательно сегодня лишь серверное начало и отправка, по ним видно окно обхода и его длительность. Поштучная доказательность = вариант (в) плюс оффлайн-буфер с досылкой, это отдельная задача со своей спекой, а не правка карточки. закрыт моделью работы → D112 | Поштучное время не показывает момент выполнения пункта: сотрудник подходит раз в час-два и отмечает несколько сразу. Колонка времени у пунктов с карточки убирается (T220), остаётся общая длительность на серверных концах. Доказательство своевременности нужно только циклическим чек-листам — уборкам и обходам — и даётся попаданием проверки в окно расписания, то есть механизмом периодических проверок (T230), а не отметками. (2026-09-18) |
| Q026 | answered | — | Порог покрытия после T220 даёт ложный красный, и сдвинуть базу мне не дали правами (действие расценено как обход CI-гейта — справедливо, это ослабление гейта). Арифметика: было 2106 покрытых ветвей из 2435, стало 2104 из 2433. Удалены ровно две ветви, обе покрытые — это удалённая колонка времени; новых непокрытых НЕ появилось, а набор проверок вырос (223 сквозных против 222, добавлен сторож с отрицательным прогоном). Доля просела арифметикой знаменателя: 86,48% → 86,47%. Варианты: (а) сдвинуть базу командой проекта `node scripts/coverage-gate.mjs --update` по отчёту приёмки — одна команда, нужен ваш «да»; (б) дописать тест на любую непокрытую ветвь ради процента — ровно тот антипаттерн, который запрещён принципами проекта; (в) сделать порог нечувствительным к удалению кода (сравнивать число непокрытых, а не долю) — правильнее всего, но это правка гейта отдельной задачей. Рекомендую (а) сейчас и (в) задачей. ОТВЕТ владельца 18.09: «да, ебаш все что надо» → D113, вариант (а) разрешён; (в) остаётся задачей T244. ВАЖНО: пока база не сдвинута, `main` не пушится — pre-push зовёт полный прогон и падает на этом пороге. (спрошено 2026-09-18) | — |
| Q027 | answered | — | Сотрудник ввёл замер, тот вышел за границы, продукт просит объяснение — и поле ввода с экрана УХОДИТ: остаются показание и строка комментария. Прочтение эталона осмысленное («замер сделан, объясняй его, а не переписывай»), но это поведение, которого спека не называет, и следствие у него простое: опечатку в показании исправить больше нельзя — только начать заполнение заново. Блок смягчил границей: поле уходит только там, где открыт блок комментария (важный и критичный пункт), у обычного остаётся. Варианты: (1) оставить как сделано — замер неприкосновенен, опечатка лечится перезаполнением; (2) оставить поле доступным для правки рядом с комментарием — тогда экран снова предлагает править показание вместо объяснения; (3) дать отдельное действие «ошибся, ввожу заново», чтобы исправление было осознанным. Рекомендую (1): на кухне ценность в том, что замер нельзя подогнать под норму задним числом, а опечатка — случай более редкий, чем соблазн. (спрошено 2026-09-18, отправлен 2026-09-18; 18.09 эталон `fill.html` приведён к НЫНЕШНЕМУ поведению — D117: замер стоит текстом на месте поля, границы и вердикт подписью рядом. Это не ответ на вопрос и вариантов не закрывает: выбор (2) или (3) означает перерисовать эталон снова) | D119 — владелец ответил «сделай как рекомендуешь», принят вариант (1) |
| Q028 | answered | — | В `~/Documents/workbench/worktrees/meridius-ds` (ветка `docs/design-system-pointer`) висит копия репозитория с одним неслитым коммитом владельца f95d906 от 18.09 01:02: 40 строк в README о том, что дизайн-система живёт в `GarroV/forma`, а здесь лежат только копии — `docs/furca/design/reference/dodo-ds.css` (ядро) и `domain.css` (типы чек-листов), плюс мост `@theme inline` в `src/app/globals.css`. Проверено: этих файлов нет ни в `main`, ни в одной ветке за всю историю репозитория, то есть README описывает состояние, до которого проект не доехал, и слитый как есть он станет неверным сразу. Варианты: (1) не вливать, копию не трогать — владелец довозит файлы и мост, README едет вместе с ними; (2) влить сейчас, переписав абзац на «переезд запланирован»; (3) снять копию и коммит, если работа передумана. Рекомендую (1): коммит владельца и похож на начало переезда, а не на отдельную правку доки. Задача T250 ждёт ответа, стройку не блокирует (спрошено 2026-09-18, отправлен 2026-09-18) | D120 — владелец ответил «сделай как рекомендуешь», принят вариант (1) |
| Q029 | open | T267 | Порядок кнопок в окнах опасных действий: эталон называет ОБА, и продукт до T267 жил сразу в двух. Факты. (1) Модуль «Опасное действие: перевыпуск кода» (`screens/states.html:202`) нарисован карточкой, и действия стоят в теле строкой `.inline`: сперва «Перевыпустить и открыть печать», потом «Отмена» — так же, как у всех карточек эталона (`catalog.html:91`, `states.html:168`). (2) Единственный настоящий диалог эталона — окно регулярности (`screens/editor.html:342`) — показывает подвал `.dialog__foot`: подсказка про Esc, распорка, «Отмена», затем действия, первичное последним. Комментарий самого эталона говорит, что правило `.dialog__foot` списано С ПОДТВЕРЖДАЮЩЕГО диалога, то есть подвал придуман ровно для таких окон. В продукте перевыпуск по решению T260 рисуется окном, а не карточкой, поэтому действовали оба прочтения сразу: окно редактора — «Отмена» первой, окно перевыпуска — первой «Перевыпустить». Для опасного действия это дороже простой несогласованности: привыкший к первому окну нажимает по памяти в то же место и попадает на перевыпуск. Выбран порядок подвала (`Отмена`, затем действие) — он старше, человек видит его чаще, и у опасного действия под нажатием по памяти оказывается отход. Расхождение с модулем `states.html` от этого стало осознанным: модуль показывает карточку, продукт рисует окно. Вопрос: подтвердить порядок «Отмена → действие» для всех окон и перерисовать модуль эталона под него — или вернуть порядок модуля и переставить кнопки в окне редактора. Стройку не блокирует: выбран порядок редактора, оба окна продукта уже приведены к нему. (спрошено 2026-09-20) | — |
| Q030 | open | T279 | Какой слой экранного эталона главнее, когда они расходятся в одном и том же селекторе. Факты. (1) `docs/furca/design/app.css` — свой слой MERIDIUS, по нему нарисованы страницы эталона (`screens/*.html` подключают `reference/tokens.css` + `app.css`). (2) `docs/furca/design/reference/{tokens,components}.css` — копия дизайн-системы линейки (в шапке так и написано: «Dodo P&L Service»), одна на три продукта, и именно `tokens.css` импортируется в продукт (`src/app/globals.css`). (3) Разошлись как минимум в двух местах: сдвиг кольца фокуса 2px против 1px и фон `.btn:hover` — `--surface-2` против `--surface-3`. Продукт сегодня следует дизайн-системе, потому что `app.css` в сборку не попадает. Варианты: (А) канон — дизайн-система, `app.css` правится под неё там, где разошёлся молча (продукт не трогаем, сверка экранов смотрит в дизайн-систему); (Б) канон — свой слой MERIDIUS, тогда продукт получает локальные переопределения на 2px и `--surface-2`, а расхождение с линейкой становится осознанным; (В) правка идёт в общую дизайн-систему в `GarroV/forma`, если 2px и `--surface-2` верны для всех трёх продуктов. РЕКОМЕНДАЦИЯ: (А) — `app.css` разошёлся без записанной причины, а линейка у трёх продуктов одна. Спрошен 2026-09-20 в терминале: канал уведомлений лежит, отправить было нечем | — |
| Q031 | open | T278 | Наведение на пункты бокового меню кабинета: две развилки, обе про состояния, которых эталон не называет. ФАКТ, С КОТОРОГО НАЧИНАЕТСЯ ВОПРОС: дефекта, ради которого заводилась T278, нет — обычные пункты меню (`nav-checklists`, `nav-library`, `nav-feed`, `nav-catalog`, `nav-qr`) под курсором дают ровно эталонные значения в обеих темах (72 замера вычисленных значений на живом экране, расхождений ноль); блок `editor` замерил одну бренд-ссылку и распространил вывод на соседей. Осталось две настоящие развилки. (1) БРЕНД-ССЫЛКА `nav-home`: эталон рисует бренд НЕ ссылкой (`app.css:32-37` `.nav__brand`, `components.css:78-80` `.appbar__brand`) и наведения ему не задаёт вовсе; продукт сделал бренд ссылкой (T124) и придумал ей своё поведение — `--ink` в покое, `--accent` под курсором. Это не расхождение с эталоном, а решение сверх него. Варианты: (А) оставить как есть и записать решением; (Б) дать бренду тот же фон, что соседям по меню; (В) вернуть бренду вид эталона — не ссылка. (2) АКТИВНЫЙ ПУНКТ ПОД КУРСОРОМ: по букве `app.css` наведение (`.nav__item:hover`, специфичность 0,2,0) перебивает активность (`.nav__item--active`, 0,1,0), то есть активный пункт под курсором должен откатываться к `--surface-3`/`--ink`; продукт сохраняет `--accent-soft`/`--accent`. Второй слой эталона этого случая не знает вовсе (там активный пункт — `span`, не ссылка). Варианты: (А) оставить как есть и записать решением (активность важнее наведения); (Б) следовать букве каскада эталона. РЕКОМЕНДАЦИЯ: по обеим — (А), оставить и записать решением: бренд-ссылка и подсветка активного раздела появились из потребностей продукта, эталон о них молчит, а не спорит. Спрошен 2026-09-20 в терминале: канал уведомлений лежит, отправить было нечем | — |
| Q032 | open | T282 | Три расхождения с эталоном, которые я не имею права закрыть сам: это развилки продукта, а не стиль. (1) ДУБЛИРОВАНИЕ ШАБЛОНА. Эталон (`design/screens/templates.html:42`) держит «Дублировать выбранный» в шапке списка и выбора строк не имеет вовсе; продукт сделал наоборот — «Дублировать» стоит в каждой строке. Чинится перестройкой экрана, решения в `decisions.md` нет. Мой совет: оставить продуктовый вариант и записать отступление — кнопка в строке короче на два действия и не требует понятия «выбранный», которого на экране больше нигде нет. (2) ПОДПИСЬ СТАНЦИИ. Эталон показывает короткое «Кухня», продукт — составное «Страна · Пиццерия · Станция», и оно обрезается многоточием. Формула нужна, чтобы различать одноимённые станции разных пиццерий (в сети «Кухня» есть в каждой), но в поле она не помещается. Мой совет: оставить формулу, но показывать её с конца — «Станция · Пиццерия», страна уходит в подсказку. (3) ФИЛЬТР «ВСЕ» У СТРАНЫ. В продукте есть, в эталоне нет (у пиццерии и станции есть в обоих), сценария «все страны сразу» эталон не знает нигде. Мой совет: оставить — методист сети работает больше чем с одной страной, а снять опцию дешевле, чем потом вернуть. Выделено из T282 волной 20 (спрошено 2026-09-20 в терминале: канал уведомлений лежит, отправить было нечем) | — |
