<!-- Описание блока. Контракт здесь — источник истины для блок-агента. -->

# Block: demo

## Назначение

Демонстрационный контур и запуск продукта на локальной площадке: чтобы показать коллегам, ничего не заводя руками.

## API-контракт

```
npm run seed:demo     # идемпотентный сид, повторный запуск не плодит дубликаты
```
Данные демо — английские (правило проекта), отдельная демонстрационная страна, не смешивается с рабочими данными.

## Зависимости

`catalog`, `editor`, `fill`, `feed`

## Definition of Done блока

1. Сид создаёт демонстрационную страну, две пиццерии, станции с кодами, три чек-листа с секциями и переиспользуемым блоком и десяток заполнений, включая одно с проваленным критичным пунктом.
   С 09.09.2026 контур показывает и режимы смены: у центральной пиццерии на сегодня стоит смена с ограничениями (2 из 4), одно вечернее заполнение сделано в критичную смену, а пункты разведены по трём уровням.
   Время заполнений задаётся местными сутками и местным временем пиццерии (`daysAgo` + `at`), а не сдвигом «столько часов назад»: заполнение обязано попадать ВНУТРЬ окна своего чек-листа при любом времени показа. Со сдвигом вечернее закрытие оказывалось «заполненным» в полдень, лента показывала его выполненным, а полоса тревог — пропущенным; оба утверждения были верны и вместе читались как дефект продукта ровно на показе. Инвариант держат проверки в `dataset.test.ts`.
   Вечернее закрытие за прошедшую ночь оставлено незаполненным НАМЕРЕННО: это вторая тревога, которую демо показывает.
   С 10.09.2026 контур показывает и третью — критичный пункт, оставленный БЕЗ ОТВЕТА: сегодняшнее утреннее заполнение полное, кроме замера температуры поставки. Это единственное неполное заполнение контура, и проверка покрытия делает исключение только ему. Тревога считается лишь по заполнениям за текущие местные сутки, поэтому `daysAgo` у него обязан оставаться нулём.
2. Повторный запуск сида возвращает контур в исходное состояние и не плодит дубликаты — проверено двойным прогоном.
3. Продукт поднимается на локальной площадке одной командой, адрес открывается в браузере.
4. Сквозной сценарий MVP проходит на поднятом продукте целиком: от создания чек-листа до появления заполнения в ленте.
5. Демонстрационные данные не попадают в рабочую страну.

## Устройство блока

| Файл | Что внутри |
|---|---|
| `src/blocks/demo/model.ts` | Форма контура: страна, пиццерии, станции, блоки библиотеки, чек-листы с версиями, заполнения. Времени в описании нет — только смещения назад от опорного момента |
| `src/blocks/demo/dataset.ts` | Сам контур: английские данные с постоянными опознавателями. Только данные, без запросов |
| `src/blocks/demo/answers.ts` | Ответы заполнения собираются из разметки версии, а не перечисляются рядом с ней |
| `src/blocks/demo/seed.ts` | `seedDemo()`: одной транзакцией снимает прошлый контур и заводит его заново |
| `src/blocks/demo/smoke.ts` | Уборка за сквозным смоуком и перепись контура: имена прогона с меткой, снятие своих строк в порядке связей, сверка базы с описанием контура |
| `src/blocks/demo/index.ts` | Публичный вход блока |
| `scripts/seed-demo.mjs` | `npm run seed:demo` — запуск сида с печатью ссылок станций |
| `scripts/src-resolve-hook.mjs` | Хук разрешения импортов: Node сам не знает ни псевдонима `@/`, ни импортов без расширения |
| `scripts/up` | `npm run up` — одна команда: база, миграции, контур, сборка, запуск |
| `scripts/mvp-smoke.mjs` | Сквозной смоук MVP в живом браузере со снимками; параметризован адресом |
| `e2e/a11y.spec.ts` | Проверка доступности axe внутри сценариев Playwright |

## Решения, принятые внутри блока

| Решение | Почему |
|---|---|
| Идемпотентность устроена как «снять своё и завести заново», а не «досоздать недостающее» | Досоздание требует сравнивать каждую строку с описанием и разбирать частичные расхождения — ровно тот код, который тихо расходится с данными. Опознаватели контура постоянны, поэтому снятие точечное и чужой строки не касается |
| Заполнения снимаются по СТАНЦИИ контура, а не только по своему опознавателю | На показе по демо-коду заполняют по-настоящему; такая запись держала бы версию внешним ключом, и второй прогон сида падал бы вместо обновления контура |
| Времени в описании контура нет — только смещения назад от опорного момента | Иначе демо через месяц показывает ленту месячной давности, а два прогона нельзя сравнить строка в строку |
| Два чек-листа одной станции закрывают сутки целиком | Демо обязано открываться по QR в любой час: чек-лист с утренним окном вечером не открылся бы, и продукт выглядел бы сломанным |
| Данные демо — только английские, ключ `en` единственный | Правило проекта (конституция, «Демонстрационный режим»). Русское устройство увидит английский текст: `pickText` откатывается к первому имеющемуся языку |
| Запуск сида идёт через хук разрешения импортов, а не отдельной копией запросов в `.mjs` | Node умеет исполнять TypeScript, но не знает псевдонима `@/` из tsconfig. Без хука сид пришлось бы писать мимо слоя доступа — второй набор запросов к базе, который разъедется со схемой |
| Смоук MVP — отдельный сценарий с параметром адреса, а не ещё один файл в `e2e/` | Он обязан гоняться по ПОДНЯТОМУ продукту, в том числе после раскатки на площадку (T079), а `e2e/` поднимает свой сервер и свою базу |

## Журнал работы

| Задача | Кем | Что сделано |
|---|---|---|
| T049 (сид) | сам | `model.ts`, `answers.ts`, `seed.ts` и `scripts/seed-demo.mjs`: описание контура, сборка ответов из разметки версии, снятие прошлого контура и вставка нового одной транзакцией. Логика идемпотентности и границы «своё/чужое» — не механическая работа, поэтому делал сам. **Отрицательный прогон:** снятие контура отключено (`removed = 0`) — упало 7 из 8 проверок сида, первой же `duplicate key value violates unique constraint "countries_pkey"` |
| T049 (данные) | исполнитель | Само содержимое контура (`dataset.ts`): страна `Demoland`, две пиццерии, пять станций с кодами, блок библиотеки `Food safety basics` в двух чек-листах, три чек-листа (у одного архивная версия и опубликованная), 26 пунктов, 12 заполнений. Отдано исполнителю по всем четырём условиям: контракт готов (типы `model.ts` + `dataset.test.ts` как приёмка), работа механическая, миграций и доступа не касается, объём не мелочь |
| T050 (одна команда) | сам | `npm run up` поднимал стенд, но работать на нём было нельзя: `next start` — это `NODE_ENV=production`, а продукт в этом режиме СОЗНАТЕЛЬНО отказывает во входе, когда `SESSION_SECRET` или `ADMIN_PASSWORD_HASH` взяты из `.env.example` (`src/blocks/auth/config.ts`). При этом сам `up` копировал `.env.example` и шёл дальше: страницы отдавали 200, а вход — пятисотку `SESSION_SECRET: значение взято из .env.example`. То есть «одной командой» поднимался стенд, на который нельзя войти, и первым это ловил не человек, а сквозной смоук. Починка в `scripts/up`: опубликованные значения заменяются своими (секрет — `openssl rand`, пароль — генерируется и хэшируется уже имеющимся `scripts/hash-admin-password.mjs`), пароль печатается один раз рядом с адресом. Опознаются они сравнением с самим `.env.example`, а не копией константы: две копии разъезжаются. **Проверка:** `npm run up` с нуля → база, миграции, контур (14 заполнений), сборка, сервер на 3210, `/admin/login` 200, вход проходит, сквозной смоук MVP целиком зелёный |
| T089 (смоук убирает за собой) | сам | Уборка средствами продукта невозможна и это не недоделка: заполнения не удаляются вовсе (`on delete restrict`), чек-лист с заполнениями продукт снимает с работы, а не стирает. Поэтому уборка — через слой доступа (`src/blocks/demo/smoke.ts`), а `DATABASE_URL` стал обязательным: без него смоук отказывается СТАРТОВАТЬ, а не заводит данные, которые потом некому убрать. Метку `Smokeland` теперь несут все имена прогона, включая название чек-листа, и чек-листы снимаются раньше станций — тогда отвязанных (`on delete set null`, тот самый невидимый остаток) не появляется вовсе. Уборка зовётся на входе, в `finally` и по сигналу; до и после прогона перепись базы сверяется с описанием контура, а ожидаемые числа считаются из самого описания, а не записаны. **Проверка на живом стенде:** прогон целиком (снято 7 строк, «после прогона в базе ровно демо-контур»), прогон, упавший в редакторе (снято 5), прогон, оборванный SIGINT (код 130, снято 6). После пяти прогонов в базе `0` строк с меткой, `0` отвязанных чек-листов, расхождений с контуром нет |
| T089 (попутно) | сам | Сквозной сценарий не проходил с 07.09.2026, и никто этого не видел: режимы смены заменили флажок «критичный» на переключатель уровней, `item-critical` из разметки исчез, а смоук продолжал жать на него. Правка в `scripts/mvp-smoke.mjs` — на `item-severity-critical` и `data-severity`. Разошлись чужой блок и мой файл, поэтому чинил у себя |
| T099 (третий вид тревоги) | исполнитель + сам | Исполнителю — настройка `unanswered` у `answersFor` и 14-е показательное заполнение по готовому контракту (работа механическая, миграций и политики не касается). **Приёмка своими руками:** исполнитель честно доложил, что ему пришлось сделать исключение в проверке «ответы покрывают ровно те пункты, которых ждал режим смены» — иначе новое заполнение ломало её регрессией. Исключение проверил: оно не ослабляет проверку до пропуска, а сравнивает с тем же списком минус один пункт. Дописал связку между двумя требованиями — что тревогу поднимает ИМЕННО то заполнение, которому сделано исключение, и что без ответа остался именно тот пункт: иначе они могли разъехаться молча. **Проверка в продукте, а не в счётной функции:** `listAlarms` на живой базе стенда даёт `criticalFailed 1, criticalUnanswered 1`, а после закрытия вечернего окна — все три вида: `criticalFailed 1, criticalUnanswered 1, missed 1` |
| T086 (мигающая проверка) | сам | Причина оказалась НЕ в данных, вопреки записи в задаче: главная админки (`src/app/admin/page.tsx`) вообще ничего не читает из базы — это статичный список разделов, и параллельные сценарии ей помешать не могли. Мешала неоконченная навигация. Вход — серверное действие с `redirect`, то есть на главную браузер уходит клиентским переходом; React показывает разметку новой страницы КАДРОМ РАНЬШЕ, чем переставляет метаданные документа. Замер наблюдателем внутри страницы: `609ms title=0 home=1`, `622ms title=1 home=1` — окно, в котором главная в DOM уже есть, а `<title>` у документа нет вовсе. `getByTestId("admin-home").waitFor()` заканчивается на первом из этих кадров, и axe успевал проверить наполовину переключённый документ: `document-title: Documents must have <title> element`. В общем прогоне один сервер отвечает сотне сценариев из нескольких воркеров, метаданные приезжают отдельным куском потока сильно позже разметки, окно растягивается — отсюда «через раз»; отдельно всегда проходило, потому что без нагрузки окно закрывается за 13 мс. Починка — постусловие `signIn` в `e2e/a11y.spec.ts`: возвращается, только когда переход доехал. Нарушение не спрятано: axe остался без `disableRules` и `exclude`, а страница без заголовка уронит шаг по таймауту с текстом. **Проверка:** до починки полный прогон упал на первом же заходе, после — 6 полных прогонов подряд по 102 сценария зелёные |
| T089 (красная проверка после подхвата) | сам | Проверка «забирает и чек-лист, отвязанный от станции» падала `expected 60 to be 59`. **Разбор до правки:** снятие работает — `swept.checklists` равен 1, и строки в базе после уборки нет; неверна была сама проверка. Последней строкой она сравнивала ОБЩЕЕ число отвязанных чек-листов «до» и «после», а тестовая база одна на прогон, файлы идут параллельно, и соседи заводят чек-листы без станции десятками (`Checklist <id>` и `… (copy)` у редактора и библиотеки, `detachChecklist` у справочника): в базе их лежало 61. Уборка снимала свой, сосед в то же мгновение заводил свой — общее число не двигалось. **Воспроизведено принудительно, а не оставлено догадкой:** вставка отвязанного чек-листа раз в 5 мс параллельным подключением дала 3 падения из 3 с тем же `expected N to be N−1` на строке 251. **Правка:** сравнивается число отвязанных чек-листов С МЕТКОЙ прогона — ровно 1 до уборки, ровно 0 после; общий счётчик остался проверкой «не меньше одного», он живёт в смоуке на стенде, где в базе только контур. Под той же нагрузкой 4 прогона из 4 зелёные. **Отрицательный прогон:** из уборки убрано условие поиска по метке (файл снят копией и восстановлен из неё, не через git) — проверка краснеет, и новая строка ловит поломку сама по себе: `expected 1 to be +0` |
| T050 (приёмка своими руками) | сам | Работа сделана прежним агентом, но проверена заново: после слияния с main в копию приехали 27 коммитов, и «было зелено вчера» про сегодня ничего не говорит. Прогон `npm run up` с нуля на своём стенде: база `meridius-demo` на порту 5444, миграции, контур (1 страна, 2 пиццерии, 5 станций, 1 блок библиотеки, 3 чек-листа, 7 версий, 14 заполнений), сборка, сервер на 3210. Ответы: `/` — 200, `/admin/login` — 200, `/admin` — 307 на вход. Вход в кабинет проходит: сквозной смоук MVP входит настоящим паролем и доходит до ленты. Адрес открыт в живом браузере, снимки — `reports/demo-shots/demo-01-admin-home.png` и `demo-02-feed.png`, сверку с эталоном вёл `norma`. Пароль кабинета заведён своим (не из `.env.example`) и в файлы репозитория не попал |
| T099 (приёмка своими руками) | сам | Данные третьей тревоги были заведены прежним агентом; проверял не по коду, а в поднятом продукте. `listAlarms` на живом стенде: `criticalFailed 1, criticalUnanswered 1, missed 1` — три вида из трёх. На экране ленты полоса читается так же: «a critical item was not done», «a critical item was left unanswered», «checklist was not filled, the window closed at 05:00»; в самой ленте заполнение с непрожатым пунктом помечено `1 ITEM UNANSWERED`. То есть тревога видна и счётной функции, и человеку на показе |
| T089 (проверка на живом стенде после слияния) | сам | Смоук прогнан заново на поднятом стенде: полный прогон зелёный, уборка сняла 7 строк, «после прогона в базе ровно демо-контур». Прогон, оборванный SIGINT на шаге печати QR, убрал за собой 6 строк. После обоих прогонов в рабочей базе ровно контур: 1/2/5/3/7/14/1/1 по таблицам, строк с меткой `Smokeland` — 0, отвязанных чек-листов — 0 |
| Исполнители (`optio`) | — | В этой волне не поднимал ни одного и объясняю почему: оставшаяся работа — разбор мигающей проверки (решение по ходу, а не готовый контракт), правка на девять строк и прогоны-приёмки. Ни одно из четырёх условий обязательной передачи не выполнено, а запуск исполнителя и чтение его отчёта стоили бы дороже самой правки |
| Проверка в браузере | `norma` | Снимки поднятого стенда (`reports/demo-shots/`) сверял проверяющий, не я: главная кабинета и лента с полосой тревог. Про предмет T099 ответ такой: все три вида тревог на экране ЕСТЬ текстом («not done», «left unanswered», «not filled, the window closed»), но между собой они не различаются ничем, кроме фразы, — один красный стиль на три смысла, а в самой дизайн-системе (`docs/furca/design/app.css`) на теги заведено всего два «плохих» цвета. Это вопрос экранного языка ленты и эталона, а не демонстрационных данных: контур своё показывает. Вынесено в отчёт диспетчеру, сам не решал и чужой блок не правил. Там же — находки не по моему блоку: в эталоне ленты (`docs/furca/design/screens/feed.html`) полосы тревог нет вовсе, главная кабинета (`src/app/admin/page.tsx`) идёт без каркаса `AdminShell`, и рядом с общим каркасом живут ещё две копии — `catalog/ui/AdminShell.tsx` и `editor/ui/AdminShell.tsx` |
| Приёмка работы исполнителя | сам | Проверял своими руками, а не по отчёту: перечитал файл целиком и прогнал **свой** разбор контура отдельным сценарием. Тесты исполнителя были зелёные, но нашёлся настоящий дефект: **пять заполнений из двенадцати отвечали на пункт свободного текста пустой строкой**. Продукт таких ответов не создаёт вовсе — `isAnswered` в блоке fill считает текст из одних пробелов НЕотвеченным пунктом, — то есть на карточке заполнения строка «Notes for the day shift» показывалась бы пустой и читалась как «поле не сохранилось». Дыра была в моём собственном контракте: `answersFor` молча подставлял `""`. Закрыто с двух сторон: `answersFor` теперь падает, если у версии есть пункт свободного текста, а заметка не задана (тест на это написан до правки и падал), и всем пяти заполнениям дописаны английские заметки |
| T173 (контур переживает работу внутри себя) | сам | **Воспроизведено до правки, а не выведено из кода.** На своём стенде (`meridius-demo`, порты 3380/3382) заведена станция `Manager desk` в демо-пиццерии — тем же вызовом `createStation`, каким её заводит админка. `npm run seed:demo` упал кодом 1 и голой трассой: `DrizzleQueryError: Failed query: delete from "stores" … violates foreign key constraint "stations_store_id_stores_id_fk"`. Уровень страны воспроизведён отдельно и на СТАРОМ коде (файлы временно подменены копией из `git show HEAD:`, обратно — из снятой заранее копии, не через `git checkout`): заведена пиццерия `Demoland, Airport`, падение то же, но на `delete from "countries" … is still referenced from table "stores"`. **Причина.** `removeContour` снимал контур ТОЛЬКО по списку опознавателей из `dataset.ts`. Список описывает, что в контуре обязано быть, и ничего не знает о том, что в нём завели руками, — а завести станцию, пиццерию или чек-лист внутри демо это ровно то, ради чего демо и показывают. Отсюда «демо-контур живёт до первого действия внутри него». **Правка.** Границей контура стала СТРАНА: снимается всё, что висит под демонстрационной страной (`contourOf` в `seed.ts` — страна → пиццерии → станции → чек-листы → версии), а опознаватели из описания добавляются к найденному, а не заменяют его. Чек-лист считается своим и по станции, замороженной в любой его версии: без этого чек-лист, отвязанный методистом, выпадал бы из контура насовсем — та же щель, что закрывали в уборке смоука 07.09. Снимаются заодно `checks` и `alarms`, которых в прежнем списке не было вовсе. Блоки библиотеки — по-прежнему только по опознавателям, и это не недосмотр: блок не привязан ни к стране, ни к станции, поэтому заведённый на показе от рабочего неотличим, и трогать его сид права не имеет. **Отказ вместо трассы.** Новый `src/blocks/demo/failure.ts`: свой отказ (`DemoSeedError`) печатается как есть, чужой — рамкой с `detail` и `constraint` базы. Единственный случай, который сид чинить НЕ имеет права, назван поимённо: заполнение или отметка обхода, сделанные на станции ВНЕ контура по версии демонстрационного чек-листа (так бывает, когда демо-чек-лист назначили на рабочую станцию). Стереть их — уничтожить свидетельство о настоящей смене, оставить — не снять контур; поэтому сид отказывается, называет строку и станцию и говорит, что делать. Первая формулировка отказа была НЕВЕРНА («отвяжите чек-лист и запустите ещё раз») — отвязка такую строку не убирает, и совет отправлял бы человека по кругу; переписано на честное «снимите назначение, чтобы не повторилось, а с накопленными строками решите отдельно». **Проверка прогоном.** После правки: станция — `снято строк прошлого контура: 35`, код 0, в базе ровно 1/2/5/3/7/14; пиццерия — `снято 35`, код 0, `Demoland, Airport` исчезла; чистый повторный прогон — `снято 34` (это и есть размер эталонного контура вместе с режимом смены, которого старый счётчик не считал). Отказ проверен вживую: заполнение рабочей станции по демо-версии → код 1, текст вместо трассы, а контур после отказа цел (2/5/3/7/14) — транзакция откатилась. Тесты писались до правки и падали: 6 из 6 новых красные на `stations_store_id_stores_id_fk`. Делал сам: задача исследовательская — разбор графа внешних ключей и решение о границе контура, готового контракта тут нет, ни одно из четырёх условий обязательной передачи исполнителю не выполнено |
| T173 (проверка в браузере) | сам | Правка видна на экране, поэтому проверена живым браузером, а не по коду: свой headless Chrome по протоколу отладки (порт 3385, профиль в рабочей папке сессии), вход в кабинет своим паролем, экран «Справочник» пиццерии `Demoland, Central Square`. ДО прогона на экране видна поломка: стран 1, пиццерий 3 (`Demoland, Airport`), станций у Central Square 4 (`Manager desk` с кодом `6f8zk4uyx4`). ПОСЛЕ прогона — эталон: пиццерий 2, станций 3, лишних строк нет, пустых и битых строк таблицы не появилось. Снимки: `reports/demo-shots/demo-t173-01-do-sida.png` и `demo-t173-02-posle-sida.png`, рядом текст страницы в `.txt`. **Сверку с экранным эталоном провести НЕ удалось, и это не пропущенный шаг, а несделанный:** запуск `norma` отклонён сторожем расхода («волна — не больше одного блока»), а снимки я читать не имею права. Состав экрана проверен текстом страницы, вёрстка против эталона — нет. Диспетчеру: сверку надо прогнать отдельно |
| T173 (покрытие) | сам | Числа приёмки достигнуты ОТНОСИТЕЛЬНО, и это надо прочитать целиком. С моей правкой: операторы 87,77%, ветви 85,46%, функции 90,82%, строки 89,05% — ниже записанных в задании (87,81 / 85,52 / 91,11 / 89,07). Померил базу своей ветки тем же прогоном (файлы блока временно заменены версиями из `git show HEAD:`, `failure.ts` и `failure.test.ts` отложены в сторону, потом всё возвращено из снятых копий): **87,65 / 85,40 / 90,74 / 88,91 — база уже ниже прошлой приёмки по всем четырём числам**, то есть просадка не моя, она приехала в `main` раньше. Моя правка каждое число ПОДНИМАЕТ: операторы +0,12, ветви +0,06, функции +0,08, строки +0,14. Диспетчеру: числа из задания на сегодняшнем `main` недостижимы, их надо либо пересчитать по факту, либо искать, чей блок их уронил |

## Как продукт живёт на площадке MUSPELHEIM

> Заведено 07.09.2026 (D038, D043, D046). Раскатка выполняется только по явному «да» владельца.

Папка на площадке — `C:\projects\meridius`, клон этого репозитория. Своё имя стенда
в Docker (`COMPOSE_PROJECT_NAME=meridius`), приложение на порту 3100, база на `127.0.0.1:5455`
и наружу НЕ публикуется: порт 5433 там занят чужим проектом, а публичной точке записи внешняя
база не нужна. На контейнере базы стоит метка `backup.pgdump=true` — по ней её забирает ночной
бэкап площадки (03:30, ретеншн 14 дней).

Секреты площадки свои, не демонстрационные из `.env.example`: пароль кабинета, секрет подписи
сессии и пароль базы. Файл `.env` лежит только на площадке и в git не попадает.

На время показа в кабинете стоит короткая единая учётка по требованию владельца (D047): порог
длины у `scripts/hash-admin-password.mjs` не снят, а пройден явным флагом `--allow-short`.
Значение пароля не пишется ни в один файл репозитория — он публичный; пароль знают владелец и
`.env` площадки. После показа его меняют на длинный.

Наружу продукт отдаёт отдельный туннель Cloudflare (`C:\bootstrap\cloudflared.exe`), продукт
живёт на корне своего адреса. Публиковать его на пути порта Tailscale нельзя, пока не закрыта
T088: обычные ссылки между экранами базового пути не знают.

Два процесса держатся задачами планировщика, а не SSH-сессией (иначе `sshd` убивает дерево
процессов при выходе):

| Задача | Что делает |
|---|---|
| `meridius-start` | `npm run start` — продукт на порту 3100, журнал в `run.log` |
| `meridius-tunnel` | `cloudflared tunnel --url http://localhost:3100`, журнал в `tunnel.log` |

Порядок обновления продукта на площадке: `git pull` → `npm ci` (если менялись зависимости) →
`npm run build` → остановить процесс продукта → `schtasks /Run /TN meridius-start`. Пересборка
обязательна при смене любого значения окружения: значения запекаются в сборку (D033).

Проверка после обновления — одной командой с машины разработчика:

```bash
node scripts/mvp-smoke.mjs --url <публичный адрес> --password <пароль кабинета> --timeout 90000
```

Шаг ожидания задаётся флагом `--timeout`: через туннель ответы идут медленнее, и значения по
умолчанию не хватает.

**Смоуку нужна база, и без неё он не стартует** (T089). Он проходит продукт настоящими
действиями, значит оставляет настоящие строки, а убрать их средствами самого продукта нельзя,
и это не недоделка: заполнения не удаляются вовсе (`submissions` — `ON DELETE RESTRICT`,
история неприкосновенна), а чек-лист с заполнениями продукт снимает с работы, а не стирает
(`removeChecklist` блока editor). Поэтому уборка идёт через слой доступа, и `DATABASE_URL`
обязателен: без него смоук отказывается начинать, а не заводит данные, которые потом некому
убрать. С машины разработчика к закрытой базе площадки — через проброс `ssh -L`.

Уборка вызывается трижды: на входе (снять брошенное прошлыми прогонами — прогон, убитый
Ctrl-C, до `finally` не доходит), в `finally` (снять своё, в том числе после падения
посередине) и по сигналу обрыва. До и после прогона смоук сверяет перепись базы с описанием
демо-контура и печатает расхождение по таблицам; расхождение до прогона — отказ стартовать
(на неизвестном стенде проверка «после прогона ровно демо-контур» ничего не значила бы).

**Почему уборки по имени страны не хватало.** `checklists.station_id` объявлен
`ON DELETE SET NULL` осознанно — удаление станции отвязывает чек-лист, но не уничтожает его
историю (см. `src/blocks/data/schema.ts`). Уборка по стране сносила станцию, а чек-лист
оставляла — уже без станции, то есть не находимым ни по стране, ни в справочнике: 07.09.2026
таких набралось восемь при трёх настоящих. Отсюда два правила в `src/blocks/demo/smoke.ts`:
метку `Smokeland` несут ВСЕ имена прогона, включая название чек-листа (по ней отвязанный
чек-лист и находится, и его нельзя спутать с тем, который методист отвязал сам через
`detachChecklist`), а чек-листы снимаются РАНЬШЕ станций — тогда отвязанных не появляется
вовсе. Проверка `забирает и чек-лист, отвязанный от станции` в `smoke.test.ts` написана
ровно на этот случай и падает, если уборка снова возьмёт только один слой.

Ожидаемое содержимое демо-контура смоук считает из самого описания (`contourCensus`), а не
из записанного числа: иначе первое же добавленное показательное заполнение сделало бы
проверку ложной. Остатки прогонов, названные до появления метки (`Kitchen opening …`), эта
уборка не заберёт — их не отличить от настоящих; сверка переписи их покажет, убирать руками.

## Статус

done
