<!-- Описание блока. Контракт здесь — источник истины для блок-агента. -->

# Block: fill

## Назначение

Публичный экран заполнения по QR — единственная часть продукта, доступная без входа. Здесь считается каждое касание.

## API-контракт

```
GET  /s/<код станции>     → опубликованная версия по станции и окну времени
POST /s/<код станции>     → заполнение { answers, startedAt }
```
Ответ не содержит ничего, кроме самого чек-листа: ни истории станции, ни списка чек-листов, ни сведений о пиццерии сверх названия (D021). Частота отправок с одного кода ограничена.

## Зависимости

`data`, `editor`

## Definition of Done блока

1. Сканирование QR открывает нужный чек-лист без единого касания сверх сканирования.
2. Язык интерфейса и текстов пунктов берётся из языка устройства, с откатом на язык страны и далее на русский.
3. Провал критичного И важного пункта требует комментарий и не даёт отправить без него; комментарий появляется сразу под пунктом, не отдельным экраном (D018, D056). Обычный пункт можно провалить молча: принуждение к объяснению про непротёртый стол учит писать «ок» в каждое поле.
4. Числовой пункт принимает значение и показывает, попало ли оно в диапазон, но не блокирует отправку.
5. Кнопка отправки неактивна, пока есть незакрытые пункты, и говорит, сколько их осталось.
6. Неизвестный или перевыпущенный код даёт понятный отказ, а не пустой экран и не чужой чек-лист.
7. Обрыв связи при отправке не теряет введённое: ответы остаются на экране, отправка повторяется.
8. Перебор кодов не отдаёт ничего, кроме отказа — отдельный тест.
9. Заполнение пишется на ту версию, что была отдана клиенту, даже если за это время опубликовали новую.
10. Экран работает на ширине 375 px, зоны нажатия не меньше 44 px, горизонтальной прокрутки нет.
11. Экран совпадает с эталоном `docs/furca/design/screens/fill.html`.
12. Режим смены виден в шапке и меняется одним касанием, не удлиняя путь сотрудника: чек-лист открывается сразу, полной сменой по умолчанию (D052, D055).
13. Будильник заводится вручную — время и подпись, — звонит в назначенное время и переживает перезагрузку планшета: он лежит строкой в базе, а не в памяти вкладки. Живёт до конца окна работы чек-листа — включая окно, переходящее через полночь, — регулярности у него нет (D070, D090). Ограничение «звук работает, пока экран открыт» названо на самом экране.

## Устройство блока

| Файл | Что внутри |
|---|---|
| `src/app/s/[code]/page.tsx` | Единственный маршрут, открытый интернету. Ничего не собирает — экран ходит за данными сам |
| `src/blocks/fill/station.ts` | Что отдаётся по коду: версия, название пиццерии и станции, язык страны. Три исхода. Плюс `findStationVersion` — версия принимается, только если она заморожена на станции этого кода |
| `src/blocks/fill/locale.ts` | Цепочка языков: устройство → страна → русский; выбор текста пункта по цепочке |
| `src/blocks/fill/answers.ts` | Правила экрана: что отвечено, сколько осталось, когда открыта кнопка, что уходит на сервер |
| `src/blocks/fill/validation.ts` | Схема тела отправки на границе и сверка ответов со снимком версии |
| `src/blocks/fill/rate-limit.ts` | Ограничение частоты: отправки по коду и по сети, открытия экрана по адресу клиента |
| `src/blocks/fill/submit.ts` | Приём заполнения: порядок проверок и запись на версию, отданную клиенту |
| `src/blocks/fill/view.ts`, `model.ts` | Модель экрана: тексты уже выбраны по языку и склеены |
| `src/blocks/fill/ui/FillScreen.tsx` | Серверная часть: данные, язык, словарь клиентской части |
| `src/blocks/fill/ui/FillForm.tsx` | Экран заполнения: секции, прогресс, кнопка, комментарий, отправка, обрыв связи |
| `src/blocks/fill/ui/StateScreen.tsx` | Состояния без заполнения: отправлено, код не действует, заполнять нечего |
| `src/blocks/fill/ui/submit-action.ts` | Серверное действие: POST уходит на тот же `/s/<код>` |
| `src/blocks/fill/shift-mode.ts` | Границы выбора режима смены: разбор ввода, ограничение частоты, запись режима пиццерии |
| `src/blocks/fill/ui/ShiftModeBar.tsx`, `shift-mode-action.ts` | Строка «Смена: … · сменить» в шапке и панель выбора режима |
| `src/blocks/fill/alarms.ts` | Будильники станции: форма тела, потолок на проход окна, заведение, список на текущий проход окна и снятие. Границы прохода окна и миг звонка считает база из часового пояса пиццерии |
| `src/blocks/fill/ui/AlarmsPanel.tsx`, `alarm-action.ts` | Панель будильников: строка «время · подпись · убрать», поле заведения, плашка звонящего будильника с кнопкой «Понятно» |
| `src/security-headers.ts` | Заголовки безопасности в одном месте: их ставят `next.config.ts` и `src/proxy.ts` |
| `e2e/fill.spec.ts`, `e2e/fill-fixtures.ts` | Сквозной путь в браузере на 375 px настоящими касаниями |

## Решения, принятые внутри блока

| Решение | Почему |
|---|---|
| Отправка — серверное действие, а не обработчик `route.ts` | Контракт требует `POST /s/<код>`, а `page.tsx` и `route.ts` в одном сегменте Next не уживаются. Серверное действие уходит POST-запросом ровно на этот адрес |
| Версия принимается, только если она заморожена на станции отсканированного кода | Идентификатор версии приходит из браузера. Без этой проверки один живой код с наклейки позволял бы писать заполнения в историю любой станции сети: `saveSubmission` выводит станцию из версии и записал бы их туда, где никто не заполнял. Архивная версия проходит намеренно — это и есть T041 |
| Неизвестный код, перевыпущенный код и версия чужой станции дают один и тот же отказ, и с тем же кодом ответа HTTP | Различать их значило бы отвечать перебору по-разному. Перевыпуск переписывает код станции, прежней строки не остаётся — так что «отозван» и «не существовал» неотличимы и в данных |
| «Заполнять нечего» — отдельное состояние от «код не действует» | Единственное, что этот ответ сообщает, — что код живой; данных в нём нет никаких. Зато сотруднику с настоящей наклейкой говорится правда, и он не идёт к управляющему за новой (принцип 2) |
| Логический пункт переключается по кругу: не отвечено → выполнено → не выполнено | В эталоне у пункта одна зона нажатия, а «нет» — такой же нужный ответ, как «да». Обычный случай (всё выполнено) стоит одного касания на пункт, провал — двух (принцип 2) |
| Правило провала не своё, а `isFailed` блока `data` | Иначе экран сотрудника и лента управляющего разошлись бы в том, что считать невыполненным. Ради этого в блоке есть `gradingSections`: модель экрана переводится обратно в пункты, понятные общему счёту |
| Правило «критичный провален → нужен комментарий» держится и сервером | Тело запроса отправляет кто угодно, а обещание «комментарий будет» дано управляющему. Кнопка в браузере — удобство, а не защита |
| Неполный чек-лист сервер принимает, пустой — нет | Отвергнуть почти готовое заполнение значит потерять работу сотрудника, а неполнота и так видна в ленте (принцип 2). Пустой список отвергается: сохранять нечего |
| Свой счётчик частоты, не общий с блоком `auth` | Границы модулей запрещают `fill` зависеть от `auth`. Считается и другое: у входа — неудачи, здесь — все обращения подряд, потому что заполнение всегда «удачно» |
| Предел на открытие экрана применяется, только когда клиента есть чем различить | Публичная площадка — туннель, который может не передавать адрес клиента (D034). Один общий ключ превратил бы предел «на клиента» в рубильник, гасящий всю сеть на пересменке. Точку записи при этом прикрывают пределы отправок: они считаются по коду и по сети и от адреса не зависят |
| Язык экрана считается здесь, а не общим `src/i18n/request.ts` | Общий отвечает одинаково на «телефон просит английский» и «телефон просит язык, которого у нас нет». Экрану заполнения во втором случае нужен язык страны пиццерии. Язык проставляется на самом экране (`lang`), а не на `<html>`: корневая разметка общая с админкой |
| Модуль правил заполнения ввозит `grading`/`types` блока `data` напрямую, а не через его вход | Он работает в браузере, а вход блока `data` тянет за собой драйвер `pg`, и клиентская сборка падает. Найдено настоящей сборкой при зелёных тестах. Тот же приём и по той же причине уже применён к миграциям — см. хвост `src/blocks/data/index.ts` |
| Будильник принадлежит СТАНЦИИ, а не чек-листу и не версии | На станции может быть открыто несколько чек-листов (экран выбора), а планшет у неё один. Будильник, привязанный к версии, исчезал бы при публикации следующей посреди смены — ровно там, где он и нужен |
| Отсчёт до звонка уходит в браузер секундами от серверного «сейчас», а не мигом времени | Часы кухонного планшета врут, и будильник, поставленный по ним, звонил бы не тогда, когда написано на экране. Секунды от сервера от часов устройства не зависят вовсе |
| Предел жизни будильника — конец текущего прохода окна чек-листа, а не местная полночь | Владелец выбрал именно это (D090): часы работы чек-листа и есть часы, когда на станции кто-то стоит. Местные сутки резали ночное окно 22:00–02:00 пополам, и будильник на 00:30, поставленный в 23:40, был запрещён — ровно тот случай, ради которого будильник заводят |
| Прошедшее время ВНУТРИ окна по-прежнему отвергается вслух, а не уезжает в следующий проход | Записка, тихо уехавшая в завтра, не прозвенит ни сегодня, ни на глазах у той смены, которая её завела. Владелец выбрал отказ, а не перенос |
| Отбор идёт по мигу звонка (`alarms.at`), а колонка местных суток снесена миграцией | Будильник на 00:30 принадлежит уже СЛЕДУЮЩИМ местным суткам: выборка «на сегодня» теряла бы его ровно в 00:00 — то есть он не прозвонил бы. Колонка, которая после этого ничего не решает, но обязана заполняться при каждой вставке, — источник расхождения, а не запас |
| Границы прохода берутся по ВСЕМ открытым чек-листам станции: от самого раннего начала до самого позднего конца | Будильник принадлежит станции, а не чек-листу, а открытых чек-листов у станции бывает несколько. Каждое открытое окно содержит текущий миг, поэтому их объединение — один непрерывный промежуток, а не набор кусков |
| Прозвонивший будильник снимается той же кнопкой «Понятно», что и крестик | Отдельное состояние «прозвонил, но висит» потребовало бы колонки и второго действия на крошечной поверхности. Разовая записка ничего не свидетельствует, и снять её — то же движение, что перевернуть таймер |
| Потолок на число будильников за проход окна считается в коде, а не правилом базы | Это заслон от набивания станции с публичной ссылки, а не правило целостности. Две одновременные записи могут проскочить на одну сверх предела, и держать ради этого в базе счётчик с блокировкой не стоит |
| Заголовки безопасности вынесены в `src/security-headers.ts`, а `/s/*` исключён из общих заголовков `next.config.ts` | Их ставят двое, и два списка разъехались бы молча именно там, где это дороже всего. Два заголовка политики на одном ответе браузер применяет пересечением — понять, что сработало, стало бы гаданием, поэтому маршрут исключён из общих, а не «перекрыт поверх» |

## Расхождения с экранным эталоном — и почему

Эталон в двух местах противоречит критериям готовности этого же блока. Побеждает критерий, эталон не переписан — решение за владельцем.

| Место | Эталон | Как сделано | Почему |
|---|---|---|---|
| `fill.html`, состояние 2 | Кнопка активна и подписана «Finish · 1 item left» при одном незакрытом пункте | Кнопка неактивна, подпись «1 item left» | Критерий 5: «Кнопка отправки неактивна, пока есть незакрытые пункты, и говорит, сколько их осталось». Состояния 1 и 3 эталона выглядят именно так — расходится только второе |
| ~~Поля будильника (время, подпись)~~ | Экран держит видимый микро-лейбл над полем в верхнем регистре (`.field__label`) | **Расхождения больше нет.** Надписи «Во сколько» и «Зачем» видны над полями, голосом `.field__label` | Владелец решил развилку сам (D093, дословно «Давай вернем»): подсказку внутри поля достаточной не признал, рост панели на строку принял. Сделано в T168 |
| Пиктограммы у полей будильника | **Ни один ввод эталона иконок не несёт** | У надписей обоих полей стоит пиктограмма: часы и листок записки | Указание владельца D094, дословно «Сделай пиктограммы , потом посмотрим». Это ОТСТУПЛЕНИЕ от эталона, и живёт оно до показа владельцу: «потом посмотрим» — не утверждение, а отложенный взгляд. Эталон не правлю — его правка или признание отступления не за блок-агентом. Отдельно: внутри самого поля времени своей иконки нет, там остаётся только нативная браузерная — см. журнал T168 |
| `states.html`, «Нет сети при отправке» | Отдельный экран во всю высоту с текстом «Your answers are kept on this screen» | Тот же экран заполнения, ответы на месте, плашка и «Отправить ещё раз» в нижней панели | Критерий 7: «ответы остаются на экране». Экран, на котором ответов не видно, но написано, что они здесь, противоречит сам себе |

## Журнал работы

Волна: один блок (`fill`), исполнителей внутри блока — двое. Остаток окна подписки
на момент запуска исполнителей — израсходовано 75% недельного окна; поэтому коммит
идёт после каждой закрытой задачи, чтобы обрыв стоил одной задачи, а не блока.

| Задача | Кем | Что сделано |
|---|---|---|
| T037 (маршрут) | сам | `src/app/s/[code]/page.tsx` и серверная часть экрана. Проверено сквозным сценарием, который строит адрес не руками, а через `stationScanUrl` блока `qr`: наклейка живёт годами, и разъехавшийся маршрут означал бы переклейку по всей сети |
| T039 (модель) | **исполнитель** | `view.ts` + 27 тестов: выбор текста по цепочке языков, склейка шапки, четыре варианта подписи диапазона, выпадение пунктов без названия. Готовый контракт, механическая работа, ни миграций, ни доступа, ни денег — обязательный случай делегирования. Исполнитель показал красный прогон, зелёный и отрицательный (снял отбрасывание пунктов без названия — упали три теста). **Проверено своими руками:** прочитал файл целиком; отдельно проверил подозрительное место — `hasTitle` считает ключи, а не значения, и пункт с пустой строкой прошёл бы. Пошёл в `editor/validation.ts`: `parseLocalizedText` пустые строки выбрасывает, в базу они не попадают — правило верное. Второе: `formatWindow("", "")` вернёт «–», но обе колонки в базе `not null`, так что случай недостижим |
| T039/T043 (состояния) | **исполнитель** | `ui/StateScreen.tsx` — три состояния по `states.html`. Разметка по готовому контракту, тестов на неё в проекте не пишут. **Проверено своими руками:** сверил каждый токен с `app.css` (кружок 56×56 на `--ok-soft`/`--ok-line`, плашка на `--warn-soft`/`--warn-line`/`--warn-ink` с текстом по левому краю), открыл все три состояния в браузере на 375 px и сверил с эталоном |
| T039 (экран) | сам | `ui/FillForm.tsx` — секции, прогресс, кнопка с остатком, числовой и текстовый ввод. Разметка интерактивная и держит правила блока, поэтому не делегирована |
| T040 | сам | Комментарий появляется прямо под проваленным критичным пунктом и запирает кнопку. Проверено в браузере настоящими касаниями и сквозным сценарием (положение поля сверяется координатами, а не наличием) |
| T041 | сам | Заполнение пишется на версию, отданную клиенту. Тест на гонку есть на обоих уровнях: модульный (`submit.test.ts`) и в браузере — сотрудник заполняет, посреди заполнения публикуется вторая версия, запись всё равно ложится на первую и со снимком её пунктов. **Отрицательный прогон:** `findStationVersion` научен брать только `published` — тест гонки упал (`expected 'refused' to be 'saved'`) |
| T042 | сам | Порядок проверок в `submit.ts`: форма тела → частота → база. Отказы неразличимы по форме и по коду ответа HTTP. Отдельный тест на перебор — и модульный, и в браузере: в ответе на подобранный код нет ни названия пиццерии, ни станции, ни чек-листа, ни другого кода ответа. Задача не делегировалась по прямому указанию задания. **Отрицательный прогон:** в отказ добавлено лишнее поле — упали два теста (`expected '[{"kind":"refused"…' not to contain 'Печь'`) |
| T043 | сам | Четыре состояния. Обрыв связи проверен и в браузере (перехват POST с отказом), и вручную подменой `fetch`: экран остаётся, «7 из 7» на месте, введённый текст на месте, кнопка становится «Отправить ещё раз», повтор после восстановления связи доходит |
| T071 | сам | Одноразовый ключ вместо `'unsafe-inline'` на публичном маршруте. Правка `proxy.ts` — та самая «минимальная», о которой говорило задание: добавлен `/s/:path*` в matcher и ветка, которая срабатывает первой и до сессии не доходит. Из общих заголовков `next.config.ts` маршрут исключён. Задача не делегировалась по прямому указанию задания. **Отрицательный прогон:** ключ сделан постоянным — сценарий упал (`expected not to be "…'nonce-AAAAAAAAAAAAAAAAAAAAAA=='…"`). Первый заход отрицательного прогона не считается: подстановка с кириллицей уронила заголовок целиком, то есть проверила не то — повторено на ASCII |
| Сборка | сам | `next build` уронил то, чего не видели 112 зелёных тестов: клиентский пакет тянул драйвер `pg` через вход блока `data`. Правила заполнения работают в браузере, поэтому `grading`/`types` ввозятся из самих файлов |
| Живой браузер | сам | Экран открыт рядом с эталоном и сверен обмером, а не на глаз: шапка 107 px, пункт 52 px, заголовок секции 46 px — совпадает с эталоном в пиксель. По дороге найдены и исправлены два дефекта: отметка выполненного пункта не закрашивалась (`bg-surface` базового класса перебивал цвет состояния, белая галочка на белом фоне), и три межстрочных интервала были заданы там, где эталон их наследует |
| T038 | сам | Цепочка языков экрана: язык устройства → язык страны пиццерии → русский (`locale.ts`). Своя функция вместо `pickLocale` блока core — та отвечает одинаково на «телефон просит английский» и «телефон просит язык, которого у нас нет», а экрану заполнения эти случаи надо различать. Тем же файлом выбор текста пункта по цепочке. **Отрицательный прогон** — ниже, вместе с остальными |
| T037 (правила) | сам | `station.ts`: что отдаётся по коду — версия, название пиццерии, название станции, язык страны, и ничего больше. Три исхода: `unknown-code`, `no-checklist`, `ok`. Правило выбора версии по окну и часовому поясу не переписано — вызывается `getPublishedVersionForStation` блока `data`. Отдельно `findStationVersion(code, versionId)`: версия принимается, только если её замороженная станция — станция этого кода. Без этой проверки один живой код с наклейки позволял бы писать заполнения в историю любой станции сети (`saveSubmission` выводит станцию из версии). **Отрицательный прогон:** проверка принадлежности снята — тест «не отдаёт версию чужой станции» упал (`expected { …(3) } to be null`) |
| T039/T040 (правила) | сам | `answers.ts`: что считается отвеченным пунктом, сколько осталось, когда открывается кнопка. Провал считается не своим правилом, а `isFailed` блока `data` — иначе экран сотрудника и лента управляющего разошлись бы в том, что невыполнено. Проваленный критичный пункт без комментария закрывает отправку; число вне диапазона отправку не закрывает (критерий 4). **Отрицательный прогон:** требование комментария отключено — упали три теста (`expected [] to strictly equal [ 'c' ]`, `expected true to be false`, `expected [] to strictly equal [ 'n' ]`) |
| T042 (правила) | сам | `validation.ts` — схема тела отправки на границе (типы, длины, пределы; наружу выходит новый объект, поля входа дальше не идут) и сверка ответов со снимком версии: чужой пункт, повтор, чужой тип значения и проваленный критичный без комментария отбиваются сервером, а не только кнопкой. `rate-limit.ts` — свой счётчик частоты (границы модулей запрещают `fill` зависеть от `auth`; там к тому же считаются неудачи, а здесь все обращения). **Отрицательный прогон:** отказ научен продлевать окно и снят потолок числа ключей — упали три теста (`expected false to be true` дважды, `expected 50 to be 3`) |
| T081 (разбор безопасности) | сам | Ключ предела на открытие экрана перестал браться из того, что клиент сообщает о себе сам. `identifyClient` принимает теперь три вещи — заголовок как пришёл, адрес соединения (если среда его даёт) и число доверенных посредников, объявленное площадкой (`TRUSTED_PROXY_HOPS`), — и выбирает звено цепочки ПО СЧЁТУ ОТ КОНЦА: приписанное слева на выбор не влияет. Посредников не объявлено — заголовок не читается вовсе; `X-Real-IP` не читается больше никогда (площадка, где посредник ставит только его, настраивает `X-Forwarded-For`). Различить клиентов нечем — предел не применяется, и это сказано вслух строкой при старте, а не молчанием (`src/startup-checks.ts`). Пределы отправок не тронуты: они считаются по коду станции и по сети целиком. **Красный прогон настоящий:** 9 тестов до правки, из них по существу `expected [ '9.9.9.9', '8.8.8.8', …(2) ] to strictly equal [ '198.51.100.4' ]` (подделанный заголовок открывал новый счётчик) и `expected true to be false` (так обходился предел на открытие экрана). **Отрицательный прогон:** выбор звена возвращён к первому в цепочке — упали ровно эти два теста. Второй отрицательный прогон (снят порог `<= 0` на числе посредников) НЕ уронил ничего: при нуле отсчёт от конца и так выводит за цепочку — то есть условие здесь явность, а не единственная опора |
| T122 (issue #33) | сам | `src/messages/ru.json`, ключ `fill.left`: латинская «c» внутри кириллического «осталcя» → «остался». Строка настоящего экрана заполнения. Вместо разовой правки заведён сторож класса дефекта — `src/blocks/fill/mixed-script.test.ts`: единица проверки — не строка и не символ, а слово (`\p{L}+`, максимальный буквенный пробег); смешением считается пробег, где встретились обе азбуки, в любую сторону. Прогнан по обоим словарям целиком (`ru.json`, `en.json`) — подтверждает находку editor: до правки поймано ровно одно слово, других нет. **Красный прогон настоящий:** тест на реальных словарях падал на `ru:fill.left → «осталcя»` до правки JSON. **Отрицательный прогон — таблица ниже, класс, а не случай** |
| T139 (issue #52) — правила и схема | сам | Будильники станции: миграция `0008_alarms` (пара файлов и запись в журнале, вручную — `db:generate` выключен D089), таблица `alarms` в схеме и `src/blocks/fill/alarms.ts` — заведение, список, снятие. Задача трогает миграции и точку записи, открытую интернету, — делегированию не подлежит. Накат проверен на ПУСТОЙ базе (`down -v` → `up` → `db:migrate`, вся история 0000–0008), обратный ход тоже: `db:rollback` сносит `alarms`, повторный накат возвращает. **Отрицательный прогон — три заслона разом:** снято условие принадлежности станции при снятии, снято условие местных суток при чтении, `<=` заменено на `<` в проверке прошедшего времени. Упали ровно три теста (`expected [] to deeply equal [ 'чужая записка' ]`, `expected [ { …(4) } ] to strictly equal []`, `expected { Object (kind, alarms) } to strictly equal { kind: 'refused', …(2) }`). Восстановлено из копии, снятой до порчи, а не `git checkout` |
| T139 — панель на экране | сам | `ui/AlarmsPanel.tsx` и `ui/alarm-action.ts`: строка «время · подпись · убрать», заведение, плашка звонящего будильника. Разметка интерактивная и держит правила блока (когда звонить, что считать снятым) — по тому же правилу, по которому не делегирован `FillForm`. **Найдено и исправлено своей проверкой в браузере:** надпись «звук не пошёл» врала — состояние звука спрашивалось сразу после вызова разрешения, а оно в этот миг всегда «suspended». Проверено обоими путями: с разрешением строки нет, с запретом (среда подменена так, что `resume()` проходит, а состояние остаётся «suspended») она появляется |
| T139 — сквозной сценарий | **исполнитель** | `e2e/fill-alarms.spec.ts`, 5 сценариев на 375 px настоящими касаниями: заведение, отказ на прошедшем времени, снятие крестиком, переживание перезагрузки планшета и изоляция по станции. Готовый контракт (опознаватели зафиксированы заранее), механическая работа по образцу `rounds.spec.ts`, ни миграций, ни доступа, ни денег — обязательный случай делегирования. Первый прогон 4/5: исполнитель нашёл гонку в собственном тесте (второй будильник заводился раньше ответа сервера на первый, кнопка оставалась запертой), поправил ожидание, повторный прогон 5 passed за 1,5 мин. **Отрицательный прогон исполнитель не довёл** — я остановил его раньше; довожу сам, см. строку ниже |
| T139 — живой браузер и сверка | сам + проверяющий | Экран открыт на 375 px, всё меряно, а не на глаз: подвал сверху 727 — низ панели 727, перекрытия нет; зон нажатия меньше 44 px нет; ширина документа 375, горизонтальной прокрутки нет; ошибок в консоли нет. Проверяющий (`norma`) нашёл одно настоящее расхождение и два мнимых. **Настоящее:** на 375 px «время + подпись + кнопка» в строку не помещались, поле подписи сжималось до 158 px и подсказка обрывалась на середине — форма перестроена в две строки, поле подписи стало 343 px. **Мнимые, снятые обмером:** «липкий подвал режет панель» — это был след моих снимков (`scrollIntoViewIfNeeded` оставляет подвал висеть над содержимым; на настоящем низу страницы перекрытия нет, замер выше), и «время набрано не тем шрифтом» — вычисленный шрифт строки будильника `IBM Plex Mono`, то есть `--font-num`, как у эталона. Первые снимки переснял, чтобы следующий проверяющий не ловил тот же след |
| T139 — отрицательный прогон сквозного сценария | сам | Доведён за исполнителя. В главном сценарии («переживает перезагрузку планшета») опознаватель плашки подменён на заведомо неверный — прогон покраснел: `expect(locator).toBeVisible() failed · element(s) not found`, `1 failed`. Значит сценарий читает настоящий экран, а не проходит вхолостую. Файл восстановлен из копии, снятой до порчи (`cp`), а не `git checkout`; дерево после восстановления чистое |
| Полный прогон | сам | `./scripts/check` целиком: 1162 модульных проверки и 156 сквозных сценариев без единого падения, мёртвого кода нет, границы модулей целы и сборка зелёная. Первый прогон упёрся в формат файла исполнителя — поправлено отдельным коммитом |
| T159 (issue #68) — предел жизни будильника | сам | Правило по решению владельца D090: будильник живёт до конца текущего прохода окна чек-листа, а не до местных суток. Задача трогает миграцию (`0009_alarm_window` парой файлов и записью в журнале — `db:generate` выключен D089), точку записи, открытую интернету, и договор об отказах — делегированию не подлежит по всем трём признакам сразу. Границы прохода и мгновения-кандидаты считает база из часового пояса пиццерии (D026): второго календаря в JavaScript не заводится. Новый отказ `outside-window` заведён точечной вставкой в оба словаря; тексты «на сегодня» из отказов и подсказки убраны — они стали неправдой |
| T159 — схема | сам | Колонка `alarms.local_date` снесена, индекс стал `(station_id, at)`. Без этого правка была бы только наполовину: строка на 00:30, заведённая в 23:40, принадлежит уже СЛЕДУЮЩИМ местным суткам, и выборка «на сегодня» теряла бы её в 00:00 — будильник не прозвонил бы. Накат проверен на ПУСТОЙ базе (`down -v` → `up -d` → `db:migrate`, вся история 0000–0009, десять записей в `drizzle.__drizzle_migrations`), обратный ход проверен с настоящей строкой: `db:rollback` вернул колонку и пересчитал её из мига звонка и пояса пиццерии (будильник 00:30 UTC → `local_date` 2026-09-17), повторный накат снова её снёс |
| T159 — отрицательный прогон | сам | Четыре порчи подряд, каждая восстановлена из копии, снятой ДО порчи, а не `git checkout`. (1) Конец окна срезан полуночью — упали 4 проверки, включая обе околополуночные. (2) Список отбирается по местной дате станции, как было до правки, — упали ровно те две, ради которых задача и заведена (`expected undefined to be '00:30'`, `expected [] to deeply equal [ 'вынести тесто' ]`): именно так будильник исчезал бы с панели в 00:00. (3) Заслон «время вне окна» снят так, что прошедшее время молча уезжает в следующий проход, — упали обе проверки про отказ вслух, то есть «значит, завтра» тесты не пропускают. (4) Отбор кандидатов по окну снят вовсе — упали три. Часы около полуночи с обеих сторон (23:40 и 00:10) в наборе есть: дневной прогон здесь не доказывает ничего |
| T159 — живой браузер | сам + проверяющий | Экран станции демо-стенда (окно чек-листа 06:00–23:00) в настоящем браузере на 375 px, оба языка. Отказ «вне часов» называет часы чек-листа: «Будильник ставится только в часы чек-листа: 06:00–23:00.» / «An alarm can only be set within the checklist hours: 06:00–23:00.»; прошедшее время внутри окна отбивается прежним отказом; будильник на 17:15 заводится строкой и снимается крестиком. Подсказка под панелью больше не обещает «до конца суток». Ширина документа 375, горизонтальной прокрутки нет, зоны нажатия 44 px и больше, ошибок в консоли нет. Снимки: `alarm-otkaz-vne-chasov.png`, `alarm-zaveden.png` |
| Полный прогон (T159) | сам | `./scripts/check` целиком зелёный: формат, типы, линт, 1168 модульных проверок (было 1162), мёртвый код, границы модулей, 156 сквозных сценариев. Покрытие 87,13% строк-утверждений. Ложных падений по таймауту (дефект #67) в этом прогоне не было |
| T159 — правка после разбора диспетчером | сам | Две находки свежего взгляда. (1) Запись в CHANGELOG противоречила сама себе: первый абзац объединённой записи T139+T159 обещал «к концу местных суток она исчезает сама» — то самое поведение, которое D090 отменило. Вычищено. (2) Отказ «не те часы» называл часы ОДНОГО чек-листа (`view.window` из шапки экрана), а граница берётся по объединению всех открытых разом: сотрудник читал «06:00–12:00», ставил на 15:00 и молча попадал. Теперь часы несёт сам отказ (`AlarmRefusal.hours`) — из того же запроса, который вынес решение, — а экран про них не знает вовсе: поле `window` из модели экрана и опора панели убраны, чтобы не оставалось второго источника. Часы объединения названы «рабочие часы станции», а не «часы чек-листа»: при нескольких открытых чек-листах это уже не окно одного из них. Отказ без часов (у станции не открыто ничего) получил свой текст на обоих языках |
| T159 — отрицательный прогон правки | сам | Проверка «при нескольких открытых чек-листах отказ называет ту границу, по которой и решает» написана до кода и упала на старой сборке (`expected { kind: 'refused', …(2) } to strictly equal { kind: 'refused', …(3) }`), вместе с проверкой часов в отказе для дневной и ночной станции. Случай взят штатный: обход 07:00–20:00 поверх открытия смены 06:00–12:00, будильник на 15:00 принимается, на 21:00 отвергается с часами «06:00–20:00» |
| Полный прогон после правки | сам | `./scripts/check`: формат, типы, линт, мёртвый код, границы модулей зелёные; 1169 модульных проверок и 156 сквозных. Упали четыре — и все четыре ложные, дефект #67: два модульных `Test timed out in 5000ms` (`fill/rounds.test.ts` «поток отметок упирается в предел частоты», `library/propagation.test.ts` «правка блока приходит во все черновики») и два сквозных по таймауту навигации в чужих блоках (`checklists-filter.spec.ts`, `library.spec.ts`), ни один не связан с правкой. Перепроверка в один поток: `vitest --maxWorkers=2` по обоим файлам — 16 проверок, все зелёные; сквозные перегнаны `playwright --workers=1` |
| T168 (issue #78) — видимые надписи и пиктограммы у полей будильника | сам | Прямое указание владельца, D093 «Давай вернем» и D094 «Сделай пиктограммы , потом посмотрим». Обе надписи были только для чтения с экрана (`sr-only`), а у поля времени и плейсхолдер пустой — на экране его не называло ничто. Теперь над каждым полем стоит видимая надпись голосом `.field__label` эталона (верхний регистр, микро-кегль, разрядка, `--ink-3`), у надписей — пиктограммы: часы и листок записки, 13 px, цвет наследуется. Тексты уже лежали в словаре обоих языков (`fill.alarms.timeLabel` / `labelLabel`) — новых строк заводить не пришлось. Панель выросла на строку, это принято владельцем. Сам, а не исполнителем: правка на два десятка строк в одном файле, и решение про нативную иконку (строка ниже) принимать было некому |
| T168 — нативная иконка часов | сам | Своя пиктограмма НЕ ставится внутрь поля времени и нативная НЕ прячется. `input[type="time"]` рисует иконку часов силами браузера, и вторая в том же поле дала бы два циферблата в одном вводе — не то, о чём просили. Спрятать нативную (`::-webkit-calendar-picker-indicator`) тоже отказался: в Chromium это и есть кнопка, открывающая выбор времени, а на кухонном планшете нажатие — основной способ ввода; убрав её ради вида, мы забрали бы работающее нажатие, и на разных браузерах это выглядит по-разному. Поэтому пиктограмма стоит в строке НАДПИСИ, а не в поле: уровни разные, дублирования в одном вводе нет. Закреплено проверкой «в поле времени своей пиктограммы нет» |
| T168 — отрицательный прогон | сам | Обе проверки написаны ДО кода и упали на старой сборке: «надпись не отрисована» (элемент не найден) и «пиктограммы нет». Проверяется РАЗМЕР отрисованной надписи, а не её наличие: `sr-only` оставляет элемент в дереве коробкой 1×1 px, и `toBeVisible` на нём проходит — проверка «надпись есть» была бы зелёной и до правки, то есть не проверяла бы ничего |
| T167 (issue #77) — неразобранное начало окна | сам | Выбран громкий отказ, а не комментарий: тот же приём уже стоит рядом в `passLocalDate`, и два разных ответа на один вопрос в одном файле разъехались бы. `checks.ts`: `parseLocalTime(window.start) ?? 0` заменён на `windowStartMinutes(window)` — публичная функция, потому что настоящие данные в отказную ветку не попадают и через базу её не проверить (испортить значение, которое стережёт сама колонка `time`, нельзя). `rounds-view.ts`: свой разбор с тем же отказом — общий пришлось бы вывозить через `data/index.ts`, а он в этой волне чужой. Четыре строки повтора дешевле лишней связи между блоками; если файл когда-нибудь освободится, объединять их в `schedule.ts`. Сам: правка на две строки в двух файлах |

### T122 — отрицательный прогон: класс дефекта, а не случай

Урок задания: проверка, подтверждённая единственной порчей, два года ловила только её.
Ниже — порчи разных видов и рядом законные строки, которые проверка обязана пропустить.

| Порча / случай | Поймана? | Чем проверено |
|---|---|---|
| Реальная находка: латинская «c» внутри «осталcя» | поймана | `ru.json` до правки — тест на реальном словаре падал |
| Латиница внутри кириллического слова (гомоглиф-подмена, синтетика: с→c, е→e, о→o, р→p, а→a, х→x, у→y) | поймана | 7 подмен по одной букве в разных словах — все дают ровно одно смешанное слово |
| Кириллица внутри английского слова (зеркальный случай: `rеset` с кириллической «е», U+0435) | поймана | `mixedScriptWords("rеset")` возвращает `["rеset"]` |
| `QR-коды`, `QR-код`, `MERIDIUS` и `Dodo` внутри русской фразы | НЕ поймана (верно) | дефис/пробел разрывают буквенный пробег — это не смешение внутри слова, а законное соседство алфавитов в строке |
| `iPhone'а`, `что-то-MERIDIUS-ещё` (склейка через апостроф/дефис) | НЕ поймана (верно) | апостроф и дефис — граница слова, как и выше |
| Плейсхолдер ICU `{count, plural, one {...} ...}` рядом с кириллицей | НЕ поймана (верно) | ключевые слова ICU (`plural`, `one`, `few`…) — отдельные буквенные пробеги, кириллицу не задевают |
| Целое слово по ошибке не на том языке (`checklist` вместо «чек-лист») | НЕ поймана (осознанно, другой класс) | пробег целиком в одном алфавите — это дефект перевода/полноты словаря, не смешения алфавитов; вне области этой проверки |
| Цифро-буквенные гомоглифы (`0`/`О`, `3`/`З`, `1`/`l`) | не проверялось | другой класс дефекта (цифра↔буква, не алфавит↔алфавит); в задаче не заявлен, за пределами T122 |

Все семь тестов таблицы — рабочий код `src/blocks/fill/mixed-script.test.ts`
(`describe("отрицательный прогон: класс дефекта, а не случай")`), не только описание:
прогон подтверждён `npx vitest run src/blocks/fill/mixed-script.test.ts` — 9/9 зелёных
после правки JSON, красный до неё (см. запись T122 выше).

## Что проверить на площадке про туннель

Продукт публикуется наружу через `tailscale funnel` (D034). Передаёт ли туннель адрес
клиента и в каком виде — **на площадке не проверено**, поэтому предел на открытие экрана
по клиенту по умолчанию не применяется (`TRUSTED_PROXY_HOPS` не задан), и продукт говорит
об этом строкой в журнале при старте. Что сделать после раскатки, по порядку:

| Шаг | Что сделать | Что значит результат |
|---|---|---|
| 1 | Открыть экран заполнения снаружи, через адрес туннеля, и посмотреть, какие заголовки о клиенте доходят до приложения: есть ли `X-Forwarded-For`, одно в нём звено или несколько | Заголовка нет вовсе — предел по клиенту на этой площадке невозможен: оставляем выключенным и не делаем вид, что он есть |
| 2 | Повторить запрос, подставив своё значение в тот же заголовок, и сравнить с шагом 1 | Значение доехало нетронутым — туннель заголовок не переписывает, доверять ему нельзя, `TRUSTED_PROXY_HOPS` остаётся 0. Появилось звено справа от подставленного — туннель дописывает своё, и число посредников можно объявить |
| 3 | Объявив `TRUSTED_PROXY_HOPS` (обычно 1), пересобрать продукт: значения переменных запекаются в сборку (D033) | Без пересборки переменная не действует, и выглядит это как «настроили, а не работает» |
| 4 | Убедиться, что предел не стал рубильником на всю сеть: открыть экран с двух устройств через одну точку выхода в интернет (пиццерия за одним внешним адресом) и проверить, что исчерпание предела одним не запирает второго | Разные устройства сводятся к одному ключу — предел считается по сети, а не по клиенту; тогда его либо поднимают, либо оставляют выключенным |
| 5 | Проверить обратное — что предел не пустышка: исчерпать его с одного устройства и увидеть экран «слишком часто» | Отказ не наступает — ключ меняется на каждый запрос, значит настройка неверна |

Отдельно: пределы на ОТПРАВКУ заполнений от этого не зависят вовсе — они считаются
по коду станции и по сети целиком, и туннель на них не влияет.

## Что осталось владельцу и диспетчеру (T139)

Три вещи в этой задаче не сделаны намеренно — решать их не мне.

| Что | Почему не сделано | Что с этим делать |
|---|---|---|
| **Переключатель блока будильников в редакторе.** Сегодня панель есть на КАЖДОЙ станции: включить или выключить её методист не может | Переключатель живёт в файлах блока `editor` (карточка свойств, `editing.ts`, `validation.ts`) и в `data/types.ts` — ровно в тех, которые прямо сейчас переписывает блок `editor` под T137. Правка там означала бы конфликт слияния в файлах, переписываемых параллельно, то есть ту самую поломку, ради которой схему в волне ведёт один блок | Маленькая задача блоку `editor` в следующей волне: колонка-признак у чек-листа плюс переключатель в карточке свойств. Хранилище будильников от этого не меняется — признак только прячет панель |
| ~~**Ночная смена через полночь.**~~ Закрыто владельцем решением D090 и сделано в T159: будильник живёт до конца окна чек-листа, ночное окно 22:00–02:00 принимает время после полуночи | — | Ничего: правило изменено, колонка местных суток снесена миграцией `0009_alarm_window`, отдельная единица хранения не понадобилась |
| **Панели будильников нет в экранном эталоне** `docs/furca/design/screens/fill.html` | Эталон рисует не блок-агент | Проверяющий оценил поверхность как заметную: вес панели на экране сопоставим с панелью обходов — толстый разделитель, заголовок в верхнем регистре, своя форма. То есть эталон дорисовывать нужно |

## Статус

in progress — волна T168/T167/T162/T138
