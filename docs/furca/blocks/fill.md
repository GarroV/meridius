<!-- Описание блока. Контракт здесь — источник истины для блок-агента. -->

# Block: fill

## Назначение

Публичный экран заполнения по QR — единственная часть продукта, доступная без входа. Здесь считается каждое касание.

## API-контракт

```
GET  /s/<код станции>     → опубликованная версия по станции и окну времени
POST /s/<код станции>     → заполнение { answers, startedAt }
```
Ответ не содержит ничего, кроме самого чек-листа: ни истории станции, ни списка чек-листов, ни сведений о пиццерии сверх названия (D021). Частота отправок с одного кода ограничена.

## Зависимости

`data`, `editor`

## Definition of Done блока

1. Сканирование QR открывает нужный чек-лист без единого касания сверх сканирования.
2. Язык экрана задаёт пиццерия (D122): и чек-лист, и отбивки, и служебные надписи идут на языке, заведённом у её страны. Язык устройства работает вторым звеном — только там, где на языке пиццерии перевода пункта нет. Где пиццерии нет вовсе (код не работает), язык берётся с телефона, а последнее звено — язык продукта, тот же `DEFAULT_LOCALE`, что у всего остального.
3. Провал критичного И важного пункта требует комментарий и не даёт отправить без него; комментарий появляется сразу под пунктом, не отдельным экраном (D018, D056). Обычный пункт можно провалить молча: принуждение к объяснению про непротёртый стол учит писать «ок» в каждое поле.
4. Числовой пункт принимает значение и показывает, попало ли оно в диапазон, но не блокирует отправку.
5. Кнопка отправки неактивна, пока есть незакрытые пункты, и говорит, сколько их осталось.
6. Неизвестный или перевыпущенный код даёт понятный отказ, а не пустой экран и не чужой чек-лист.
7. Обрыв связи при отправке не теряет введённое: ответы остаются на экране, отправка повторяется.
8. Перебор кодов не отдаёт ничего, кроме отказа — отдельный тест.
9. Заполнение пишется на ту версию, что была отдана клиенту, даже если за это время опубликовали новую.
10. Экран работает на ширине 375 px, зоны нажатия не меньше 44 px, горизонтальной прокрутки нет.
11. Экран совпадает с эталоном `docs/furca/design/screens/fill.html`.
12. Режим смены виден в шапке и меняется одним касанием, не удлиняя путь сотрудника: чек-лист открывается сразу, полной сменой по умолчанию (D052, D055).
13. Будильник заводится вручную — время и подпись, — звонит в назначенное время и переживает перезагрузку планшета: он лежит строкой в базе, а не в памяти вкладки. Живёт до конца окна работы чек-листа — включая окно, переходящее через полночь, — регулярности у него нет (D070, D090). Ограничение «звук работает, пока экран открыт» названо на самом экране.
14. Просроченная проверка поднимает сигнал СТАНЦИИ: он встаёт по закрытию прохода без отметки, три просрочки дают один звонок по самой частой из настроек методиста (10/20/60 или молчать, D068), гаснет с отметкой, наружу не эскалируется (D073) и адресован месту, а не человеку (D001). Ограничение «звук работает, пока этот экран открыт» названо на самом экране постоянной строкой, а не только в момент звонка.

## Устройство блока

| Файл | Что внутри |
|---|---|
| `src/app/s/[code]/page.tsx` | Единственный маршрут, открытый интернету. Ничего не собирает — экран ходит за данными сам |
| `src/blocks/fill/station.ts` | Что отдаётся по коду: версия, название пиццерии и станции, язык страны. Три исхода; отбивка «заполнять нечего» несёт язык страны и больше ничего. Плюс `findStationVersion` — версия принимается, только если она заморожена на станции этого кода |
| `src/blocks/fill/locale.ts` | Цепочка языков: страна пиццерии → устройство → язык продукта; выбор текста пункта по цепочке |
| `src/blocks/fill/answers.ts` | Правила экрана: что отвечено, сколько осталось, когда открыта кнопка, что уходит на сервер |
| `src/blocks/fill/validation.ts` | Схема тела отправки на границе и сверка ответов со снимком версии |
| `src/blocks/fill/rate-limit.ts` | Ограничение частоты: отправки по коду и по сети, открытия экрана по адресу клиента |
| `src/blocks/fill/submit.ts` | Приём заполнения: порядок проверок и запись на версию, отданную клиенту |
| `src/blocks/fill/ticket.ts` | Пропуск на отправку: серверное время выдачи экрана под подписью. Оттуда берётся начало заполнения — браузер его больше не называет |
| `src/blocks/fill/repeat.ts` | Повторная отправка: заполнение, уже записанное по этому пропуску, ищется по паре «версия + начало»; одновременный повтор ловит правило базы, и `saveOnce` превращает его отказ в квитанцию первой записи |
| `src/blocks/fill/view.ts`, `model.ts` | Модель экрана: тексты уже выбраны по языку и склеены |
| `src/blocks/fill/ui/FillScreen.tsx` | Серверная часть: данные, язык, словарь клиентской части |
| `src/blocks/fill/ui/FillForm.tsx` | Экран заполнения: секции, прогресс, кнопка, комментарий, отправка, обрыв связи |
| `src/blocks/core/ui/StateScreen.tsx` (живёт в `core` с волны 6) | Состояния без заполнения: отправлено, код не действует, заполнять нечего. Экран общий с сотрудническими отказами публичной стороны, поэтому переехал в `core`; `fill` его импортирует |
| `src/blocks/fill/ui/submit-action.ts` | Серверное действие: POST уходит на тот же `/s/<код>` |
| `src/blocks/fill/shift-mode.ts` | Границы выбора режима смены: разбор ввода, ограничение частоты, запись режима пиццерии |
| `src/blocks/fill/ui/ShiftModeBar.tsx`, `shift-mode-action.ts` | Строка «Смена: … · сменить» в шапке и панель выбора режима |
| `src/blocks/fill/alarms.ts` | Будильники станции: форма тела, потолок на проход окна, заведение, список на текущий проход окна и снятие. Границы прохода окна и миг звонка считает база из часового пояса пиццерии |
| `src/blocks/fill/overdue.ts` | Сигнал станции о просроченной проверке: кто звонит, как часто и когда вкладке перерисоваться. Чистый расчёт из состояния, уже посчитанного панелью |
| `src/blocks/fill/ui/OverduePlate.tsx` | Плашка сигнала и двое часов при ней: перерисовка страницы к ближайшей границе прохода и повтор звонка |
| `src/blocks/fill/ui/beep.ts` | Гудок планшета, один на будильники и на сигнал о просрочке |
| `src/blocks/fill/ui/refresh-clock.ts` | Часы перерисовки: когда вкладке сходить к серверу за состоянием обходов. Взводятся заново после каждого срабатывания — в том числе когда очередная величина совпала с предыдущей |
| `src/blocks/fill/ui/AlarmsPanel.tsx`, `alarm-action.ts` | Панель будильников: строка «время · подпись · убрать», поле заведения, плашка звонящего будильника с кнопкой «Понятно» |
| `src/security-headers.ts` | Заголовки безопасности в одном месте: их ставят `next.config.ts` и `src/proxy.ts` |
| `e2e/fill.spec.ts`, `e2e/fill-fixtures.ts` | Сквозной путь в браузере на 375 px настоящими касаниями |

## Решения, принятые внутри блока

| Решение | Почему |
|---|---|
| Отправка — серверное действие, а не обработчик `route.ts` | Контракт требует `POST /s/<код>`, а `page.tsx` и `route.ts` в одном сегменте Next не уживаются. Серверное действие уходит POST-запросом ровно на этот адрес |
| Версия принимается, только если она заморожена на станции отсканированного кода | Идентификатор версии приходит из браузера. Без этой проверки один живой код с наклейки позволял бы писать заполнения в историю любой станции сети: `saveSubmission` выводит станцию из версии и записал бы их туда, где никто не заполнял. Архивная версия проходит намеренно — это и есть T041 |
| Неизвестный код, перевыпущенный код и версия чужой станции дают один и тот же отказ, и с тем же кодом ответа HTTP | Различать их значило бы отвечать перебору по-разному. Перевыпуск переписывает код станции, прежней строки не остаётся — так что «отозван» и «не существовал» неотличимы и в данных |
| «Заполнять нечего» — отдельное состояние от «код не действует» | Единственное, что этот ответ сообщает, — что код живой; данных в нём нет никаких. Зато сотруднику с настоящей наклейкой говорится правда, и он не идёт к управляющему за новой (принцип 2) |
| Логический пункт переключается по кругу: не отвечено → выполнено → не выполнено | В эталоне у пункта одна зона нажатия, а «нет» — такой же нужный ответ, как «да». Обычный случай (всё выполнено) стоит одного касания на пункт, провал — двух (принцип 2) |
| Правило провала не своё, а `isFailed` блока `data` | Иначе экран сотрудника и лента управляющего разошлись бы в том, что считать невыполненным. Ради этого в блоке есть `gradingSections`: модель экрана переводится обратно в пункты, понятные общему счёту |
| Правило «критичный провален → нужен комментарий» держится и сервером | Тело запроса отправляет кто угодно, а обещание «комментарий будет» дано управляющему. Кнопка в браузере — удобство, а не защита |
| Неполный чек-лист сервер принимает, пустой — нет | Отвергнуть почти готовое заполнение значит потерять работу сотрудника, а неполнота и так видна в ленте (принцип 2). Пустой список отвергается: сохранять нечего |
| Свой счётчик частоты, не общий с блоком `auth` | Границы модулей запрещают `fill` зависеть от `auth`. Считается и другое: у входа — неудачи, здесь — все обращения подряд, потому что заполнение всегда «удачно» |
| Предел на открытие экрана применяется, только когда клиента есть чем различить | Публичная площадка — туннель, который может не передавать адрес клиента (D034). Один общий ключ превратил бы предел «на клиента» в рубильник, гасящий всю сеть на пересменке. Точку записи при этом прикрывают пределы отправок: они считаются по коду и по сети и от адреса не зависят |
| Язык экрана считается здесь, а не общим `src/i18n/request.ts` | Общий знает только телефон, а язык этой поверхности принадлежит пиццерии (D122). Язык проставляется на самом экране (`lang`), а не на `<html>`: корневая разметка общая с админкой и в базу не ходит |
| Модуль правил заполнения ввозит `grading`/`types` блока `data` напрямую, а не через его вход | Он работает в браузере, а вход блока `data` тянет за собой драйвер `pg`, и клиентская сборка падает. Найдено настоящей сборкой при зелёных тестах. Тот же приём и по той же причине уже применён к миграциям — см. хвост `src/blocks/data/index.ts` |
| Будильник принадлежит СТАНЦИИ, а не чек-листу и не версии | На станции может быть открыто несколько чек-листов (экран выбора), а планшет у неё один. Будильник, привязанный к версии, исчезал бы при публикации следующей посреди смены — ровно там, где он и нужен |
| Отсчёт до звонка уходит в браузер секундами от серверного «сейчас», а не мигом времени | Часы кухонного планшета врут, и будильник, поставленный по ним, звонил бы не тогда, когда написано на экране. Секунды от сервера от часов устройства не зависят вовсе |
| Предел жизни будильника — конец текущего прохода окна чек-листа, а не местная полночь | Владелец выбрал именно это (D090): часы работы чек-листа и есть часы, когда на станции кто-то стоит. Местные сутки резали ночное окно 22:00–02:00 пополам, и будильник на 00:30, поставленный в 23:40, был запрещён — ровно тот случай, ради которого будильник заводят |
| Прошедшее время ВНУТРИ окна по-прежнему отвергается вслух, а не уезжает в следующий проход | Записка, тихо уехавшая в завтра, не прозвенит ни сегодня, ни на глазах у той смены, которая её завела. Владелец выбрал отказ, а не перенос |
| Отбор идёт по мигу звонка (`alarms.at`), а колонка местных суток снесена миграцией | Будильник на 00:30 принадлежит уже СЛЕДУЮЩИМ местным суткам: выборка «на сегодня» теряла бы его ровно в 00:00 — то есть он не прозвонил бы. Колонка, которая после этого ничего не решает, но обязана заполняться при каждой вставке, — источник расхождения, а не запас |
| Границы прохода берутся по ВСЕМ открытым чек-листам станции: от самого раннего начала до самого позднего конца | Будильник принадлежит станции, а не чек-листу, а открытых чек-листов у станции бывает несколько. Каждое открытое окно содержит текущий миг, поэтому их объединение — один непрерывный промежуток, а не набор кусков |
| Прозвонивший будильник снимается той же кнопкой «Понятно», что и крестик | Отдельное состояние «прозвонил, но висит» потребовало бы колонки и второго действия на крошечной поверхности. Разовая записка ничего не свидетельствует, и снять её — то же движение, что перевернуть таймер |
| Потолок на число будильников за проход окна считается в коде, а не правилом базы | Это заслон от набивания станции с публичной ссылки, а не правило целостности. Две одновременные записи могут проскочить на одну сверх предела, и держать ради этого в базе счётчик с блокировкой не стоит |
| Сигнал о просрочке стоит, пока идёт НЕОТМЕЧЕННЫЙ проход, а не пока `missedCount > 0` | Отметка всегда встаёт в текущий проход и пропущенные не догоняет (D066), поэтому после первого же пропуска счётчик не обнуляется никогда. Сигнал на одном счётчике стало бы невозможно погасить — а D068 требует «гаснет с отметкой». Гасит его факт отметки: строка уходит в `done`, и звонящих пунктов не остаётся |
| Без текущего прохода станция молчит, даже когда пропуски есть | Отметить сейчас всё равно нельзя: запись отказывает «обхода сейчас не ждут». Звонок, который нечем погасить, — издевательство над сменой, а не напоминание |
| Плашку сигнала нельзя закрыть кнопкой | Кнопка «понятно» превратила бы сигнал в то, что отщёлкивают не глядя, а пропуск остался бы пропуском. Закрывается плашка делом — отметкой |
| Вкладка перерисовывается с сервера к ближайшей границе прохода, а не считает состояние сама | Проход и просрочку считает сервер по местному времени пиццерии (D026), а часы кухонного планшета врут. Без этих часов просрочка, наступившая при открытом экране, не показалась бы до следующего касания — то есть сигнал не сработал бы ровно там, где он и нужен |
| Ограничение звука названо постоянной строкой под панелью, а не только в момент звонка | Узнать, что звук работает лишь при открытом экране, в ту минуту, когда сигнал уже пропущен, — поздно. Строки нет вовсе, если ни у одного пункта оповещение не настроено: обещать звук там, где его не будет, хуже молчания |
| Гудок вынесен в `ui/beep.ts` и делится с будильниками | Две копии разъехались бы по громкости и длительности, и планшет сообщал бы о двух своих сигналах двумя разными звуками |
| Заголовки безопасности вынесены в `src/security-headers.ts`, а `/s/*` исключён из общих заголовков `next.config.ts` | Их ставят двое, и два списка разъехались бы молча именно там, где это дороже всего. Два заголовка политики на одном ответе браузер применяет пересечением — понять, что сработало, стало бы гаданием, поэтому маршрут исключён из общих, а не «перекрыт поверх» |
| Единица числового пункта — отдельное поле модели экрана, а не приклеенная к границам строка; без границ показывается, у нечислового пункта — нет, пустая строка значит «без единицы» | Пункт без границ, но с единицей иначе выдавал бы себя за пункт с диапазоном; «шт» у пересчёта — сведения, а не украшение; без числа единица ничего не измеряет; разделитель перед пустотой — дырка на экране (T242) |
| «Одно заполнение — одна запись» держит правило базы — частичный уникальный индекс на пару «версия + начало» (миграция 0012), — а не транзакция вокруг поиска и записи | Поиск не видит запись, которая ещё не зафиксирована, и транзакция на обычной изоляции этого не меняет: оба запроса и внутри неё слышат «не найдено». Правило, проверяемое в самой вставке, заставляет второй запрос дождаться первого. Поиск перед записью остался быстрым путём обычного повтора (T219) |
| Проигравший гонку получает квитанцию первой записи, а не отказ | Работа сотрудника принята, и сказать ему надо это. Страница ошибки или «нет связи» были бы неправдой и толкали бы отправить ещё раз. Проверено в живом браузере: без этой обработки экран «отправлено» не появляется (T219) |
| Повторы, легшие до правила, помечаются колонкой `submissions.duplicate` и выводятся из-под индекса, а не удаляются | Иначе миграция упала бы на базе, где такие пары уже есть, а удаление — правка истории (принцип 3). Ранней в паре считается отправленная раньше, при равном времени — с меньшим id: правило обязано выбрать ровно одну (T219) |

## Расхождения с экранным эталоном — и почему

Эталон в двух местах противоречит критериям готовности этого же блока. Побеждает критерий, эталон не переписан — решение за владельцем.

| Место | Эталон | Как сделано | Почему |
|---|---|---|---|
| `fill.html`, состояние 2 | Кнопка активна и подписана «Finish · 1 item left» при одном незакрытом пункте | Кнопка неактивна, подпись «1 item left» | Критерий 5: «Кнопка отправки неактивна, пока есть незакрытые пункты, и говорит, сколько их осталось». Состояния 1 и 3 эталона выглядят именно так — расходится только второе |
| Пиктограммы у полей будильника | **Ни один ввод эталона иконок не несёт** | У надписей обоих полей стоит пиктограмма: часы и листок записки | Указание владельца D094, дословно «Сделай пиктограммы , потом посмотрим». Это ОТСТУПЛЕНИЕ от эталона, и живёт оно до показа владельцу: «потом посмотрим» — не утверждение, а отложенный взгляд. Эталон не правлю — его правка или признание отступления не за блок-агентом. Отдельно: внутри самого поля времени своей иконки нет, там остаётся только нативная браузерная — см. журнал T168 |
| Плашка сигнала о просрочке | **Эталон состояния «просрочено» не рисует вовсе**: в `fill.html` пропуск показан только второй строкой обхода («Missed 2 · Next at 14:00») | Плашка над строками панели: красная рамка, число пропущенных и названия пунктов | Задача требует сигнала, а не пометки в строке (T138): пометку, которую никто не заметил, пропускают ровно так же, как сам обход. Цвета и отступы взяты из токенов эталона, но самой поверхности в нём нет — как и панели будильников. Признать это отступлением или дорисовать эталон — не за блок-агентом |
| `states.html`, «Ссылка недействительна» | Текст утверждает замену наклейки: «The sticker has been replaced… the old code was revoked» | Заголовок нейтральный, под ним два прочтения: сперва про опечатку в наборе, затем плашкой про заменённую наклейку | Для кода, которого никогда не было, утверждение эталона — неправда, а сотрудник по нему идёт к управляющему вместо того, чтобы перенабрать (T182). Ответ сервера при этом прежний и один: различать исходы в ОТВЕТЕ значило бы отвечать перебору по-разному (D021), да и в данных они неразличимы — перевыпуск переписывает код станции, прежней строки не остаётся. Эталон не правлю: его правка не за блок-агентом |
| `fill.html`, числовой пункт | Ожидаемые значения написаны в подсказке под названием («Expected 160–180 °C»), а у поля стоит единица и вердикт («°C · within range») | Границы и единица стоят вместе у поля («-22…-10 °C · вне диапазона»), в подсказке под названием их нет | Половина расхождения снята T242: единица появилась в данных (T240) и показана у границ. Осталась вторая половина — где живут сами границы, — и владелец решил её прямо (D110, дословно «эталон в этой части остаётся как есть»): слот подсказки под названием принадлежит методисту (T233), а одна и та же пара чисел в двух местах экрана — то самое «один факт на двух уровнях». Эталон не правлю: его правка не за блок-агентом |
| `states.html`, «Нет сети при отправке» | Отдельный экран во всю высоту с текстом «Your answers are kept on this screen» | Тот же экран заполнения, ответы на месте, плашка и «Отправить ещё раз» в нижней панели | Критерий 7: «ответы остаются на экране». Экран, на котором ответов не видно, но написано, что они здесь, противоречит сам себе |

## Что проверить на площадке про туннель

Продукт публикуется наружу через `tailscale funnel` (D034). Передаёт ли туннель адрес
клиента и в каком виде — **на площадке не проверено**, поэтому предел на открытие экрана
по клиенту по умолчанию не применяется (`TRUSTED_PROXY_HOPS` не задан), и продукт говорит
об этом строкой в журнале при старте. Что сделать после раскатки, по порядку:

| Шаг | Что сделать | Что значит результат |
|---|---|---|
| 1 | Открыть экран заполнения снаружи, через адрес туннеля, и посмотреть, какие заголовки о клиенте доходят до приложения: есть ли `X-Forwarded-For`, одно в нём звено или несколько | Заголовка нет вовсе — предел по клиенту на этой площадке невозможен: оставляем выключенным и не делаем вид, что он есть |
| 2 | Повторить запрос, подставив своё значение в тот же заголовок, и сравнить с шагом 1 | Значение доехало нетронутым — туннель заголовок не переписывает, доверять ему нельзя, `TRUSTED_PROXY_HOPS` остаётся 0. Появилось звено справа от подставленного — туннель дописывает своё, и число посредников можно объявить |
| 3 | Объявив `TRUSTED_PROXY_HOPS` (обычно 1), пересобрать продукт: значения переменных запекаются в сборку (D033) | Без пересборки переменная не действует, и выглядит это как «настроили, а не работает» |
| 4 | Убедиться, что предел не стал рубильником на всю сеть: открыть экран с двух устройств через одну точку выхода в интернет (пиццерия за одним внешним адресом) и проверить, что исчерпание предела одним не запирает второго | Разные устройства сводятся к одному ключу — предел считается по сети, а не по клиенту; тогда его либо поднимают, либо оставляют выключенным |
| 5 | Проверить обратное — что предел не пустышка: исчерпать его с одного устройства и увидеть экран «слишком часто» | Отказ не наступает — ключ меняется на каждый запрос, значит настройка неверна |

Отдельно: пределы на ОТПРАВКУ заполнений от этого не зависят вовсе — они считаются
по коду станции и по сети целиком, и туннель на них не влияет.

## Что осталось владельцу и диспетчеру (T139)

Три вещи в этой задаче не сделаны намеренно — решать их не мне.

| Что | Почему не сделано | Что с этим делать |
|---|---|---|
| **Переключатель блока будильников в редакторе.** Сегодня панель есть на КАЖДОЙ станции: включить или выключить её методист не может | Переключатель живёт в файлах блока `editor` (карточка свойств, `editing.ts`, `validation.ts`) и в `data/types.ts` — ровно в тех, которые прямо сейчас переписывает блок `editor` под T137. Правка там означала бы конфликт слияния в файлах, переписываемых параллельно, то есть ту самую поломку, ради которой схему в волне ведёт один блок | Маленькая задача блоку `editor` в следующей волне: колонка-признак у чек-листа плюс переключатель в карточке свойств. Хранилище будильников от этого не меняется — признак только прячет панель |
| **Панели будильников нет в экранном эталоне** `docs/furca/design/screens/fill.html` | Эталон рисует не блок-агент | Проверяющий оценил поверхность как заметную: вес панели на экране сопоставим с панелью обходов — толстый разделитель, заголовок в верхнем регистре, своя форма. То есть эталон дорисовывать нужно |

## Статус

**Волна 12: T261 закрыта (#129).** Язык публичной поверхности задаёт пиццерия, а не
телефон в кармане сотрудника.

| Что сделано | Чем проверено |
|---|---|
| Цепочка перевёрнута: страна пиццерии → устройство → язык продукта (`locale.ts`). Устройство осталось вторым звеном — оно выбирает текст пункта там, где на языке пиццерии перевода нет | `fill/locale.test.ts` — правило, а не порядок звеньев: «чей это язык». Живой браузер на своём стенде: русская пиццерия с английского телефона пришла по-русски |
| Отбивка «заполнять нечего» говорит языком пиццерии: `no-checklist` несёт язык страны и больше ничего (`station.ts`, `FillScreen.tsx`). Оба пути в неё — нет открытой версии и режим смены срезал все пункты — отвечают одинаково | `fill/station.test.ts`: язык меняется вслед за данными страны, а не стоит константой. Сквозной `e2e/page-lang.spec.ts` на живой станции без чек-листа |
| Запасное звено взято из `DEFAULT_LOCALE` вместо буквы «ru» | `e2e/page-lang.spec.ts`: неизвестный код и страница входа с одного и того же телефона на третьем языке отвечают одним языком — расхождение этих двух ответов и было #129 |

Порча (каждая применилась — проверено grep'ом по файлу, — покраснела и восстановлена из
копии): прежний порядок звеньев — «expected 'en' to be 'ru'» в трёх проверках; запасное
звено обратно в «ru» — «expected 'ru' to be 'en'» в двух; язык отбивки константой вместо
данных страны — «countryLocale: 'en'» против «'ru'» в двух.

**Смоук живым браузером** на стенде блока (3200), не по HTML: читался отрендеренный
экран — `document.documentElement.lang`, язык корня содержимого и видимый заголовок.
Демо-пиццерия английская (D083), затем у её страны временно переключался язык и
возвращался обратно.

| Телефон | Английская пиццерия | Русская пиццерия | Код не работает |
|---|---|---|---|
| английский | en · «Closing tasks» | **ru** · кнопка «осталось 6 пунктов» | en · «This code does not work» |
| русский | **en** · «Closing tasks» | ru | ru · «Этот код не работает» |
| немецкий | en | ru | **en** · «This code does not work» |
| не назвал язык | en | ru | **en** · «This code does not work» |

Жирным — то, что до этой задачи отвечало иначе. Отбивка «заполнять нечего» проверена
там же и идёт языком пиццерии: «Nothing to fill in right now» против «Сейчас заполнять
нечего» на одном и том же английском телефоне.

Заголовок чек-листа в демо остаётся английским и на русском экране — у него нет русского
перевода (D083 убрал русские данные), и `pickFillText` показывает то, что есть: пустая
строка на кухне хуже строки на чужом языке. Интерфейс при этом русский целиком.

Гейт `./scripts/check` зелёный целиком, код возврата 0: 263 сквозных сценария, покрытие
выросло по всем четырём мерам (непокрытых ветвей 323 против 328 в базе, строк 308 против
312), база покрытия не тронута — сдвигает её приёмка.

### Решено внутри блока: чем отвечать, когда пиццерии нет вовсе

D122 этот случай прямо оставляет открытым («язык там выбирать не из чего»), а половина
#129 — именно про него. Выбран язык продукта (`DEFAULT_LOCALE`, сегодня английский), а не
прежний русский. Не по вкусу, а по трём уже записанным вещам:

1. **#129 заведён владельцем и сам называет русский отказ дефектом:** «чек-лист по-английски,
   а отказ — по-русски, то есть на языке, которого он с большой вероятностью не знает, и
   именно в тот момент, когда ему надо понять, что делать дальше».
2. **D083 (владелец, 15.09)** убрал из данных Россию и русскую пиццерию: «у нас весь
   интерфейс сейчас на инглише». Обоснование прежнего русского — «пилот идёт в
   русскоязычной сети» — к этому дню уже не описывало продукт.
3. **`core/locale.ts` объявляет умолчание продукта английским.** Второе умолчание рядом с
   первым — это и есть механизм, которым они разъезжаются молча; поэтому в коде стоит не
   буква, а ссылка на `DEFAULT_LOCALE`.

Вопрос владельцу не заводился: развилки не осталось — все три источника показывают в одну
сторону. Если владелец решит иначе, правка стоит одной строки в `locale.ts`.

---

**Волна 10: T219 закрыта.** Форма, ушедшая на сервер дважды одновременно (ретрай прокси,
второе касание после подвисшего ответа), даёт одну запись, а второй запрос — «отправлено».

| Что сделано | Чем проверено |
|---|---|
| Миграция `0012_submission_repeat_guard` руками (D089): `submissions.duplicate` и частичный уникальный индекс `submissions_one_per_filling_idx`; `schema.ts` объявляет то же | `data/repeat-guard-migration.test.ts` на выброшенной базе: накат поверх тройки повторов и пары с равным временем отправки не меняет записей и помечает ровно поздние; новый повтор отвергается; откат снимает правило. Стенд: демо на 0011 плюс двойник записи → `npm run db:migrate` прошёл, md5 всех 15 записей совпал, двойник помечен |
| `saveOnce` в `repeat.ts`, через него пишет `submit.ts` | `fill/submit.test.ts`: гонка с удержанным окном и пачка из шести одновременных отправок — до починки красные 5 из 5, после зелёные 5 из 5. `fill/repeat.test.ts`: отказ по другому правилу базы и ошибка без кода квитанцией не становятся. Живой браузер 375 px (временный смоук вне репозитория): сервер ждал незафиксированную запись, экран «отправлено», запись одна |

Порча (применилась, покраснела, восстановлена из копии): снятая обработка отказа — «duplicate
key», в браузере нет «отправлено»; неуникальный индекс — «expected 3 to be 1»; повтор «всегда да» — «expected undefined to be '23514'»; снятая пометка
или второй ключ — «could not create unique index». Шесть POST разом по HTTP гонку не
воспроизвели и со снятой обработкой: её проверяет только удержанное окно.

### Вынесено наверх, не решено мной

- **Двойники прошлого в ленте** видны как прежде; скрыть или удалить — правка истории,
  решать владельцу. Сколько их на площадке: `select count(*) from submissions where duplicate`.
- **Единица в строке обхода** есть в эталоне («176 °C»), в продукте нет (перенесено из волны 9).
- **Опечатку в показании после провала не исправить**: поле исчезает (перенесено из волны 8).

### Открыто

- Повтор опознаётся по времени выдачи пропуска, неповторимому ВНУТРИ процесса. Два процесса
  за одной базой приняли бы второе настоящее заполнение за повтор; сегодня процесс один.
- Файл длиннее потолка в 120 строк за счёт контракта и решений; срез отложен (D104, T228).

---

**Волна 15: T270 — язык документа врёт о содержимом.** Публичный экран рисует текст на
языке пиццерии (D122), а `<html lang>` объявлял язык телефона: посредник `src/proxy.ts`
считал его из `Accept-Language`, потому что пиццерию он не знает, а корневая разметка
рисуется раньше страницы и спросить её не может.

### Что выбрано и почему не группа маршрутов

Направление в задаче — своя корневая разметка публичного маршрута через `app/(fill)/`.
Взято другое, и вот отчего:

1. **Группа не убирает причину, а удваивает её.** Своя разметка всё равно считала бы язык
   САМА, отдельно от экрана, — то есть на месте одного расхождения появились бы два места,
   которые обязаны договориться. Единый источник решения нужен в любом случае, а с ним
   вторая разметка уже ничего не добавляет.
2. **Несколько корневых разметок Next разрешает только без `app/layout.tsx`** — значит все
   остальные маршруты (кабинет, `page.tsx`, `error.tsx`, `not-found.tsx`) пришлось бы
   перенести в группу, переписать пути в сторожах и доках блока `core` и проверить
   поведение общего `not-found` под двумя корнями. Это переделка маршрутов продукта ради
   атрибута.
3. **Третье место, рисующее `<html>`, — риск, а не цель.** Его не приходится стеречь, если
   его нет.

### Решение

Одно решение о языке этой поверхности на запрос — `src/blocks/fill/document.ts`, и его
берут ОБА: и документ (корневая разметка), и содержимое (`FillScreen`). Код станции
корневой разметке называет посредник заголовком (он и так стоит на `/s/:path*` ради
одноразового ключа), а в базу за языком страны ходит уже разметка — ей это можно.

### Шаги (отмечать по ходу)

- [x] план записан
- [ ] `fill/document.ts`: `fillLanguage(code)` под `cache()` — предел частоты, язык страны, цепочка
- [ ] `proxy.ts`: вместо готового языка передаёт код станции; чужое значение затирает
- [ ] `app/layout.tsx`: язык документа на публичном маршруте — из `fillLanguage`
- [ ] `FillScreen.tsx`: язык и вердикт предела — оттуда же, своих расчётов не держит
- [ ] сторож в `core/failure-screens.test.ts`: `<html>` рисуют ровно два файла
- [ ] сквозной сценарий: два языка телефона × три состояния экрана
- [ ] гейт `./scripts/check` кодом возврата
