<!-- Описание блока. Контракт здесь — источник истины для блок-агента. -->

# Block: feed

## Назначение

Лента заполнений и карточка одного заполнения: что заполнено, когда, сколько заняло и что пошло не так. Без оценок и цветных ярлыков — только факты.

Над лентой — полоса тревог: то, что требует вмешательства сегодня (D053). Это единственное место блока, где цвет что-то значит.

## API-контракт

```ts
/admin/feed?country=&store=&station=&period=today|week|month
/admin/feed/<id>?country=&store=&station=&period=   // фильтры доезжают до карточки и возвращают в ту же ленту
```
Карточка показывает пункты в той версии, по которой заполняли, — снимком, а не текущим чек-листом.

Период задаётся тремя вариантами эталона, а не парой дат `from`/`to`: границы всё равно считает сервер в часовом поясе пиццерии, и пара дат в адресе позволяла бы вручную задать окно, которого на экране нет (см. решения блока).

## Зависимости

`data`, `auth`

## Definition of Done блока

1. Фильтры по стране, пиццерии, станции и периоду работают вместе.
2. Три показателя за выбранный день считаются по тем же данным, что и лента.
3. Карточка показывает ответ и время по каждому пункту и комментарии к проваленным.
4. После правки и публикации новой версии чек-листа старая карточка не меняется — сквозной тест.
5. Пустое состояние объясняет, что делать дальше.
6. Экраны совпадают с эталонами `feed.html` и `submission.html`.
7. Полоса тревог показывает провал критичного пункта, критичный пункт без ответа и незаполненный чек-лист; при тишине говорит «Тревог нет», а не исчезает.
8. Режим смены виден в ленте и в карточке: сокращённый прогон нельзя спутать с полным (D055).

## Устройство блока

| Файл | Что внутри |
|---|---|
| `src/blocks/feed/routes.ts` | `FEED_PATH`, `submissionPath(id)` — адреса блока в одном месте |
| `src/blocks/feed/view.ts` | Разбор фильтров из адреса и сборка адресов (`parseFeedView`, `feedHref`, `submissionHref`) |
| `src/blocks/feed/zone.ts` | Часовые пояса: смещение, местная дата, начало местных суток |
| `src/blocks/feed/period.ts` | Три периода эталона в границы времени и `relativeDay` (сегодня / вчера / раньше) |
| `src/blocks/feed/selection.ts` | Согласование фильтров между собой и выбор пояса экрана |
| `src/blocks/feed/options.ts` | Свой запрос справочника для выпадающих списков и пояс станции |
| `src/blocks/feed/failures.ts` | Сколько пунктов провалено в каждом заполнении — дочитывание по идентификаторам строк ленты |
| `src/blocks/feed/versions.ts` | Дата публикации версии для оговорки о снимке |
| `src/blocks/feed/outcome.ts` | Итог заполнения одной величиной: провал критичного → провал → без ответа → всё выполнено |
| `src/blocks/feed/metrics.ts` | Три показателя по массиву строк ленты |
| `src/blocks/feed/alarms.ts` | Тревоги: `listAlarms(scope, at)` — провал критичного пункта, критичный пункт без ответа, незаполненный чек-лист. Считаются запросом в момент обращения, таблицы тревог нет (D053). Область видимости — страна, пиццерия и станция; период на полосу не влияет |
| `src/blocks/feed/format.ts`, `text.ts`, `checklist-link.ts` | Длительность «3:24», выбор языка заголовка, ссылка в редактор |
| `src/blocks/feed/model.ts` | Модель обоих экранов: что посчитано для разметки |
| `src/blocks/feed/ui/build-model.ts` | Сборка моделей: фильтры → слой доступа → показатели → снимок |
| `src/blocks/feed/ui/Feed*.tsx`, `OutcomeTag.tsx` | Экран ленты по эталону `feed.html` |
| `src/blocks/feed/ui/AlarmStrip.tsx` | Полоса тревог над лентой: шесть строк, оговорки и тихое состояние |
| `src/blocks/feed/ui/Submission*.tsx`, `AnswersCard.tsx` | Карточка по эталону `submission.html` |
| `src/app/admin/feed/` | Маршруты: лента, карточка, «не найдено» |
| `e2e/feed.spec.ts` | Сквозные сценарии, включая главный: новая версия не меняет старую карточку |

## Решения, принятые внутри блока

| Решение | Почему |
|---|---|
| Три показателя считаются функцией `computeMetrics` по тому же массиву строк, который показан в ленте, а не отдельным запросом с агрегатами | Требование T045 буквально. Два запроса с независимой фильтрацией разъезжаются на первой же правке, и управляющий видит «14 заполнений» над лентой из тринадцати — причём без единой ошибки в коде, просто от расхождения условий |
| Строка ленты различает провал критичного пункта, провал обычного и пункт без ответа; «Всё выполнено» показывается, только когда провалов нет вовсе | `listSubmissions` отдаёт лишь `failedCriticalCount`, и заполнение с непройденным НЕкритичным пунктом получило бы зелёную метку «Всё выполнено». Это ложь на главном экране управляющего — хуже отсутствия метки |
| Число проваленных пунктов дочитывается вторым узким запросом по идентификаторам уже отобранных строк (`failures.ts`), а не собственной копией запроса ленты | Копия запроса означала бы второй набор условий отбора — ровно то, чего требует не допускать T045. Здесь набор строк по-прежнему целиком за слоем доступа: дочитывание идёт по первичному ключу и правилом провала берёт `isFailed` из `data`, то есть лента и карточка считают провал одинаково |
| Своими запросами читается и справочник для фильтров (`options.ts`), и дата публикации версии (`versions.ts`) | D024: блоки, которым нужны свои запросы, берут из `data` схему и `getDb()` и пишут их у себя. Заказывать в `data` списки стран и пиццерий значило бы собирать там CRUD всех блоков |
| Период — три варианта эталона (сегодня / 7 дней / месяц), а не произвольная пара дат | Эталон рисует именно список, а не выбор дат; «месяц» — последние 30 суток, а не календарный месяц, иначе третьего числа управляющий видит почти пустой экран |
| Границы суток считаются в часовом поясе пиццерии, а время каждой строки показывается в поясе ЕЁ пиццерии | Сотрудник видел на кухне местное время, и управляющий обязан видеть то же самое. В сети из нескольких стран (эталон рисует Казахстан и Узбекистан) пояс сервера превратил бы утренний чек-лист соседней страны в ночной |
| Когда пиццерия не выбрана, а пояса у пиццерий фильтра разные, период считается по поясу площадки — и экран подписывает, по какому именно | Одного правильного ответа здесь не существует, а молчаливое допущение о том, что такое «сегодня», — худший вариант: он невидим. Подпись под фильтрами делает допущение явным |
| Выбранная станция задаёт пояс экрана, даже если пиццерия в фильтре осталась «Все» | Станция однозначно принадлежит одной пиццерии: считать её сутки по поясу площадки значило бы игнорировать уже сделанный выбор |
| Пиццерия чужой страны и станция чужой пиццерии из фильтра выпадают (`resolveSelection`) | Иначе несогласованные фильтры складываются в заведомо пустую ленту, и экран показывает «ничего не заполняли» там, где заполнений просто не может быть по построению |
| Пустое состояние различает три причины: нет станций, не заполняли ни разу по этим фильтрам, не заполняли в этом периоде | Критерий готовности требует объяснить, что делать дальше, а совет невпопад («напечатайте QR-коды» там, где надо расширить период) хуже отсутствия совета. Второй запрос ради этого уходит ТОЛЬКО при пустой ленте и берёт одну строку |
| Предел выдачи ленты (200) задан в блоке явно, и экран говорит, когда упёрся в него | Унаследованное умолчание слоя доступа не позволяет отличить «столько и было» от «дальше обрезано»: часть истории молча выглядела бы всей историей |
| Фильтры — обычная GET-форма, применяемая по выбору из клиентского компонента, с кнопкой отправки в `<noscript>` | На эталоне кнопки «Показать» нет, а состояние экрана обязано жить в адресе (ссылкой на «Кухню за неделю» делятся в чате). Без JavaScript экран остаётся рабочим |
| Тревоги вычисляются запросом в момент обращения: ни таблицы, ни фоновой задачи, ни «закрытия дня» | D053. Тревога-состояние не может застрять после того, как чек-лист заполнили, и нет колонки, которую пришлось бы чинить руками после каждого сбоя. Цена — счёт при каждом открытии экрана; выборка узкая и по индексам |
| Критичный пункт БЕЗ ответа — отдельная тревога, а не часть провала | Неполное заполнение продукт принимает сознательно (`matchAnswersToSnapshot`), поэтому «выключить газ» можно просто не тронуть: провала нет — ответа нет, пропуска нет — заполнение есть. Без этой тревоги самый важный пункт продукта уходил бы из надзора тише обычного. Складывать провал и молчание в одно число нельзя: за «2 критичных не закрыто» стоят два разных разговора |
| Пропущенным считается проход окна, закончившийся СЕГОДНЯ по местному времени, а не начавшийся | У окна через полночь (20:00–00:00) проход, начатый сегодня, кончается завтра: по началу вечернее закрытие не порождало бы тревоги никогда — а именно оно и есть самое важное |
| Режим смены для тревоги берётся за сутки, в которых окно НАЧАЛОСЬ | Чек-лист ждали от смены, которая его открыла, а не от той, что пришла после полуночи (D055) |
| Полоса тревог не зависит от выбранного периода | Тревога — про сейчас. «За сегодня» прятало бы вечернее закрытие, закрывшееся в полночь, а «за месяц» приносило бы месяц старых тревог |
| Выборки тревог переводят время по имени зоны из `pg_timezone_names`, а не по `stores.timezone` напрямую | Имя, которого база не знает, роняет запрос целиком. Для запроса по одной станции это отказ одной пиццерии (с этим продукт живёт с T062), для запроса по всей сети — отказ экрана управляющего из-за одной строки справочника. С присоединённым именем такая пиццерия выпадает из условий и попадает в счёт `unknownTimezoneStores`, который полоса показывает вслух: тихо вычесть пиццерию из надзора нельзя |
| Тревоги считаются своим запросом, а не по уже загруженным строкам ленты | У ленты свой период и предел выдачи 200. Тревога, пропавшая из-за выбранного периода, — ровно та тихая потеря, ради которой тревоги и заводились |
| Полоса показывает шесть строк, остальные объявляет числом; при тишине печатает «Тревог нет» | Полоса из сорока строк перестаёт быть тревогой и становится фоном. А отсутствие полосы неотличимо от неработающей полосы: управляющий обязан видеть разницу между «тревог нет» и «тревоги не показываются» |
| Метка итога — один компонент `OutcomeTag` на оба экрана | Лента и карточка обязаны называть один и тот же итог одними словами; две копии таблицы соответствий разъезжаются на первой правке словаря |
| Каркас админки (`AdminNav`, `AdminShell`) снова скопирован в блок | Границы модулей запрещают ленте зависеть от `catalog`, `editor` и `qr`, а общего места для разметки каркаса в проекте нет. Та же цена уже заплачена блоками `editor` и `qr`. **Цена оказалась выше заявленной:** вместе с разметкой скопировался и список разделов, четыре копии разъехались, и готовые разделы месяц показывались надписью «Раздел ещё не готов» (#11). Адреса и готовность с 07.09.2026 живут одним фактом в `src/blocks/core/admin-sections.ts`, разметка по-прежнему копия; связность держит `e2e/admin-nav.spec.ts` |
| Списки фильтра сами объявляют `data-live`, когда применение выбора включилось | Разметку списков отдаёт сервер, а обработчик выбора появляется только после гидратации. Между этими моментами список выглядит рабочим, принимает выбор и не делает ничего — снаружи два состояния неразличимы, а разница между ними целая навигация. Сквозной сценарий из-за этого мигал (T106), и никакое ожидание «снаружи» эту разницу не видит: адрес, разметка и варианты в списке к тому моменту уже правильные. Атрибут ставится эффектом, то есть после коммита гидратации, поэтому его появление и есть доказательство, что обработчик на месте. Это та же порода фактов о разметке, что `data-testid` и `data-kind` рядом, и он же договаривает вторую половину прогрессивного улучшения — первую договаривает кнопка в `<noscript>` |

## Журнал работы

| Задача | Кем | Что сделано |
|---|---|---|
| T044 | сам | Фильтры страны, пиццерии, станции и периода: разбор адреса (`view.ts`), согласование выбора между собой (`selection.ts`), периоды в границы времени по часовому поясу (`zone.ts`, `period.ts`), справочник для списков (`options.ts`). Логика фильтров — не механическая работа: здесь решается, что считать «сегодня» у сети в разных поясах, поэтому делал сам. **Отрицательный прогон:** фильтр по станции молча потерян (`const stationId = null`) — упало 10 проверок, включая «страна, пиццерия, станция и период работают вместе» |
| T045 | сам | Три показателя (`metrics.ts`) считаются по массиву строк, который экран и показывает; предел выдачи задан явно, экран говорит, когда упёрся в него. **Отрицательный прогон:** показатели переведены на отдельный запрос без периода — упали обе проверки (`expected 8 to be 2`, `expected { submissionCount: 11 } to strictly equal { submissionCount: 0 }`) |
| T046 | сам | Карточка: пункты из снимка заполнения, ответ, время и комментарий по каждому, дата публикации версии (`versions.ts`), пункт без ответа отличается от выполненного. Заодно закрыт разрыв контракта: `listSubmissions` отдаёт только критичные провалы, и заполнение с непройденным НЕкритичным пунктом получало бы метку «Всё выполнено» — добавлено дочитывание провалов по идентификаторам строк (`failures.ts`) с правилом провала из `data`. **Отрицательный прогон:** ветка «провал без критичных» убрана из `outcomeOf` — упали три проверки (`expected { kind: 'ok' } to strictly equal { kind: 'failed', count: 1 }`) |
| T047 | сам | Сквозной сценарий `e2e/feed.spec.ts`: правка и публикация новой версии не меняют сохранённую карточку. **Здесь же найден молчаливый гейт — свой собственный.** Первая редакция сценария публиковала новую версию (вставка строки) и проходила ЗЕЛЁНОЙ на нарочно испорченном коде, где карточка читает пункты из версии, а не из снимка: заполнение ссылается на прежнюю версию, и её содержимое никто не трогал. То есть сценарий проверял только неизменяемость версий — первую опору D002 — и молчал о второй. Добавлен шаг «переписать саму версию, по которой заполняли»: на той же порче сценарий упал (`Expected -12 / Received +3` — вместо трёх исходных пунктов карточка показала один новый), после восстановления снова зелёный |
| T048 (лента) | сам | Экран ленты по эталону `feed.html`: каркас, фильтры (GET-форма, применяется по выбору, с кнопкой в `<noscript>`), три показателя, таблица, пустое состояние трёх видов. Делал сам вынужденно: запуск исполнителей был отклонён сторожем лимита подписки (см. строку о волне ниже) |
| T048 (карточка) | исполнитель | Карточка по эталону `submission.html`: четыре факта, оговорка о снимке, секции и строки ответов с метками, значениями, временем и комментариями (`SubmissionScreen.tsx`, `SubmissionFacts.tsx`, `AnswersCard.tsx`, `SubmissionNotFound.tsx`). Готовый контракт (модель, ключи словаря, разбиение на файлы, тестовые идентификаторы), механическая работа, миграций и доступа не касается — ровно тот случай, когда задача обязана уходить исполнителю |
| Приёмка работы исполнителя | сам | Проверял руками, а не по отчёту: перечитал все четыре файла, прогнал типы, линт, мёртвый код, границы модулей, настоящую сборку `next build` и открыл экраны в браузере рядом с эталоном. Найдено и исправлено мной: (1) пункт показывал ДВА пояснения подряд — посчитанный диапазон и подсказку методиста, которая его же и повторяет («160–180 160–180 °C»), тогда как на эталоне пояснение одно; теперь подсказка побеждает, диапазон остаётся запасным вариантом; (2) на экране «заполнение не найдено» один и тот же заголовок стоял и в шапке, и в карточке — в шапке теперь раздел. Остальное принято как есть: разбиение, стиль и все ветки модели отрисованы |
| Сверка с эталоном в живом браузере | сам | Снимки эталонов и живых экранов сняты рядом (`reports/feed-block/*.png`, вне git). Расхождения, найденные глазами и исправленные: список станций показывал несколько одинаковых «Кухня» — без выбранной пиццерии станция теперь названа путём (та же причина, что в D032); подпись показателя не согласовывалась с числом («4 заполнений» вместо «4 заполнения») — переведена на множественные формы; строка о часовом поясе показывалась всегда, хотя на эталоне её нет, — теперь только когда пояс действительно неоднозначен |
| Волна и лимит подписки | сам | Оба исполнителя (лента и карточка) были запущены одновременно и **отклонены сторожем лимита**: «окно seven_day израсходовано на 75%… в волне уже 2». Повтор через десять минут пропустил одного — им сделана карточка; лента написана мной. Записано сюда по требованию сторожа: если стройка оборвётся по лимиту, причина должна остаться в файле состояния, а не только в исчезнувшем уведомлении |
| Режимы смены и тревоги (09.09.2026) | сам | Режим смены доведён до ленты и карточки (метка сокращённой смены, пункты, которых в этом режиме не спрашивали). Тревоги: `alarms.ts` + `AlarmStrip.tsx`, 22 проверки на настоящей базе и сквозной сценарий в браузере. **Найдено по ходу, не по плану:** (1) `itemCount` строки ленты считался по всему снимку, и сокращённая смена показывала «не отвечено 2» про пункты, которых у сотрудника не спрашивали; (2) критичный пункт, оставленный БЕЗ ответа, не ловила ни одна тревога — неполное заполнение продукт принимает сознательно, и самый важный пункт уходил из надзора тише обычного; (3) выборка по всей сети падала целиком из-за одной пиццерии с непризнаваемым поясом — переведена на `pg_timezone_names` со счётом выпавших пиццерий |
| T106 (мигающий сценарий фильтра) | сам — разбор и правка; исполнитель — отрицательный прогон | **Причина установлена прицельным зондом, а не рассуждением и не общей нагрузкой.** Мигал не отдельный тест, а общий помощник `pick` в `e2e/feed.spec.ts`: он объявлял переход состоявшимся по смене адреса. Смена адреса наступает при commit нового документа — разметку сервер уже отдал, а скрипты страницы ещё едут. Список фильтра в этом окне выглядит совершенно рабочим (он в DOM, с правильными вариантами, принимает выбор) и НЕ ДЕЛАЕТ НИЧЕГО: обработчик выбора появляется только после гидратации, до неё событие `change` уходит в пустоту. Следующий `pick` ждёт смены адреса, которой уже не будет никогда, и падает по таймауту — ровно «ожидание смены адреса после выбора фильтра» из формулировки задачи. **Замер зондом (11.09.2026)** с искусственной задержкой раздачи `/_next/static/**` на 3 с: первый `pick` возвращался за 184 мс в состоянии `readyState=loading`, `stationInDom=true`, `stationHydrated=false` — ровно в эту дыру, — и второй `pick` падал по таймауту. Без задержки то же окно закрывается за 1 мс: поэтому отдельный прогон был зелёным всегда, а в общем (один сервер отвечает сотне сценариев из нескольких воркеров) окно растягивается — отсюда «через раз». Тот же класс, что T086. **Чинилась причина, а не симптом: ни один таймаут не увеличен.** `pick` получил постусловие «переход доехал»: списки приехавшей страницы объявили себя живыми. Говорить это научился сам продукт — `FeedFilterSelects` ставит на каждый список `data-live="true"` эффектом, то есть после коммита гидратации, когда обработчик выбора заведомо на месте (тот же род фактов о разметке, что `data-testid` и `data-kind` рядом; заодно это честно описывает вторую половину прогрессивного улучшения — первую описывает кнопка в `<noscript>`). Оба ожидания в `pick` обязательны и не заменяют друг друга: смена адреса доказывает, что переход вообще начался (без неё проверка «списки живые» прошла бы, не сходя со СТАРОЙ страницы — она-то давно живая), `data-live` доказывает, что жива именно приехавшая. Контрольный опыт: под одной и той же задержкой в 3 с старый `pick` падает по таймауту, новый проходит. Отрицательный прогон — таблицей ниже |
| T118 (дубль адреса раздела) | сам | Четыре строки-копии адресов разделов убраны: `FEED_PATH` (`routes.ts`), `CATALOG_PATH` и `QR_PATH` (`ui/FeedEmpty.tsx`), `CHECKLISTS_PATH` (`checklist-link.ts`) — все читают `core/admin-sections`. Задача называла одну строку (QR), но дубль был один класс на четыре места, и починка трёх из четырёх оставила бы ровно ту же мину. Делал сам: четыре правки и один файл проверки — запуск исполнителя стоит дороже самой работы (условие «больше пятидесяти строк однотипного кода» не выполнено). **Проверка — структурный сторож, а не равенство строк:** `routes.test.ts` читает исходники блока (`src/blocks/feed`, `src/app/admin/feed`) и ищет в них любой адрес из `ADMIN_SECTIONS`. Равенство `FEED_PATH === ADMIN_SECTIONS.feed.path` копию НЕ видит — прописанная руками строка даёт тот же результат, пока раздел не переедет; это подтвердил отрицательный прогон ниже. |
| Отрицательный прогон T118 | сам | Обе копии возвращены на место разными способами: `FEED_PATH = "/admin/feed"` в кавычках и `QR_PATH = `/admin/qr`` в шаблонной строке (именно так дубль вернулся в блоке редактора — T115). Прогон `npx --no-install vitest run src/blocks/feed/routes.test.ts`: **3 прошло, 1 упало**, сторож назвал оба файла (`expected [ 'src/blocks/feed/routes.ts', 'src/blocks/feed/ui/FeedEmpty.tsx' ] to deeply equal []`). Тест на равенство при этом остался ЗЕЛЁНЫМ — он и есть та проверка, которой было бы недостаточно. Файлы восстановлены из копий, снятых до порчи (не `git checkout`), прогон снова 4 из 4. |
| T126 (полоса тревог и фильтр станции) | сам | **Решено по эталону, а не по удобству: полоса ОБЯЗАНА сужаться станцией** (дизайн режимов и тревог, «Полоса на экране»: период на полосу не влияет — D053, а страна, пиццерия и станция влияют). Продукт это и делал: `scopeOf` кладёт `stationId` в область видимости, `alarms.ts` ставит условие по станции, и модульная проверка `alarms.test.ts` его держит. Дефект был не в продукте и не в сценарии, а **в данных прогона**: у пиццерии тревожила ровно одна станция, поэтому потеря station-фильтра ничего не меняла и сценарий оставался зелёным. Соседняя станция (касса) получила собственную тревогу — её версия несёт критичный пункт, и он провален. Заведены две проверки: модульная в `ui/build-model.test.ts` (адрес разбирается тем же `parseFeedView`, что и в маршруте, — потеря фильтра по дороге от адреса до запроса видна) и сквозная в `e2e/feed.spec.ts`. Исполнителя не звал: две правки в двух файлах, и обе переплетены с отрицательным прогоном — свой же прогон ломал бы работу исполнителя. Период на полосу проверкой НЕ закреплён и это сказано вслух: `listAlarms(scope, at)` вовсе не принимает период, сузить полосу им можно только сменив подпись функции — это ловит проверка типов, а не тест; честного отрицательного прогона на такое поведение не существует |
| T114 (три формулировки тревог) | сам | **Различать визуально запрещено (D063, «хватит текста») — значит проверять надо обратное: что различает текст.** Формулировка осталась единственной опорой, а под проверкой её не было вовсе. Хуже: полоса зовёт словарь СОБРАННЫМ ключом (`t(row.kind, …)`), и общий сторож словаря (`core/messages-keys.test.ts`) разбирает только статические ключи — оба ключа провалов ему не видны, их пропажа прошла бы мимо гейта. Мапка «вид тревоги → ключ и подстановки» вынесена из разметки в чистую `alarm-text.ts`, рядом `alarm-text.test.ts`: 20 проверок на обоих языках — ключ нарисован словами, а не сам собой; три вида различимы при 1, 2 и 5 (русские множественные формы); ни одна формулировка не начало другой; незаполненный чек-лист называет время, провалы — число. Эталон `feed.html` полосы не знает; расхождение закрыто **записью** в `docs/furca/design/map.md` (Q009 отвечен D063, перерисовывать нечего, полоса — не лишний блок). Исполнителя не звал: один новый файл на тридцать строк и две правки в разметке — запуск дороже работы |
| Отрицательный прогон T114 | сам | Две порчи разной природы, обе в общем словаре, обе восстановлены копией (не `git checkout`). (1) `ru.criticalUnanswered` укорочен до «критичный пункт» — строки остались РАЗНЫМИ, проверка на различие прошла бы, а полоса читалась бы как два провала: упала проверка на приставку, `expected [ 'критичный пункт не выполнен' ] to deeply equal []`. (2) Строка `en.criticalUnanswered` удалена целиком: **упало 2 из 20**, `expected 'feed.alarms.criticalUnanswered' not to contain 'criticalUnanswered'` — тот самый тихий отказ next-intl, на котором остальные 18 проверок и весь общий сторож молчат. После восстановления `git diff src/messages` пуст, прогон снова 20 из 20 |

### Отрицательный прогон T106: класс порч, а не одна

Урок блока auth: проверка, подтверждённая единственной порчей, ловит только её. Поэтому
порчи взяты классом — две ломают сам продукт, одна ломает доставку выбора в адрес, одна
ломает опору самой починки, и одна воспроизводит исходную (доправочную) проверку.

Команда прогона каждой порчи: `npx --no-install playwright test e2e/feed.spec.ts` (7 сценариев).

| Порча | Что именно сломано | Прогон | Поймана? | Первая строка отказа |
|---|---|---|---|---|
| `requestSubmit()` убран из `applyOnChange` | продукт: выбор в списке не применяется вовсе | 3 прошло, 4 упало | да | `Test timeout of 30000ms exceeded` |
| `setLive(true)` убран из эффекта | опора самой починки: признак «список ожил» не появляется никогда | 3 прошло, 4 упало | да | текст постусловия `pick`: «Фильтры приехавшей страницы так и не ожили…» |
| `name` снят со списка станции | продукт: выбор не доезжает до адреса — форма уходит без поля | 3 прошло, 4 упало | да | `Test timeout of 30000ms exceeded` |
| `view.ts`: разобранный `stationId` выбрасывается | продукт: фильтр станции молча теряется (порча класса T044) | 4 прошло, 3 упало | да | `expect(locator).toHaveCount(expected) failed`, `expect(locator).toBeVisible() failed` |
| `pick` ждёт только смены адреса (код ДО правки) | сама проверка | под задержкой скриптов 3 с — упал; без задержки — прошёл | под нагрузкой да, без неё нет | `page.waitForFunction: Test timeout exceeded` |

Две вещи, которые видно только из таблицы и которые стоит помнить:

- **Последняя строка — причина, по которой одного зелёного прогона мало.** Исходная
  проверка ловит свою же поломку ТОЛЬКО под задержкой; без неё она зелёная и молчит.
  Поэтому доказательством здесь служит прицельный зонд, а не прогон.
- **Полоса тревог не держала фильтр станции — и это оказалось дырой в данных прогона,
  а не в продукте.** На порче `view.ts` сценарий тревог оставался зелёным. Причина
  разобрана в T126 (ниже): у пиццерии тревожила ровно одна станция, и потеря
  station-фильтра не меняла ни одной строки полосы. Вывод «фильтр держат три других
  сценария» был верен только про ленту: сама полоса не держалась ничем.

### Отрицательный прогон T126: полоса тревог и фильтр станции

Полоса обязана сужаться выбранной станцией (дизайн режимов и тревог, «Полоса на экране»).
Проверок на это не было ни одной: `alarms.test.ts` проверяет запрос, которому область
видимости передали руками, а доставку выбора от адреса до запроса не проверял никто.

Порчи взяты классом по всей цепочке «адрес → выбор → область видимости → запрос».
Команды: `npx --no-install vitest run src/blocks/feed/ui/build-model.test.ts -t "полоса тревог"`
и `npx --no-install playwright test e2e/feed.spec.ts` (8 сценариев).

| Порча | Что именно сломано | Модульная | Сквозная | Первая строка отказа |
|---|---|---|---|---|
| `view.ts`: разобранный `stationId` выбрасывается | адрес: фильтр теряется на разборе (порча класса T044/T106) | упала | 5 из 8 упали | `expected [ …(2) ] to have a length of 1 but got 2` |
| `build-model.ts`: станция снята с области видимости ТОЛЬКО у тревог | «полосу чинят»: лента сужается, полоса нет | упала | 2 из 8 упали (обе про полосу, лента зелёная) | `toHaveCount(expected) failed · Expected: 1 · Received: 2` |
| `alarms.ts`: из `scopeConditions` убрано условие по станции | запрос тревог перестаёт знать про станцию | упала | — | `expected [ …(2) ] to have a length of 1 but got 2` |

Что из этой таблицы стоит помнить:

- **Вторая строка — та, ради которой задача и заводилась.** Порча сужает ленту и не
  сужает полосу: экран выглядит рабочим, управляющий видит чужие тревоги под своим
  фильтром. До T126 такую правку не ловило ничего.
- **Зелёный сценарий подводит не текстом, а данными.** Сценарий полосы был написан
  верно; молчал он потому, что в пиццерии прогона тревожила одна-единственная станция.
  Проверка «сузилось до одного» ничего не стоит, пока до сужения их не было двух.

## Статус

done
