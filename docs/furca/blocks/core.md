<!-- Описание блока. Контракт здесь — источник истины для блок-агента. -->

# Block: core

## Назначение

Каркас проекта: приложение Next.js, база в Docker Compose, все проверки качества и единая команда прогона. Ничего продуктового здесь нет — этот блок делает так, чтобы остальные девять строились на общем основании и не спорили о стиле.

## API-контракт

Экспортирует не код, а окружение:

- `docker-compose.yml` — PostgreSQL 17 на порту 5433, том для данных.
- `.env.example` с именами переменных: `DATABASE_URL`, `PORT`, `ADMIN_PASSWORD_HASH`, `SESSION_SECRET`.
- `scripts/check` — исполняемый, запускает формат, линт, типы, тесты с покрытием, мёртвый код, границы; пишет `reports/*.junit.xml`.
- `.githooks/pre-push`, включается `git config core.hooksPath .githooks`; падает, когда инструмента нет.
- `scripts/up` — два режима и один список шагов (T194). `./scripts/up` готовит стенд и
  запускает сервер (foreground, Ctrl-C); `./scripts/up --no-start` доводит стенд до
  той же готовности (база, миграции, демонстрационный контур, сборка) и выходит кодом 0 —
  это то, что зовёт всякий неинтерактивный прогон: CI, приёмка блока, сквозной смок.
  Неизвестный параметр — отказ до любого действия. Сторож — `up-script.test.ts`.
- Приложение поднимается на порту из настроек копии (`PORT` в окружении, затем в
  настройках, затем умолчание 3100) — одинаково для `npm run dev`, `npm run start`
  и `./scripts/up`; источник один, `src/blocks/core/app-port.ts` (T163). Тот же источник
  спрашивают сквозной смоук и заведение пакета чек-листов (T190).
- Сетка каркаса кабинета — `core/ui/admin-frame.ts` (T193): `ADMIN_FRAME_CLASS` (корень) и
  `ADMIN_CONTENT_CLASS` (область содержимого). Сверху складки (`--page-fold`, 768 px) меню —
  боковая колонка 208 px, ниже — верхняя полоса, содержимому достаётся вся ширина.
  **Экран, рисующий каркас сам** (сегодня такой один — редактор), берёт класс отсюда, а не
  пишет литерал; сторож — `admin-frame.test.ts`.
- **Экраны отказа — общие и заводятся ЗДЕСЬ**, а не каждым блоком заново:
  `src/app/error.tsx` (не собрался экран публичной стороны), `src/app/global-error.tsx`
  (не собралась сама разметка), `src/app/not-found.tsx` (адреса нет, публичная сторона),
  `src/app/admin/error.tsx` (не собрался экран кабинета) и `src/app/admin/not-found.tsx`
  (адреса нет в кабинете). Оба кабинетных стоят внутри каркаса и охраны.
- **Вид состояния выбирает поверхность, а не блок** (T213-T215):
  - сотрудническая (телефон, публичные пути) — `core/ui/StateScreen.tsx`: полный экран,
    белый фон от края, заголовок `--fs-display`, одно акцентное действие во всю ширину
    (`STATE_ACTION_CLASS`, не шире 280 px), плашка `warn` или `err`, мелкая строка кода
    ошибки. Его же берут состояния экрана заполнения («отправлено», «код недействителен»);
  - кабинетная — `core/ui/StatusCard.tsx` внутри `AdminShell`: карточка с рамкой и тенью
    на сером канвасе, заголовок `--fs-title`, акцентное действие (`STATUS_ACTION_CLASS`),
    плашка отказа цвета `--err`.

  Перепутать их снова мешает сторож `failure-screens.test.ts`. Отказ окрашен отдельно от
  пустого состояния: «сломалось» и «здесь пока пусто» — разные новости.
- Клиентской стороне словарь отдаётся разделами, а не целиком (иначе 28 КБ на каждую
  страницу, включая кухонный телефон): корневая разметка отдаёт `failure`, разметка
  кабинета — `failure` и `admin` (их просит граница ошибки кабинета, рисующая меню).
- Язык документа равен языку содержимого: корневая разметка объявляет язык запроса, а
  `core/ui/HtmlLangSync.tsx` подтягивает его к языку, который экран объявил на своём корне
  (`<div lang=…>` у экрана заполнения). Экран, говорящий не на языке запроса, обязан
  объявить свой язык на КОРНЕ своего содержимого — иначе документ останется с чужим.
- **Тема — атрибут `data-theme` на `<html>`, и больше ничего** (T236, D106). Значения живут
  единственным экземпляром в эталоне (`[data-theme="dark"]`, D013), продукт их не копирует.
  Атрибут ставят двое: корневая разметка из куки `meridius_theme` (явный выбор — страница
  приезжает уже в нужной теме) и инлайновый скрипт первым в `<body>` из
  `prefers-color-scheme`, когда выбора нет (на публичном маршруте — с одноразовым ключом
  строгой политики). Механизм — `core/theme.ts`, переключатель на три положения —
  `core/ui/ThemeToggle.tsx` в меню кабинета, выбор доносит `ThemeProvider.tsx`,
  `color-scheme` — `globals.css` (полосу прокрутки и нутро полей токены не красят вовсе).
  Сторожа — в `design-reference.test.ts`: экран не смеет взять токен без тёмного значения и
  писать цвет мимо токенов (кроме бумаги — листа QR-наклеек); читают исходники, а не
  собранный CSS: проигравший в каскаде цвет не виден, но ошибкой быть не перестаёт.
## Зависимости

—

## Definition of Done блока

1. `docker compose up -d` поднимает базу, `psql "$DATABASE_URL" -c 'select 1'` отвечает.
2. `npm run dev` отдаёт страницу на порту копии (умолчание 3100), и `npm run start` — на нём же.
3. `scripts/check` проходит на пустом проекте и пишет отчёт в `reports/`.
4. Каждая из шести проверок **проверена на испорченной копии**: неформатированный файл, нарушение правила линта, ошибка типов, падающий тест, неиспользуемый экспорт, импорт мимо графа — каждая падает и печатает внятную причину.
5. `.npmrc` содержит `min-release-age=7`.
6. Смоук Tailwind 4 с токеном из `@theme inline` на реальной странице — цвет применился.
7. Смоук next-intl: язык страницы меняется по заголовку браузера без cookie.
8. Хук `pre-push` останавливает пуш при красной проверке (проверено намеренной поломкой).

## Осознанные ограничения — проверено, а не забыто

- **Язык последнего рубежа (`global-error.tsx`) — только английский, и это плата.** Язык
  страницы вычисляет корневая разметка, которая в этом сценарии и упала; а сказать то же
  по-русски значит тянуть словарь целиком на каждую страницу продукта. Обычная граница
  ошибки — та, которую человек и увидит, — переведена полностью.
- **`global-error.tsx` живьём не проверялся вовсе:** чтобы он сработал, должна упасть сама
  корневая разметка, а подстроить это без порчи кода нечем. Проверена только его форма.
- **Экран отказа при недоступной базе прогоном не проверяется** — только живым запуском с
  остановленным контейнером базы. Сквозной набор идёт в пять потоков по одной базе: её
  падение уронило бы всё остальное по чужой причине. Сторож проверяет форму файлов
  (клиентская директива, вызов `reset`, отсутствие `error.message`), а не то, что видно.
- **Язык документа без JavaScript** остаётся языком запроса.
- **Автоматика темы без JavaScript не работает** — документ остаётся светлым. Плата за
  единственный экземпляр значений: медиазапросом их включить можно только переписав всё
  вторым блоком, а копия разошлась бы молча. Явный выбор работает и без скриптов.
- **Переключатель стоит только в кабинете:** на кухонном экране эталон такого органа не
  рисует, а сценарий сотрудника одноразовый. Нужен ли он там — вопрос владельцу, не блоку.

## Состояние

**Где стою.** Волна 18.09.2026, восьмая: T236 закрыта — тёмная тема по всем экранам сразу,
автоматикой и переключателем. Ветка `feat/core-w8`; сквозные сценарии писал `optio`.

**Чем проверено.**

- `theme.test.ts` исполняет сам скрипт довключения подделками `document` и `matchMedia`.
- Порчей покраснели: скрипт без проверки уже стоящего атрибута; кука по вхождению имени;
  снятое тёмное `--ink` (сторож назвал поимённо экраны, которые взяли бы светлое);
  `bg-white` и жёсткий хекс в вёрстке. Каждая порча подтверждена `grep`-ом до прогона,
  восстановление — из копии в каталоге-черновике, не через git.
- Живой браузер: кухонный экран под строгой политикой темнеет сам (`--canvas` `#12161C`,
  `--ink-3` `#8A97A6` — значение D044), консоль чиста; переключатель меняет тему без
  перезагрузки, выбор переживает переход, «Авто» стирает куку. Снимки светлой и тёмной
  версий теперь РАЗНЫЕ — это и был дефект T236.

**Что открыто.**

- Экрана с переключателем у эталона нет: вид — решение блока, а не выписка.
- Сторож цвета был пойман собственной порчей: жёсткую тень в произвольном значении Tailwind
  пропускал молча (подчёркивание — словесный символ). Тот же класс возможен в соседях.
