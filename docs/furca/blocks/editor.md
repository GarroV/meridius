<!-- Описание блока. Контракт здесь — источник истины для блок-агента. -->

# Block: editor

## Назначение

Редактор чек-листов — главный экран продукта. Задача блока не «дать возможность создать чек-лист», а сделать так, чтобы чек-лист из двадцати пунктов заводился за минуты.

## API-контракт

```ts
createChecklist({ stationId, title, window }); getDraft(checklistId)
saveDraft(checklistId, sections: Section[])
publish(checklistId): Promise<ChecklistVersion>
duplicateChecklist(checklistId, { toStationId? }): Promise<string>
removeChecklist(checklistId): Promise<'deleted' | 'archived'>   // стереть или снять с работы — по наличию заполнений
previewRemoval(checklistId): Promise<RemovalPreview>            // что произойдёт, для экрана подтверждения
parsePastedList(text: string): Item[]      // строки → пункты, пустые строки и маркеры списка отброшены
```
Формат `sections` — единственный контракт с блоком `fill`, описан в плане.

## Зависимости

`data`, `auth`

## Definition of Done блока

1. Enter в поле пункта создаёт следующий и ставит в него курсор; Alt+↑/↓ переставляют пункт.
2. Вставка 20 строк из буфера превращается в 20 пунктов за одно действие; маркеры списка и нумерация отброшены, пустые строки пропущены.
3. Тип ответа по умолчанию «да/нет»; число принимает границы диапазона; уровень пункта переключается по месту тремя положениями — обычный, важный, критичный (D056).
4. «Сохранить черновик» и «Опубликовать» — разные действия: публикация создаёт версию, черновик остаётся черновиком.
5. Дублирование чек-листа копирует секции и пункты, но не историю заполнений.
6. Предпросмотр открывает тот же экран, что видит сотрудник, с данными черновика.
7. Экран совпадает с эталоном `docs/furca/design/screens/editor.html`.

## Устройство блока

| Файл | Что внутри |
|---|---|
| `src/blocks/editor/validation.ts` | Разбор всего, что приходит из браузера: `parseSections`, `parseWindow`, `isUuid`, `EditorInputError` с кодом отказа, пределы разметки |
| `src/blocks/editor/drafts.ts` | `createChecklist`, `updateChecklist`, `saveDraft`, `loadEditor` — черновик и состояние экрана |
| `src/blocks/editor/publish.ts` | `publish` — освежение вставленных блоков и вызов `publishVersion` из `data` |
| `src/blocks/editor/duplicate.ts` | `duplicateChecklist` — копия чек-листа, в том числе на другую станцию |
| `src/blocks/editor/paste.ts` | `parsePastedLines`, `parsePastedList` — разбор списка из буфера |
| `src/blocks/editor/editing.ts` | Правка разметки в браузере: Enter, Alt+стрелки, вставка, типы, секции, блоки библиотеки |
| `src/blocks/editor/library-links.ts` | Вставленные блоки: развернуть ссылки живыми пунктами, счётчик использования |
| `src/blocks/editor/listing.ts` | `listChecklists` (сужённый фильтром), `listStations` — списки для экранов |
| `src/blocks/editor/filter.ts` | Фильтр списка чек-листов в адресе: разбор параметров, сборка адреса |
| `src/blocks/editor/filter-options.ts` | Справочник фильтра из станций, согласование выбора, крошка пути |
| `src/blocks/editor/uuid.ts` | `isUuid` без единого импорта: проверку зовут и с сервера, и из разметки, уезжающей в браузер |
| `src/blocks/editor/removal.ts` | Удаление чек-листа: стереть целиком или снять с работы, сохранив историю |
| `src/blocks/editor/actions.ts`, `action-state.ts` | Серверные действия (`requireAdmin` внутри каждого) и состояние формы |
| `src/blocks/editor/routes.ts` | Адреса экранов редактора и адрес вставленного блока в библиотеке (`libraryBlockPath`); путь самого раздела берётся из `core/admin-sections` |
| `src/blocks/editor/dead-controls.test.ts` | Сторож на класс дефекта: намертво выключенный элемент управления в разметке редактора (постоянный `disabled`, литерал `aria-disabled="true"`) |
| `src/blocks/editor/ui/` | Экраны: каркас админки, список чек-листов, заведение, сам редактор, предпросмотр, подтверждение удаления |
| `src/blocks/editor/ui/PreviewScreen.tsx` | Предпросмотр с переключателем режима смены: методист видит чек-лист глазами обычной, сокращённой и критичной смены |
| `src/app/admin/checklists/**` | Страницы Next под охраной `src/app/admin/layout.tsx` |
| `src/blocks/editor/ui/ChecklistFilters.tsx`, `ChecklistFilterSelects.tsx` | Карточка фильтров над списком: GET-форма и три списка, применяющиеся в момент выбора |
| `src/blocks/editor/ui/use-live.tsx` | `useLive()` — признак «экран ожил, обработчики на месте». Им ставится `data-live` в списках фильтра, на кнопке «Вставить блок», на форме правки блока библиотеки (`library` вправе импортировать `editor` по графу) и на кнопке «Завести чек-лист» |
| `src/blocks/editor/window-field.ts` | Окно смены на проводе: имя поля `window`, кодирование `«06:00\|11:00»` (`windowFieldValue` / `parseWindowField`) и три готовых окна, общих для формы заведения и карточки свойств. Заведён в T129 вместе с починкой: до него окна были двумя одинаковыми списками в двух файлах |
| `e2e/editor.spec.ts`, `e2e/checklist-remove.spec.ts`, `e2e/checklists-filter.spec.ts`, `e2e/database.ts` | Сквозные сценарии (правка, удаление, фильтры) и своя база прогона |

## Решения, принятые внутри блока

| Решение | Почему |
|---|---|
| Опубликованная версия хранит секцию блока библиотеки как `source: { blockId }` с развёрнутыми пунктами | Снимок — это пункты (D002), а происхождение секции нужно библиотеке: без него `listUsages` (T030) не сможет сказать, сколько опубликованных версий затронет правка блока |
| Публикация освежает ссылки в черновике и только потом зовёт `publishVersion` | Обратный порядок означал бы правку уже созданной версии, а версии неизменяемы. Черновик — единственная мутируемая строка в этой цепочке |
| Пункт без названия пропускается разбором, а не роняет сохранение | Пустая строка внизу списка — обычное состояние редактора: методист нажал Enter и ещё не напечатал текст |
| Пустой чек-лист не публикуется (`nothingToPublish`) | Опубликованная пустая версия — это открытый по QR экран без единого пункта, и понять это можно только на кухне |
| Окно «без ограничения» записывается как `00:00–24:00` | Равные границы запрещены базой (T057), а `24:00` — законное значение `time` в PostgreSQL, делающее условие выбора версии истинным в любую минуту |
| Экран объявляет `data-live`, когда ожил, а форма правки блока — ещё и `data-block-id` | Разметку отдаёт сервер, и до гидратации (а при переходе внутри приложения — пока переход не доехал) экран выглядит рабочим, принимает ввод и НИЧЕГО не делает: обработчика ещё нет. Снаружи два состояния неразличимы, а разница между ними — целое действие методиста. Хуже того, у экрана библиотеки форма прежнего блока неотличима от формы нового: `data-testid` у них один. Оба признака ставятся из состояния, то есть после коммита, поэтому их появление и есть доказательство. Это тот же род фактов о разметке, что `data-testid` рядом, и тот же приём, которым лента закрыла T106 |
| Окно выбирается из трёх готовых вариантов, а не вводится временем | Так в эталоне, и это меньше касаний: у чек-листа станции по сути три окна. Уже записанное нестандартное окно показывается отдельным пунктом списка, чтобы открытие чек-листа не переписало его молча |
| Окно едет на сервер ОДНИМ полем `window`, и на заведении его отправляет сам список (`name`), а не состояние экрана (T129) | Копия выбора в состоянии отстаёт от самого выбора: до гидратации состояния ещё нет, и выбор, сделанный в первую секунду, в него не попадает — форма отправляет то, чего на экране никто не видел. Лечится это не подстраховкой, а снятием копии: у именованного списка расходиться не с чем, и работает он даже без единого скрипта. На экране ПРАВКИ список стоит вне обеих форм (кнопки «Сохранить» и «Опубликовать» — две отдельные формы в шапке), поэтому там окно по-прежнему отправляет состояние; общим осталось хотя бы поле, чтобы два вида одного свойства не разошлись молча |
| Alt+стрелки переставляют пункт только внутри секции | Пункт, уехавший в соседнюю секцию от одного нажатия, для методиста исчезает: он смотрит на своё место в списке. Требование блока — «переставляют пункт», и оно выполнено без этого риска |
| Вставка в пустой пункт занимает его место | Иначе после вставки списка в только что созданную Enter'ом строку в чек-листе остаётся дырка |
| Тексты пунктов пишутся на языке интерфейса, второй язык не затирается | У методиста нет переключателя языка контента (его нет и в эталоне), а терять уже введённый второй язык нельзя |
| Каркас админки (левое меню) продублирован из блока `catalog`, а не переиспользован | Границы модулей запрещают `editor` зависеть от `catalog` (`.dependency-cruiser.cjs`), а общего места для каркаса в продукте пока нет. Разметка скопирована один в один; правильное место для него — блок `core`, это вынесено наверх диспетчеру |
| ~~Разделы, которых в продукте ещё нет (библиотека, заполнения, QR), показаны неактивными надписями~~ **Отменено (T115)** | Решение было верным ровно до появления этих разделов — и молча пережило их на месяцы. Неактивный пункт человеку неотличим от сломанного: он видит серое и решает, что дело в его правах. Теперь все пункты редактора ведут туда, куда обещают; появится новый неготовый раздел — вернётся и механика, и сторож на неё (`dead-controls.test.ts`) заставит объяснить её явно |
| «Открыть блок» у вставленной секции ведёт на сам блок (`?block=<опознаватель>`), а не в список библиотеки | В библиотеке блоков десятки: «ведёт в раздел» на деле значит «ищи сам». Адрес собирает `editor/routes.ts`, а не общий помощник с блоком `library`: границы модулей запрещают редактору зависеть от библиотеки — зависимость идёт в обратную сторону. Общий здесь ровно один факт, путь раздела, и он взят из `core/admin-sections`. Что имя параметра у двух блоков совпадает, держит не модульный тест (он упал бы на границах модулей), а сквозной сценарий: он открывает блок и читает его название — тот же приём, что у справочника с разделом QR (T107) |
| Футер предпросмотра — показ, а не выключенная кнопка | Разбор ниже, в журнале T115: кнопка, которая не оживёт никогда, читается как сломанная, а на экране заполнения та же кнопка законна, потому что сотрудник её оживит |
| У сквозных сценариев своя база `dodo_qr_e2e_<метка копии>` | Рабочую снесла бы подготовка схемы, тестовую посреди прогона пересоздаёт vitest, а экраны редактора читают и пишут настоящие данные |
| Тексты редактора уходят в браузер провайдером `next-intl` | Клиентская часть большая и с числительными («4 пункта», «используется ещё в 6 чек-листах»); пробросить готовые строки нельзя — количество меняется прямо на экране |
| Состояние фильтра списка живёт в адресе, а не в памяти компонента | Тот же приём, что у ленты (`feed/view.ts`): ссылкой на «Кухню Алматы» делятся в чате, и она обязана открыться тем же списком. Заодно экран остаётся серверным и работает без JavaScript кнопкой из `<noscript>` |
| Справочник фильтра выводится из `listStations()`, а не из трёх новых запросов | Запросов не прибавилось, а в списках оказываются только страны и пиццерии, где есть хотя бы одна станция, — то есть только те, где чек-лист вообще может жить. У ленты справочник свой (три запроса), потому что она фильтрует заполнения, а не привязку к станции |
| Выбор станции достраивает пиццерию и страну, а выбор пиццерии — страну | Выбрав «Кухню Алматы», методист выбрал и Алматы: списки обязаны это показать, иначе экран одной станции подписан «Все страны». Побочно чинит вёрстку: без выбранной пиццерии станции названы путём («Алматы, Абая 44 · Кухня»), и выбранное значение не влезало в поле, обрезаясь посреди слова |
| Пустой результат — отдельное состояние, а не «чек-листов пока нет» | Второе звало бы завести новый чек-лист там, где выбрана не та станция, — и в сети появился бы дубль уже существующего. На эталоне этого состояния нет вовсе, поэтому собрано по `.empty` из дизайн-системы |
| Пункт «Все» есть и в списке «Страна», хотя на эталоне его там нет | Без него состояние «фильтр не выбран» невыразимо, а именно оно открывается по умолчанию. Отступление от эталона намеренное, сверка его подтвердила |
| `isUuid` вынесен в файл без импортов | Списки фильтра клиентские, и через `validation.ts` в браузерный бандл затягивался драйвер базы — сборка падала. Поймал только настоящий `next build`: ни типы, ни тесты этого не видят |
| «Удалить» ведёт на отдельный экран подтверждения, а не удаляет сразу | Последствие зависит от истории и его надо назвать заранее: чек-лист без заполнений стирается целиком, а чек-лист, по которому уже заполняли, только уходит из работы (`archived_at`, миграция 0005) — заполнения хранят снимок пунктов, которые видел сотрудник (принцип 3, D002). «Удалить» и «убрать из работы» слишком разные вещи, чтобы выясняться после нажатия. Заодно экран работает без JavaScript, как остальные формы, — всплывающее «вы уверены?» без JS не работает вовсе |

## Журнал работы

| Задача | Кем | Что сделано |
|---|---|---|
| T019 | сам | Черновик чек-листа: `createChecklist` заводит его сразу с черновиком (пустая секция и пустой пункт — курсор попадает в поле, а не в пустой экран), `saveDraft` пишет разметку вставкой с разрешением конфликта по частичному индексу «один черновик», `updateChecklist` ведёт название, станцию и окно, `loadEditor` отдаёт экрану всё за один заход. Здесь же разбор входа на границе (`validation.ts`) и списки для экранов. Ядро блока и работа с базой делегированию не подлежат |
| T020 | сам | Клавиатура: Enter создаёт следующий пункт сразу за текущим и уводит в него курсор, Alt+↑/↓ переставляют пункт внутри секции и оставляют курсор на нём. Правила вынесены в чистые функции (`editing.ts`, 20 тестов), связка с настоящими событиями браузера — в сквозном сценарии. Главное обещание блока, поэтому сам |
| T021 | исполнитель | Разбор вставленного из буфера списка (`paste.ts`): чистая функция с готовым контрактом и перечнем случаев, без базы — ровно тот случай, когда задача обязана уходить исполнителю. Сдал 18 тестов зелёными, с красным прогоном до кода и проверкой порчей |
| T021 — приёмка | сам | Разобрал код исполнителя и прогнал свою пробу на краях: дробное число в начале строки (`1.5 кг муки`), строка Excel из нескольких колонок, чекбокс Markdown без маркера списка. Все три прошли; случаи добавлены в набор постоянно (21 тест) |
| T022 | сам | Типы ответов и критичность по месту: «да/нет» по умолчанию, число с границами, текст. Смена типа с числа снимает границы — невидимое значение не должно уезжать в базу. Строка пункта по эталону, сохранение проверено сквозным сценарием |
| T023 | сам | Публикация: `publish` освежает вставленные блоки библиотеки в черновике и зовёт `publishVersion` из `data`. Ключевое правило продукта (D002) делегированию не подлежит. **Отрицательный прогон:** публикация без освежения ссылок роняет тест снимка (`expected [ { id: 'item-санитария', …(3) } ] to strictly equal [ { id: 'item-санитария-2', …(3) } ]`), файл восстановлен копией |
| T024 | исполнитель | Дублирование чек-листа (`duplicate.ts`) по готовому контракту и образцу `drafts.ts`: механическая работа без миграций и без правки чужих блоков. Сдал 19 тестов зелёными, с красным прогоном и порчей (5 падений на двух внесённых дефектах) |
| T024 — приёмка | сам | Разобрал код и написал свой сквозной тест на связку с публикацией: копия чек-листа, у которого есть и черновик, и опубликованная версия с заполнениями, берёт черновик, заполнений не наследует и публикуется на свою станцию (`duplicate-publish.test.ts`) |
| T025 | исполнитель | Предпросмотр «как это увидит сотрудник» по эталону `fill.html` из данных черновика. **Временный:** сохранения ответов нет, настоящий экран заполнения принесёт блок `fill`; на самом экране об этом сказано плашкой. Исполнитель нашёл и починил свой же дефект: стартовый пустой пункт показывался пустой строкой вместо «в черновике пока нет ни одного пункта» |
| T026 | сам + исполнитель | Экран редактора по эталону — сам (состояние, клавиатура, секции, правая колонка); список чек-листов, экран заведения и каркас админки — исполнитель по эталонам `templates.html` и `editor.html`: разметка по готовому эталону с готовыми данными и текстами механическая |
| T026 — сверка с эталоном | сам | Экран открыт в живом браузере рядом с эталоном (безголовый Chrome по отладочному протоколу, снимки обоих). Сверка нашла то, чего не видели ни типы, ни тесты: время публикации приходило из базы строкой и такой же попадало в правую колонку (на регрессию есть тест); название блока библиотеки бралось «первым ключом» JSONB и в русском интерфейсе показывалось по-английски; не хватало «Свернуть», «Открыть блок», ссылки «Вся» и кнопки «Вставить блок из библиотеки»; у списков не было стрелки эталона; счётчик пунктов торчал на экране лишней цифрой |
| Приёмка работы исполнителей | сам | Каркас админки оказался скопирован дважды внутри блока (мой `AdminNav` и меню внутри `AdminShell` исполнителя) — сведён в один компонент: две копии одного меню разъехались бы на первой же правке. Клавиатура проверена мной отдельно от сквозных сценариев: безголовый Chrome, `Input.dispatchKeyEvent` и настоящий буфер обмена (`commands: ["paste"]`) |
| Проверка порчей сквозных сценариев | сам | Обработка Enter отключена одной правкой — падают 2 сценария клавиатуры с внятной причиной (`expect(locator).toHaveCount(expected) failed, unexpected value "1"`), восстановление копией возвращает 6 зелёных |
| T075 | сам | Фильтр списка чек-листов: разбор адреса (`filter.ts`), справочник и согласование выбора (`filter-options.ts`), сужение в самом запросе (`listing.ts`). Ядро блока и работа с базой делегированию не подлежат. Приём взят у ленты, а не выдуман заново: состояние в адресе, GET-форма, списки сужают друг друга. **Отрицательный прогон:** сужение отключено одной правкой — падают 5 тестов списка (`expected [ …(2) ] to strictly equal []`), восстановление копией возвращает 13 зелёных |
| T075 — вёрстка фильтров | исполнитель | Карточка фильтров по эталону `templates.html` и готовому образцу `feed/ui/FeedFilters.tsx`: контракт пропов, ключи текстов и разметка эталона были готовы — ровно тот случай, когда задача уходит исполнителю. Сдал с прогоном формата, типов и линта; сам предложил проп ширины поля, потому что на эталоне у трёх полей разные ширины (180/220/160), а у ленты одна |
| T075 — приёмка исполнителя | сам | Разобрал код: сокращений против образца нет, `id`/`htmlFor` связаны (иначе axe уронил бы прогон), стрелка списка взята общая, а не продублирована. Своя проверка порчей на связке с браузером: автоприменение выбора отключено — падают 4 сквозных сценария из 6 (`page.waitForURL: Test timeout of 30000ms exceeded`), а два, которые ходят по прямому адресу, остаются зелёными, как и должны |
| T075 — сверка с эталоном | сам | Экран поднят живьём и сверен с `templates.html` в четырёх состояниях (без фильтра, страна, станция, пустой результат). Нашлось то, чего не видели ни типы, ни тесты: крошка над заголовком стояла постоянной надписью «Все страны», хотя на эталоне там выбранная страна; подпись пустого результата говорила про станцию даже при сужении по стране; поле «Станция» раздувалось шире «Пиццерии» из-за длинных подписей с путём. Первые две починены прямо, третья — не ограничением ширины, а достраиванием пиццерии по станции: поле пришло ровно к ширине эталона (160 px, замерено) |
| Покрытие | сам | Серверные действия не покрывались вовсе: файл с `"use server"` нельзя разобрать по частям. Разбор формы и выбор кода отказа вынесены в `action-input.ts` и покрыты тестами — путь отказа единственное, что методист видит вместо своей работы |
| T115 | сам | «Открыть блок» у вставленной секции стал ссылкой на сам блок: `linkedBlockId` (`editing.ts`) отдаёт опознаватель и заодно сужает тип, `libraryBlockPath` (`routes.ts`) собирает адрес от `ADMIN_SECTIONS.library.path`, разметка ведёт `Link`, а не якорем (D046). Прежний `isLinked` убран: он отвечал «да/нет», не сужая тип, и после появления второго ответа стал мёртвым — knip такое не прощает. Заодно `CHECKLISTS_PATH` перестал быть строкой и берётся оттуда же: это тот же дубль, что T116 чинит в блоке `qr`, но в моём файле, и без него новый сторож на дубль был бы сторожем с исключением |
| T115 — решение по футеру предпросмотра | сам | Имитация, а не недоделка, и оставлена имитацией — но перестала быть элементом управления. На экране заполнения `<button disabled>` законна: сотрудник ответит на пункты, и она оживёт. В предпросмотре оживить её нечем и никогда не будет: отвечает сотрудник со своего телефона, открыв чек-лист по QR-коду станции, и ответ принадлежит смене, а не черновику. Кнопка, которая не оживёт, читается как сломанная — ровно та же цена, что у серого пункта. Поэтому футер остался тем же прямоугольником эталона (та же приглушённость `opacity:.45`), но разметкой стал `<div>`: ни фокуса, ни курсора «нажми меня», ни «выключено» для чтения вслух. Текст виден и читается — методист обязан видеть то же, что увидит сотрудник. Заодно исправлена плашка сверху: она обещала, что «сохранение ответов появится вместе с блоком заполнения», хотя блок построен, — теперь говорит, что это показ, и называет, где отвечают на самом деле |
| T115 — сторож на класс | исполнитель | `dead-controls.test.ts`: структурная проверка по образцу `core/admin-links.test.ts` — в разметке редактора нет ни постоянного `disabled`, ни литерала `aria-disabled="true"`. Механическая работа по готовому контракту (что считать нарушением, что законно, какой охват), поэтому исполнителю. Сдал с прогоном и с доказательством, что сторож не слепой: разбор строки проверен на настоящих строках-образцах — ловит оба вида, не ловит `disabled={saving}`, `disabled={inserted}` и класс `disabled:cursor-not-allowed` |
| T115 — приёмка исполнителя | сам | Разобрал код и проверил ссылки на документы, которые он приводит (T107 в `catalog.md`, признание блока `library` про нетронутый `SectionCard.tsx` в `library.md`) — обе на месте, не выдуманы. Нашёл у него настоящую находку по ходу: приём `withoutComments`, скопированный из `admin-links.test.ts`, выбрасывает строки-комментарии целиком и сдвигает нумерацию — у образца это незаметно, потому что он номер строки не печатает, а у сторожа номер в сообщении, и он называл `PreviewScreen.tsx:237` вместо `252`. Исполнитель поймал это сам и починил бланкованием строк; в `admin-links.test.ts` правка не нужна, и он туда не полез — правильно. От себя убрал из комментариев номера строк живых файлов: они протухают на первой же правке, оставлены пометки «как было до T115» |
| T115 — отрицательный прогон | сам | Классом порч, а не одной: восемь порч, таблица ниже. Поймали не все проверки и не всё — и это главное, что даёт таблица |
| T121 (мигающий сценарий вставки блока) | сам — разбор и правка; исполнитель — отрицательный прогон | **Причина установлена зондом, а не рассуждением, и оказалась не той, на которую я шёл.** Мигал не сценарий, а помощник `createLibraryBlock` в `e2e/editor.spec.ts`: он объявлял блок заведённым, хотя блок заводился БЕЗ названия, и падал потом `insertBlockByTitle`, которому нечего было искать. Ожидание «форма правки видна» здесь не значит ничего: экран библиотеки показывает правку выбранного блока ВСЕГДА, и пока переход к новому блоку едет, на экране стоит форма прежнего — с тем же `data-testid`. **Замер зондом (11.09.2026):** в момент «форма видна» `readyState=complete`, поле гидратировано, а в поле стоит название ПРОШЛОГО блока; после `fill` — напечатанное; через несколько секунд — снова «Новый блок», и в библиотеке лежит блок с этим именем («название доехало: НЕТ»). **Разведённый опыт 2×2** (задержка раздачи скриптов × библиотека пуста/не пуста) поправил меня же: дело не в нагрузке. Название теряется в трёх случаях из четырёх, в том числе БЕЗ всякой задержки — достаточно, чтобы в библиотеке уже лежал другой блок: тогда экран показывает его правку, и печатать есть куда мимо. Уцелел единственный случай — пустая библиотека и никакой задержки. Отсюда «в одиночку зелёный, в наборе красный»: в файле `editor.spec.ts` блок библиотеки заводит только этот сценарий, и на свежей базе библиотека к его началу пуста, а в полном наборе соседние сценарии наполняют её параллельно. Падение было закономерным, случайным был состав набора. **Вторым зондом найдена вторая дыра того же класса**, которую я сначала принял за единственную: на обычной загрузке документа `fill` до гидратации саму гидратацию переживает (React 19 не затирает напечатанное), но первый же перерисов — его вызывает следующее нажатие «+ Пункт» — возвращает в поле значение из состояния React, куда напечатанное не попало. Итог тот же. Тот же класс, что T086 и T106. **Чинилась причина: ни один таймаут не увеличен.** Помощник получил постусловие из двух половин, ни одна не заменяет вторую: смена адреса доказывает, что переход начался, а `data-block-id` + `data-live` — что на экране форма ИМЕННО нового блока и она ожила. Контрольный опыт: под той же задержкой в 3 с старый помощник заводит блок без названия, новый доводит название до базы. Отрицательный прогон — таблицей ниже |
| T121 — фильтр списка чек-листов | сам | Второй пункт той же задачи, взят на упреждение, а не по миганию. `ChecklistFilterSelects` устроен ровно так же, как был устроен фильтр ленты до T106 (`requestSubmit()` в `onChange`), а сценарии ждали только смену адреса. Не мигало случайно: в каждом тесте ровно один выбор и сразу после перехода, а переход уже дождался загрузки. Первый же тест с двумя выборами подряд получил бы ровно то, что получила лента. Все выборы переведены на помощник `pick`, который спрашивает продукт, ожил ли фильтр, — и только потом выбирает |
| T121 — волна и лимит подписки | сам | На запуске исполнителя сторож сообщил: окно `seven_day` израсходовано на **81%**, в волне 1 (этот исполнитель, отрицательный прогон). Записано сюда по требованию сторожа: если стройка оборвётся по лимиту, причина должна остаться в файле состояния, а не только в исчезнувшем уведомлении. Правка закоммичена ДО запуска исполнителя — обрыв стоил бы отрицательного прогона, а не всей задачи |
| T129 (окно смены уезжало не тем, что на экране) | сам — зонд и правка; исполнитель — отрицательный прогон | **Начал с воспроизведения, а не с описания, и зонд сказал больше, чем задача.** Три опыта на стенде копии (11.09.2026, все до единой правки). **Зонд A, медленная раздача скриптов:** окно выбрано в первую секунду — на экране «вечер», в скрытом поле `06:00`, и ПОСЛЕ гидратации на экране по-прежнему «вечер», а в поле по-прежнему `06:00`; в базе у заведённого чек-листа окно `06:00–11:00`, редактор после перехода показывает утро. То есть React выбор не откатывает — он его просто не видит, и расхождение живёт, пока методист не нажмёт «Завести». **Зонд B, скриптов нет вовсе:** форма отправляется обычным способом браузера и уводит в редактор — то есть заведение работает без JavaScript, — и окно так же приезжает утренним. Это и решило способ лечения: раз без скриптов форма живая, честнее убрать копию выбора, чем подстраховывать её подхватом разметки при монтировании (приём #7). Теперь окно едет одним полем `window`, и отправляет его сам список своим `name`; пара `windowStart`/`windowEnd` исчезла с обоих экранов, готовые окна переехали в `window-field.ts` из двух одинаковых списков. **Красная проверка написана до правки и покраснела на настоящем поведении:** `expect(locator).toHaveValue(expected) failed — unexpected value "06:00\|11:00"`. Сценариев три: без скриптов вовсе (отправляет браузер), с медленной раздачей и ожиданием `data-live` (отправляет React) и контрольный — нетронутое окно остаётся утренним, иначе «всегда вечер» прошло бы обе проверки. Модульные: новый `window-field.test.ts` и две проверки в `action-input.test.ts` — пустое поле даёт пустые границы, а не тихое утро |
| T129 — волна и лимит подписки | сам | На запуске исполнителя сторож сообщил: окно `seven_day` израсходовано на **84%** (на T121 было 81%), в волне 1 — этот исполнитель на отрицательном прогоне; параллельно идёт блок `library`. Правка и проверки закоммичены ДО запуска исполнителя: обрыв по лимиту стоит отрицательного прогона, а не всей задачи |
| T128 (третья копия приёма снятия комментариев) | сам | Одна правка в одном файле — запуск исполнителя и чтение его отчёта стоят дороже самой работы. Локальная копия `withoutComments` из `dead-controls.test.ts` убрана, сторож берёт общий `core/source-text`. **Проверено порчей, а не чтением:** если комментарии не снимать, сторож ловит собственное объяснение в охраняемой разметке — `ui/PreviewScreen.tsx:20 (disabled): * (T115). Раньше футер был настоящей <button disabled>`, 1 падение из 6; на помощнике 6 из 6 зелёные. Порча снята копией, сделанной до порчи, а не через git |
| T127 (охват сторожа мёртвых элементов) | сам | Тот же файл, что в T128, плюс решения по ходу (что считать законным исключением) — исполнителю отдавать нечего. Охват `src/blocks/editor/ui` → `src`: 70 файлов вместо 15, включая публичные экраны заполнения, где серая кнопка хуже, чем в кабинете. Препятствие, из-за которого охват был узким, снял T117 **по месту**, а не исключением: страна пиццерии стала обычным текстом (`store-country-value`), потому что поле только для чтения — не элемент управления. Отсюда правило, записанное в файле: списка исключений у сторожа нет — он держал бы законное и недоделанное под одной строкой. **Красное до зелёного:** новая проверка охвата с якорями из четырёх блоков и из маршрутов на прежнем корне даёт 2 падения из 7 (`expected 15 to be greater than 40`, `expected […(15)] to include 'src/blocks/catalog/ui/StoreCard.tsx'`), на новом — 7 из 7. **Охват настоящий, а не на словах:** `disabled` без выражения, поставленный порчей в `catalog/ui/StoreCard.tsx`, поймался с номером строки (`StoreCard.tsx:89 (disabled): disabled`) — прежний сторож не видел его вовсе. Чужой файл восстановлен копией сразу после прогона, в коммит не попал |

### Отрицательный прогон T115: порча → поймана/пропущена

Первая редакция сторожа на дубль пути смотрела на `"/admin/…` — то есть на кавычку. **Порча П4 прошла молча**: путь вернулся в файл внутри шаблонной строки, где вместо кавычки обратная. Сторож остался зелёным, и на одной порче («перепиши путь строкой в кавычках») он выглядел бы доказанным. Исправлено — теперь путь ищется где угодно в коде со снятыми комментариями; в таблице показан результат после правки.

| Порча | Модульные | Типы | Сквозные | Итог |
|---|---|---|---|---|
| П1. Откат: `Link` → прежний `<span aria-disabled="true">` | `dead-controls`: `expected [ Array(1) ] to deeply equal []` | — | `toHaveAttribute` href: `Received: ""` | поймана дважды |
| П2. Адрес без `?block=` — ведёт в раздел, а не на блок | `routes`: 2 падения, `expected '/admin/library' to be '/admin/library?block=3f2504e0…'` | — | href: `Received: "/admin/library"` | поймана дважды |
| П3. В адрес уехал `section.id` вместо `blockId` | **пропустили** (`routes.ts` не тронут) | — | href: ждал один опознаватель, получил другой | поймана только сквозным |
| П4. Путь раздела прописан строкой (в шаблоне) | `routes`: `expected [ '/admin/library' ] to deeply equal []` | — | **пропустили** (адрес получается тот же) | поймана только модульным |
| П5. `Link` → обычный `<a href>` | `admin-links` (T088): `expected [ Array(1) ] to deeply equal []` | — | метка после перехода: `Expected "жив", Received undefined` | поймана дважды |
| П6. Ветки секции перепутаны (`===` → `!==`) | пропустили | `TS2345: Argument of type 'null' is not assignable to parameter of type 'string'` | — | поймана типами |
| П7. Футер предпросмотра снова `<button disabled>` | `dead-controls`: `expected [ Array(1) ] to deeply equal []` | — | кнопок в предпросмотре: `Expected 0, Received 1` | поймана дважды |
| П8. Опознаватель без `encodeURIComponent` | `routes`: `expected '…?block=a b&mode=critical' to be '…?block=a%20b%26mode%3Dcritical'` | — | **пропустили** (опознаватели в прогоне — uuid, кодировать нечего) | поймана только модульным |

Три порчи из восьми ловит ровно одна проверка, и все три разные: П3 — только браузер, П4 и П8 — только модульные. Набор из одних сквозных сценариев пропустил бы возвращение дубля пути, набор из одних модульных — ссылку на чужой блок.

### Находки по ходу T121, которые я НЕ чинил

Все три — не про T121, и потому оставлены с доказательством, а не починены молча.

**1. Окно смены, выбранное в первую секунду, уходит в базу неверным — молча.**
`src/blocks/editor/ui/NewChecklistForm.tsx` (мой файл). Список окна не имеет `name`:
на сервер уезжают скрытые поля `windowStart`/`windowEnd`, посчитанные из состояния
React. Выбор, сделанный до того как экран ожил, в состояние не попадает. **Измерено
зондом:** на экране выбран «вечер», в скрытом поле стоит `06:00`, у заведённого
чек-листа окно `06:00|11:00` — методист видит одно, сохраняется другое. Это ровно тот
класс, от которого `ChecklistEditor` уже защищён (эффект подхвата разметки при
монтировании, #7), а форма заведения — нет. Цена: чек-лист откроется по QR не в ту
смену, и понять это можно только на кухне. Правка в T121 не входит — нужна задача.

**2. Тот же класс в помощнике блока `library`.** `e2e/library.spec.ts:33` (`createBlock`)
устроен так же, как мой до правки: ждёт `block-editor` и `?block=`, но не спрашивает,
ЧЕЙ это редактор и ожил ли он. От полного повторения T121 его спасает случайность —
проверка `block-no-items`, которая на экране прежнего блока обычно не проходит, потому
что у того блока пункты есть. Сегодня зелен; файл чужой, не трогал.

**3. `useLive` теперь в двух блоках, а у ленты — своя копия.**
`FeedFilterSelects.tsx` объявляет `data-live` собственным хуком; я вынес такой же в
`editor/ui/use-live.tsx` и им же пользуется `library`. Третья копия того же пятистрочного
хука живёт в `feed`. Свести их можно только через `core`, а это чужие файлы и блок `feed`
работает параллельно — решение за диспетчером.

### Отрицательный прогон T121: порча → поймана/пропущена

Урок собственной прошлой задачи: первая редакция сторожа T115 оказалась ложно зелёной,
и поймала её только порча, поставленная сбоку от ожидаемой. Поэтому порчи взяты классом —
три ломают опоры самой починки, четыре ломают продукт, одна воспроизводит помощника до
правки. Прогон каждой порчи делал исполнитель, последовательно (порт и база у копии одни).

Команды: `npx --no-install playwright test e2e/editor.spec.ts` (8 сценариев) и
`… e2e/checklists-filter.spec.ts` (6 сценариев).

| Порча | Что именно сломано | Прогон | Поймана? | Первая строка отказа |
|---|---|---|---|---|
| `data-live` снят с формы правки блока | опора починки: признак «экран ожил» не появляется | 7 прошло, 1 упало | да | текст постусловия: «Правка нового блока так и не ожила…» |
| `data-block-id` снят с формы правки блока | вторая опора: экран перестаёт называть, чей он | 7 прошло, 1 упало | да | тот же текст постусловия |
| `onChange` названия блока — пустой | продукт: напечатанное не доходит до состояния, блок сохраняется безымянным | 7 прошло, 1 упало | да | `Test timeout of 30000ms exceeded` — вставлять нечего |
| `data-live` снят с кнопки «Вставить блок» | опора починки в редакторе | 7 прошло, 1 упало | да | «Редактор чек-листа так и не ожил…» |
| Кнопка «Вставить блок» ничего не открывает | продукт: панель под секциями не появляется | 7 прошло, 1 упало | да | «Панель библиотеки под секциями не открылась…» |
| `requestSubmit()` убран из `applyOnChange` | продукт: выбор в фильтре не применяется вовсе | 2 прошло, 4 упало | да | `Test timeout of 30000ms exceeded` (`waitForURL`) |
| `data-live` снят со списков фильтра | опора починки фильтра | 2 прошло, 4 упало | да | «Фильтры приехавшей страницы так и не ожили…» |
| `filter.ts`: разобранный `stationId` выбрасывается | продукт: фильтр станции молча теряется (порча класса T044) | 3 прошло, 3 упало | да | `expect(locator).toHaveCount(expected) failed` |
| Помощник ДО правки, один сценарий, свежая база | сама проверка | 1 прошёл, 0 упало | **нет** | — |

Две строки, которые стоит помнить, и обе про то, что одного прогона мало:

- **Последняя строка — не промах исполнителя, а факт про исходную проверку.** Старый
  помощник в одиночном прогоне на свежей базе проходит, потому что библиотека к началу
  сценария пуста — а это единственный случай из четырёх, в котором он и был исправен
  (опыт 2×2 выше). Доказательством здесь служит зонд, а не прогон: прогон этой поломки
  не видит ровно в тех условиях, в которых его удобнее всего запускать. Тот же полный
  набор со старой спекой упал у меня 1 из 1 — в нём библиотека не пуста никогда.
- **Порча «кнопка ничего не открывает» до T121 проходила молча.** Панель библиотеки
  стоит в правой колонке всегда, и `.first()` брал её вместо так и не открывшейся:
  сценарий оставался зелёным на сломанной кнопке. Теперь панелей требуется две.

### Находка по ходу T129, которую я НЕ чинил

**Тот же класс живёт на экране ПРАВКИ, и подхват разметки (#7) его там не закрывает.**
`src/blocks/editor/ui/ChecklistEditor.tsx` (мой файл). Эффект при монтировании,
заведённый ради #7, подхватывает из разметки НАЗВАНИЕ и СТАНЦИЮ — окна в нём нет.
**Измерено зондом C (11.09.2026, до правки и независимо от неё):** у чек-листа с
утренним окном при медленной раздаче скриптов выбираю вечер — на экране `20:00|00:00`,
в скрытом поле `06:00`; нажимаю «Сохранить черновик» — экран возвращается к
`06:00|11:00`, в базе окно не изменилось. То есть правка окна, сделанная в первую
секунду, пропадает целиком: продукт говорит «черновик сохранён», а сохранено не то.
Отличие от T129 в одном — здесь расхождение видно после сохранения (список
возвращается к правде), а на заведении не видно никогда.

Почему не починил здесь же: лечение у этого экрана другое. Список окна стоит ВНЕ обеих
форм — «Сохранить» и «Опубликовать» это две отдельные формы в шапке, — поэтому приём
T129 (список отправляет себя сам) там неприменим, и остаётся именно подхват разметки
при монтировании, то есть правка в чужой области задачи со своей красной проверкой и
своим отрицательным прогоном. Порча П8 отрицательного прогона поставлена ровно сюда —
чтобы сказать, видит ли эту дыру хоть одна существующая проверка.

### Отрицательный прогон T129: порча → поймана/пропущена

Порчи взяты классом, а не по одной: три ломают опоры самой починки, четыре ломают
продукт (включая точное воспроизведение исходного дефекта на новом контракте), одна
поставлена СБОКУ — в соседний экран, куда починка не дотянулась. Прогон делал исполнитель,
последовательно (порт и база у копии одни); первую порчу прогнал я сам — на ней
проверялась сама оснастка.

Команды: `npx --no-install vitest run src/blocks/editor` (180 тестов, 15 файлов) и
`npx --no-install playwright test e2e/editor.spec.ts` (11 сценариев).

Прогон сделан 13.09 на слитом дереве: на 11.09 модульных в блоке было 179, проверку
охвата сторожа добавил T127. База — 180 зелёных и 11 зелёных сценариев.

| Порча | Что сломано | Модульные (180) | Сквозные (11) | Поймана? |
|---|---|---|---|---|
| П1. Список окна на заведении снова без `name` | опора починки: список перестаёт отправлять себя сам | 180 зелёных — **пропустили** | 11 из 11 упало: `expect(locator).toBeVisible() failed … element(s) not found` — `editor-screen` не появляется после «Завести» (помощник `createChecklist`, строка 53) | да, сквозными |
| П2. `data-live` снят с кнопки «Завести» | опора починки: сценарий теряет единственный способ дождаться отправки через React | 180 зелёных — **пропустили** | 1 из 11: `expect(locator).toHaveAttribute(expected) failed`, `getByTestId('create-checklist')`, `Expected: "true"`, `Received: ""` | да, одним сценарием — тем самым, ради которого признак и заведён |
| П3. Склейка и разбор поля разъехались: `parseWindowField` режет по запятой | опора починки: одно поле и один разбор на оба экрана | 5 из 180: `AssertionError: expected { …(3) } to strictly equal { …(3) }` — три в `window-field.test.ts`, две в `action-input.test.ts` | 11 из 11: `expect(locator).toBeVisible() failed` — `editor-screen` не появляется, чек-лист не заводится | да, обоими наборами |
| П4. **Исходный дефект T129 на новом контракте:** список снова без `name`, окно едет скрытым полем, посчитанным из состояния React | продукт: выбор, сделанный до того как экран ожил, пропадает молча | 180 зелёных — **пропустили** | 2 из 11 — ровно оба сценария окна: `expect(locator).toHaveValue(expected) failed`, `getByTestId('checklist-window')`, `Expected: "20:00\|00:00"`, `Received: "06:00\|11:00"` | да, только сквозными |
| П5. Умолчание формы заведения стало вечерним — «всегда вечер» | продукт: нетронутое окно уезжает не той сменой | 180 зелёных — **пропустили** | 1 из 11 — контрольный сценарий: `Expected: "06:00\|11:00"`, `Received: "20:00\|00:00"`; оба сценария про вечер зелёные | да, только контрольным сценарием |
| П6. Разбор формы подставляет утро, когда поля окна нет вовсе | продукт: тихое умолчание вместо внятного отказа | 1 из 180: `AssertionError: expected { start: '06:00', end: '11:00' } to strictly equal { start: '', end: '' }` | 11 зелёных — **пропустили** (поле приходит всегда, подставлять нечего) | да, только модульным |
| П7. Экран правки снова отправляет окно парой `windowStart`/`windowEnd` | продукт: вернулся старый контракт, правка окна не доезжает до сервера | 180 зелёных — **пропустили** | 4 из 11: `expect(locator).toHaveText(expected) failed`, `getByTestId('editor-meta')`, `Expected: "Черновик сохранён"`, `element(s) not found` — черновик не сохраняется вовсе | да, но сценариями сохранения, а не окна |
| П8 (сбоку). Подхват разметки при монтировании на экране правки перестаёт находить поля (приём #7) | соседний экран, куда починка не дотянулась | 180 зелёных — **пропустили** | 11 зелёных — **пропустили** | **нет, ни одной проверкой** |

Три вывода, и ни один не читается из кода.

- **Модульный набор в одиночку пропустил бы ровно тот дефект, ради которого задача
  заведена.** П4 воспроизводит T129 точь-в-точь на новом контракте — и 180 модульных
  остаются зелёными, краснеют только два сценария окна: `Expected "20:00|00:00",
  Received "06:00|11:00"`. Обратное тоже верно: П6 (тихое утро в разборе формы) не видит
  ни один браузер. Проверки здесь не дублируют друг друга, а делят дефекты по классам.
- **Контрольный сценарий оправдал себя на П5.** «Всегда вечер» ломает его одного, а оба
  сценария про выбранный вечер остаются зелёными: без контрольного порча прошла бы молча,
  и набор доказывал бы не «окно уезжает выбранным», а «окно уезжает вечерним».
- **П8 — ответ на находку выше, и ответ отрицательный.** Подхват разметки на экране
  правки можно выключить целиком, и набор остаётся полностью зелёным: 180 и 11. Дыру,
  описанную абзацем выше, не видит ни одна существующая проверка — то есть починка T129
  закрыла заведение, а правка держится не на проверке, а на том, что окно там редко
  трогают в первую секунду. Это и есть содержание оставленной находки, теперь измеренное.

Отдельно про П7: возврат старого контракта ловится, но не проверкой про окно. Сценарии
окна его не видят вовсе — они не доходят до экрана правки, — а краснеют четыре сценария
сохранения черновика, которым окно безразлично. Проверки, прицельно сторожащей окно на
правке, в наборе нет.

**Как прогоняли.** Оснастка моя: точная замена по якорю (якорь обязан встретиться ровно
один раз), два набора, снятие порчи копией, снятой ДО порчи, в `finally` и на сигналах —
поэтому дерево остаётся чистым даже на обрыве. На П1 оснастка проверялась, её прогнал я
сам; П2–П8 прогнал исполнитель по готовому списку, последовательно. После каждой порчи
оснастка печатает `git status` по тронутым файлам: все восемь раз — чисто.

## Статус

done
