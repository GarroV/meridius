<!-- managed by forge: do not change the row format below — forge-build and forge-status parse this file -->
# Decisions

<!-- id: Dnnn (D001, D002, ...). who: owner (the owner decided) | auto (decided autonomously by the dispatcher). -->

| id | date | decision | why | who |
|---|---|---|---|---|
<!-- | D001 | 2026-07-29 | Example decision | Example rationale | owner | -->
| D001 | 2026-09-05 | Заполняющего чек-лист не опознаём: ни входа, ни PIN, ни привязки устройства. Заполнить может любой, кто отсканировал QR | «не усложняем» — основная концепция продукта; опознание требует администрирования списков и раздачи доступов, чего на пилоте не окупается | owner |
| D002 | 2026-09-05 | История заполнений хранится двумя способами сразу: версии чек-листа неизменяемы, и каждое заполнение хранит снимок пунктов | «нам просто надо учесть что чек листы будут меняться, и история не должна перестраиваться вместе с ними» | owner |
| D003 | 2026-09-05 | GPS не запрашиваем, порогов скорости заполнения нет. В детализации видно только длительность заполнения | Гео в помещении даёт обратный по надёжности сигнал: честный сотрудник на кухне получает нарушение, нечестный из дома — чистые координаты. Пороги скорости ложно срабатывают на коротких чек-листах | owner |
| D004 | 2026-09-05 | Расписания смен и календаря нет. У чек-листа есть только окно времени, чтобы утром открывался утренний, а вечером вечерний | «не будем усложнять систему календарем... управляющие и так знают график». Метрика «пропущено» из MVP выпадает вместе с календарём | owner |
| D005 | 2026-09-05 | Офлайн-режима нет: ни IndexedDB, ни Service Worker | «оффлайн не нужен, забиваем» | owner |
| D006 | 2026-09-05 | QR-код станции перевыпускается в любой момент, вплоть до ежедневного. Предусмотрен вывод QR на экран планшета, а не только печать наклейки | «чтобы условная пиццерия могла хоть каждый день их менять... пригодится для привязки куара в вебе, на планшете». Ротация кода — единственная защита публичной ссылки при отказе от опознания (D001) | owner |
| D007 | 2026-09-05 | Иерархия: УК → страна → пиццерия → станция. Уровня «партнёр» нет | «у нас есть УК и есть страны. Стандартные чек листы делает УК. Страны могут пользоваться сервисом чтобы генерировать себе куаркоды для личных чек листов» | owner |
| D008 | 2026-09-05 | Разработка ведётся на личной машине владельца, боевое размещение — позже в контуре компании. На личной машине только демонстрационные данные, реальных выгрузок нет | «это разработка для команды международного направления... разработка да на личной машине, мне так проще. потом перекинем на сервера компании» | owner |
| D009 | 2026-09-05 | Документы проекта ведутся на русском, тексты продукта — на английском и русском | Владелец и команда читают документы по-русски; настройка Forge по умолчанию (английский) не соответствует этому проекту | auto |
| D010 | 2026-09-05 | Состав MVP: веб-сервис, где создаётся чек-лист для конкретной пиццерии конкретной страны, печатается QR станции, чек-лист заполняется по этому QR и результат виден в ленте. Аналитика, роли, тенанты партнёров, языки контента — после MVP | «что я вижу мвп: у нас есть веб сервис, в котором мы можем создать чек лист для конкретной пиццерии в конкретной стране. дальше уже доделываем аналитики, тенанты партнеров и прочее» | owner |
| D011 | 2026-09-05 | В чек-листе есть секции (группы пунктов со своим заголовком) и библиотека переиспользуемых блоков: блок заводится один раз и вставляется в любое число чек-листов, правка идёт в одном месте. Оба механизма входят в MVP | «блоки 1 раз завел и 10 раз переиспользуешь, да». Это главная ценность редактора: заведение чек-листов пачками превращается из копипасты в сборку | owner |
| D012 | 2026-09-05 | Чек-лист на экране заполнения визуально разделён на секции, а не идёт сплошным полотном | «визуально в самом чек листе это не должен быть скрижаль с данными, а все же немного разделяться» | owner |
| D013 | 2026-09-05 | Визуальный язык берётся из дизайн-системы Dodo P&L Service (направление «Реестр»): токены цвета, типографика Golos Text, малые радиусы, плотность, видимый фокус. Берётся только визуал — структура экранов своя. Токены скопированы в docs/furca/design/reference/ | «возьми за основу дизайн систему сервиса по работе с пнл. только визуал! не структуру» | owner |
| D014 | 2026-09-05 | Вход в админку в MVP есть, но минимальный: один аккаунт с паролем, без ролей и областей видимости | Без входа конструктор чек-листов открыт всему интернету; полноценные роли отложены за пределы MVP по D010 | owner |
| D015 | 2026-09-06 | Экранный эталон опубликован артефактом для сверки с коллегами: https://claude.ai/code/artifact/8f518909-fea6-4b71-8fad-b1658a8dbf6c — ссылка постоянная, страница обновляется по мере правок. Замечания коллег собираются на самой странице и читаются обратно в проект | «переведи это все в артефакт и закрепи его как постоянный... мне это надо чтобы отдать коллегам для сверки и обратной связи» | owner |
| D016 | 2026-09-06 | Лист QR печатается из браузера (`@media print`, `@page { size: A4 }`), серверный PDF не делаем | Ноль зависимостей и ноль расхождений между тем, что видно на экране, и тем, что вышло из принтера. Серверный PDF нужен для отправки почтой и архива — это не сценарий MVP | auto |
| D017 | 2026-09-06 | Версии стека: Next.js 16.3.4, Node 24 LTS, Drizzle ORM 0.45.2 (не кандидат 1.0), Tailwind 4.3.3, next-intl 4.14.2, Vitest 5 + Playwright 1.63, **TypeScript 6.0.3 — не 7.x** | TypeScript 7 (порт на Go) лишился Compiler API и несовместим с typescript-eslint 8.69: линтер с типами просто не запустится. Drizzle 1.0 не выпущен в стабильный тег даже авторами, а на нём держится вся схема данных | auto |
| D018 | 2026-09-06 | Промежуточного экрана-сводки перед отправкой заполнения нет: кнопка отправляет сразу | Чек-лист станции короткий, проваленный пункт уже подсвечен в моменте. Лишний экран — лишнее касание там, где мы боремся за каждое | auto |
| D019 | 2026-09-06 | Каркас проекта собирается руками, без CLI-генератора | Генераторы тянут аутентификацию, оплату и слои, которых у продукта нет; вычищать их дороже, чем написать пять конфигов | auto |
| D020 | 2026-09-06 | Перетаскивание пунктов мышью в MVP не делаем: порядок меняется стрелками и с клавиатуры | Перетаскивание на планшете мучительно, клавиатурные стрелки работают везде и уже заложены в экранный эталон | auto |
| D021 | 2026-09-06 | Защита публичной ссылки заполнения тремя мерами: ссылка позволяет только отправить заполнение и не отдаёт историю станции; частота отправок с одного кода ограничена; наклейка размещается в рабочей зоне, а не в зале | Следствие отказа от опознания (D001). Исследование рынка показало, что во всей категории продуктов вход обязателен — значит ответственность за защиту публичной точки записи полностью на нас | auto |
| D022 | 2026-09-06 | Бюджета на внешние сервисы нет. Всё, что нужно продукту, работает локально; платных зависимостей в MVP не появляется | «бюджета нет, строим в первую очередь мвп (мы обсуждали выше)» | owner |
| D023 | 2026-09-06 | Репозиторий заводится в личном аккаунте GarroV. Вопрос публичности вынесен владельцу отдельно: в открытый репозиторий уедут токены внутренней дизайн-системы Dodo P&L и документы внутреннего проекта | «репо личный открытый на гитхабе» — владелец назвал открытый, но публикация внутренних материалов компании необратима, поэтому подтверждается отдельно | owner |
| D024 | 2026-09-06 | В базу не ходит мимо `data` никто: драйвер импортирует только он. Но блоки, которым нужны свои запросы (справочник в `catalog`), берут из `data` схему и `getDb()` и пишут запросы у себя, а не заказывают функции в `data` | Иначе `data` собрал бы в себе CRUD всех блоков, то есть половину продукта, и стал бы узким местом параллельной стройки. Проверяемая машиной граница — запрет импорта драйвера — сохраняется | auto |
| D025 | 2026-09-06 | Язык страны в схеме называется `countries.locale`; контракт блока `catalog` приведён к этому имени, а не наоборот | Колонка одна и та же, код уже написан и покрыт тестами; переименование ради формулировки в документе — правка ради правки | auto |
| D026 | 2026-09-06 | В схему добавлена `stores.timezone`, окно времени чек-листа сравнивается с местным временем пиццерии | Без часового пояса утренний чек-лист в стране со сдвигом открывался бы среди дня. Прямое следствие D004: окно — единственный механизм выбора чек-листа, и оно обязано быть местным | auto |
| D027 | 2026-09-06 | Уязвимости esbuild внутри drizzle-kit терпим, drizzle-kit до 0.18 не откатываем | `npm audit --omit=dev` даёт ноль уязвимостей: в том, что попадает в работающий продукт, их нет вовсе. Дефект esbuild эксплуатируется только через запущенный дев-сервер самого esbuild, которого в проекте нет. Откат — ломающее изменение ради проблемы, которой у нас не существует | auto |
| D028 | 2026-09-06 | Шрифты Golos Text и IBM Plex Mono подключены через next/font с самохостингом; первая сборка в свежем клоне требует сети, чтобы их скачать | Самохостинг важнее: на кухне первый экран должен появляться быстро, а не ждать чужой сервер шрифтов. Требование сети на сборке приемлемо — сборка и так тянет зависимости | auto |
| D029 | 2026-09-06 | Расхождение экрана входа с эталоном решено в пользу продукта: заголовок — h1 с межстрочным из токена, эталон приведён к нему | Продукт следует правилам самой дизайн-системы, а в эталоне заголовок был обычным блоком и наследовал чужое межстрочное. Прав тот, кто следует системе | auto |
| D030 | 2026-09-06 | Относительный порог покрытия к приёмке блока catalog не применяется: общий показатель упал с 89,11% до 69,7%, но логика выросла до 91,5%, а вниз тянет впервые появившаяся разметка (25,3%), которая проверяется сквозными сценариями в браузере. Заведена T072 — считать покрытие раздельно | Порог заведён против молчаливой деградации. Здесь деградации нет: сравнивать 89% чистой логики с 69% смеси логики и разметки — сравнивать разные величины, и такой порог начал бы заворачивать блоки за то, что у продукта появились экраны | auto |
| D031 | 2026-09-06 | Экранный эталон приведён к спецификации: коды станций на макетах больше не содержат знаков 0 и 1 | Спека запрещает похожие знаки в коде станции, чтобы код нельзя было спутать при чтении с наклейки, а эталон рисовал их вопреки этому. Прав тот, кто задаёт правило, — эталон догоняет | auto |
| D038 | 2026-09-06 | Целевая площадка продукта — домашний сервер MUSPELHEIM: пилот живёт там, а личная машина владельца остаётся площадкой разработки (D008). Перенос в контур компании — за горизонтом MVP. Площадка общая: своя папка `C:\projects\dodo_qr_service`, своё имя compose-проекта, свои порты; чужие контейнеры не трогаем | «напомню что проект ставим на муспельхейм». MUSPELHEIM — площадка новых проектов: работает постоянно и не зависит от того, открыт ли ноутбук владельца, а чек-лист на кухне заполняют тогда, когда владельца нет за машиной | owner |
| D039 | 2026-09-06 | Репозиторий проекта — `GarroV/dodo_qr_service` на личном GitHub владельца, публичный. Беклог зеркалируется в Issues того же репозитория. Следствие принято сознательно: в задачи не попадает ничего, что не должно быть публичным, а находки по безопасности идут приватным advisory, а не задачей | «заводим на моем гитхаб, публичный репо». Так же заведены соседние проекты владельца (`dodo_pnl_service`, `dodo_audit_service`, `dodo_kb_bot` — все публичные): единая практика важнее разовой осторожности, а секретов в коде нет — `.env` вне git, значения в `.env.example` демонстрационные и в production вход с ними отказывает | owner |
| D034 | 2026-09-06 | Страница заполнения публикуется наружу через `tailscale funnel` на своём порту, а не переводом телефонов сотрудников в Tailscale. Корень `:443` на площадке занят чужим продуктом, поэтому берём отдельный порт | «tailscale funnel, свой порт». Продукт построен на том, что заполняющего не опознают (D001): требование ставить VPN на телефон каждого сотрудника вернуло бы администрирование доступов, которого продукт избегает. Плата — публичный маршрут заполнения, под него уже стоит задача T071 о строгом CSP | owner |
| D035 | 2026-09-06 | Переезд на MUSPELHEIM выполняется после волны 1, разом готовым MVP, а не поэтапно по ходу стройки. До переезда продукт живёт на машине владельца | «После волны 1, разом». Ставить сегодня значило бы показать снаружи админку без главного сценария — заполнения по QR: продукт выглядел бы готовым, не будучи им. Блоки `fill`, `qr`, `feed`, `library`, `demo` ещё не начаты | owner |
| D036 | 2026-09-06 | Публичным репозиторий становится только после того, как из истории вычищен PDF «Проектирование системы чек-листов Додо Пицца» — внутренний документ компании, лежащий в git с первого коммита (1,1 МБ). До этого репозиторий заводится приватным | Публикация необратима: документ уходит в поисковые кэши и клоны, и удаление файла потом уже ничего не отменяет. Само решение владельца о публичности (D039) этим не отменяется — меняется только порядок: сначала чистка, потом видимость | auto |
| D032 | 2026-09-06 | Выбор станции в редакторе показывает путь «Страна · Пиццерия · Станция», а не одно название станции, как на эталоне | Список станций в редакторе глобальный: «Кухня» есть в каждой пиццерии, и без пути методист выбирает вслепую. Эталон рисовал экран одной пиццерии, продукт работает со всей сетью | auto |
| D033 | 2026-09-06 | Сборка запекает значения переменных окружения: после смены секретов нужна пересборка, иначе запуск отвечает ошибкой о значении из примера. Учесть в раскатке | Найдено блоком editor при проверке живой сборки. На раскатке это выглядит как «поменяли пароль — продукт лёг», причём тесты и локальный запуск в режиме разработки проблемы не показывают | auto |
| D037 | 2026-09-06 | D036 отменено: репозиторий `GarroV/dodo_qr_service` переведён в публичные сразу, PDF концепции остаётся в истории, чистка не выполняется (T076 снята) | «репозиторий публичный, все документы что я тебе дал — не несут никакой сверх информации». Оценку чувствительности документов компании делает владелец, а не система: он их и принёс. Моя часть — показать, что именно уходит наружу, и это было показано до публикации | owner |
| D040 | 2026-09-06 | Базовый адрес в ссылке QR-кода берётся из переменной окружения, а не зашивается в код | На площадке продукт публикуется по адресу вида `https://<хост>:10000`, а на машине разработчика это `http://localhost:3100`. Адрес попадает внутрь напечатанного кода: зашитый в код — значит все наклейки во всех пиццериях становятся мусором при переезде, вопреки D006 | auto |
| D041 | 2026-09-06 | Расхождения экранного эталона с критериями блока fill решены в пользу критериев: кнопка отправки неактивна, пока есть незакрытые пункты; при обрыве связи ответы остаются на том же экране, а не заменяются отдельной страницей. Эталон правится под продукт | Критерии готовности блока писались как проверяемые требования и покрыты тестами; эталон рисовался раньше и в этих двух местах противоречит сам себе (обещает сохранность ответов на экране, где ответов не видно) | auto |
| D042 | 2026-09-06 | Кнопка «Скачать PDF» убирается из эталона листа QR: серверного PDF в продукте нет по решению D016, печать браузерная | Эталон обещал действие, которого в продукте нет и не будет; кнопка, ведущая в никуда, хуже её отсутствия | auto |
| D043 | 2026-09-07 | Продукт выводится наружу СЕЙЧАС, для показа коллегам, не дожидаясь закрытия волны 1 — D035 («переезд после волны 1, разом готовым MVP») этим отменяется в части срока | «мне надо продукт показать другим людям, выводи его в интернеты». Библиотека блоков (T027-T031) в показ не попадает, и владелец знает об этом: сквозной сценарий (чек-лист → QR → заполнение → лента) проверен фактическим прогоном и работает | owner |
| D044 | 2026-09-07 | Токен третичного текста `--ink-3` отступает от дизайн-системы ради порога доступности: в светлой теме #98A1AC → #626B77, в тёмной #6D7A8A → #8A97A6. Ссылка в крошках карточки заполнения получила подчёркивание | Прежнее значение давало контраст 2,27:1 на фоне приложения при пороге 4,5:1 — падали девять проверок доступности сразу, то есть надстрочные и метаданные были нечитаемы для слабовидящего на всех экранах продукта. Тон сохранён, двинута только светлота, и ровно до порога. Прав тот, кто задаёт правило: проверка доступности заложена в задачу T052, а токен приехал из чужой системы, где этого порога не держат (как в D031 эталон догонял спеку) | auto |
| D045 | 2026-09-07 | Продукт публикуется по схеме мульты (Tailscale терминирует TLS и проксирует в локальный порт), но не на корне адреса, а на своём пути: `https://muspelheim.tail48dfee.ts.net:10000/qr`. Появились `BASE_PATH` (basePath сборки) и путь внутри ссылки напечатанного QR | «сделай также как сделано у мульты. уже обкатанное решение». У Tailscale Funnel всего три порта под публикацию, и все заняты: :443 отдаёт чужой проект на общей площадке, :8443 — демо P&L, :10000 — MCP сервиса аудита, которому нужен именно интернет. Забрать порт целиком значило бы сломать чужое или своё работающее демо, поэтому продукт встал на путь рядом с MCP. Пустой `BASE_PATH` — обычный корень, поэтому разработка и 84 проверки живут как раньше | owner |
| D046 | 2026-09-07 | Публикация наружу идёт через Cloudflare Tunnel на площадке, а не через путь на порту Tailscale: продукт живёт на корне своего адреса. D045 отменён в части пути — код `BASE_PATH` остаётся, но не используется | Путь потребовал бы провести базовый префикс через ВСЕ ссылки продукта: обычным `<a href>` Next префикс не добавляет, и сценарий ломался на переходе в карточку заполнения. Это долг, который кусал бы каждую новую фичу. Отдельный туннель снимает весь класс проблем и ничего чужого на общей площадке не двигает. Схема та же, что у мульты: слой публикации отдельно от продукта. Плата — адрес вида `*.trycloudflare.com` живёт до перезапуска туннеля; постоянный требует домена в Cloudflare | auto |
| D047 | 2026-09-07 | Единая короткая учётка на время показа поставлена на площадке: требование владельца подтверждено повторно, и оно исполнено. Проверку длины не снимали — она стала преодолимой явным флагом `--allow-short` у `scripts/hash-admin-password.mjs`, с предупреждением на выходе | «сделай пока единую учетку admin admin, потом уже решим позже», затем повторно — что этот пароль не пускает. Требование повторено владельцем — это его решение, исполняем полностью. Порог 12 знаков оставлен по умолчанию, чтобы короткий пароль не появлялся случайно и не появлялся молча; отступление оформлено флагом и помечено комментарием в `.env` площадки. Риск назван владельцу: пока продукт открыт в интернете, такой пароль подбирается первой попыткой, а за кабинетом стоит публичная точка записи в базу (D014). Держим до конца показа, дальше — длинный. Самого значения нет ни в одном файле git: репозиторий публичный, значение живёт только в `.env` площадки и у владельца | owner |
| D048 | 2026-09-07 | Первый экран кабинета (`/admin`) остаётся отдельным экраном и ведёт в разделы ссылками, хотя в экранном эталоне такого экрана нет: эталон открывает методиста сразу рабочим экраном | Заглушку надо было снять сегодня — владелец не мог пройти дальше входа. Вариант «перенаправлять вход сразу в чек-листы» ближе к эталону, но переписывает ожидания семи наборов сквозных сценариев и трогает мигающую проверку доступности (#6) — риск не по размеру задачи. Отдельный экран заодно даёт то, что нужно прямо сейчас: показывая продукт коллегам, видно все разделы сразу. Отступление осознанное и обратимое: когда #6 закрыт, экран можно заменить перенаправлением одной правкой | auto |
| D049 | 2026-09-07 | «Удалить чек-лист» удаляет целиком только то, по чему не заполняли; чек-лист с заполнениями снимается с работы (`checklists.archived_at`) и уходит из списка и со станции, сохраняя историю | Владелец: «не вижу как удалить чек лист» — кнопки не было вовсе. Стирать чек-лист с историей нельзя: заполнения ссылаются на его версии и хранят снимок пунктов, которые видел сотрудник (принцип 3, D002), а лента показала бы историю, ведущую в никуда. Вариант «удалять нельзя, есть заполнения» отвергнут: это тупик, а методисту нужно убрать чек-лист из работы. Разница названа на экране подтверждения до нажатия, и кнопка называется тем, что произойдёт | auto |
| D050 | 2026-09-07 | Целевая картина продукта — личные пространства стран: страна заходит в своё пространство, берёт стандартные чек-листы УК за основу, адаптирует их и заводит собственные. Это меняет смысл слова «MVP» в проекте: D010 и D014 выносили роли и области видимости за MVP, теперь они — цель, к которой всё проектируемое обязано стыковаться, а не откладываемая надстройка | «как итоговый MVP я вижу то, что мы даём возможность зайти в их личное пространство в системе, и там начать работать со своими чек-листами. То есть брать за основу наши чек-листы, адаптировать их, что-то менять и для своей страны заводить собственные». Требование владельца: «очень важно сделать так, чтобы ничего не сломать, ну и плюс стыковаться с другими планами, которые у нас были» | owner |
| D051 | 2026-09-07 | Уровни критичности пунктов, режимы смены, понятие закрытия дня, станция-планшет и уведомления проектируются двумя дизайн-документами, и оба обязаны быть совместимы с D050: любая новая сущность заводится с владельцем (УК или страна) с первого дня, а не глобально с миграцией потом | Выбор владельца из четырёх вариантов раскроя. Глобальный справочник, заведённый сегодня, при появлении пространств стран превращается в переливание данных на живом продукте | owner |
| D052 | 2026-09-07 | Режим смены выбирается прямо на публичной странице станции, без гейта: ни кода менеджера, ни входа. Удерживает от злоупотребления только прозрачность — выбранный режим и его причина попадают в историю и видны управляющему в ленте | Выбор владельца из четырёх вариантов веса гейта. Возражение при выборе было названо: бесплатное и анонимное сокращение имеет свойство становиться ежедневной нормой, и тогда сокращённый режим перестаёт быть исключением. Владелец принял этот риск в пользу отсутствия трения на кухне: гейт стоит времени у человека, у которого его нет, а продукт проигрывает бумаге ровно на этом | owner |
| D053 | 2026-09-07 | Объекта «день пиццерии» и действия «закрыть день» не заводим: критичный пункт не блокирует ничего, он поднимает тревогу. При этом незаполненный чек-лист сам по себе считается тревогой того же веса, что провал критичного пункта | «давай возьмем вариант что никаокго дня, только тревога. но незаполненный чеклист это тоже критично само по себе». Дешевле всего и не вводит кнопку, которую по D001 некому нажать. Вторая половина решения важнее первой: без неё молчание неотличимо от порядка, и продукт видел бы только тех, кто и так заполняет | owner |
| D054 | 2026-09-07 | «Пропущен» определяется по окну чек-листа, а не по календарю: опубликован, привязан к станции, окно за сегодня в часовом поясе пиццерии прошло, заполнения нет. D004 (нет расписания и календаря) этим НЕ отменяется — метрика пропущенного строится на уже имеющемся окне и `stores.timezone` (D026) | Следствие D053. Названная слабость: в нерабочий день тревога сработает ложно, потому что отличить закрытую пиццерию от забытого чек-листа без календаря нечем. Принято как терпимое для сети, работающей ежедневно | auto |
| D055 | 2026-09-07 | Режим смены — состояние пиццерии, а не станции и не заполнения: выбран на любой станции, действует на все станции этой пиццерии до конца местных суток по `stores.timezone`. Переставляется в любой момент, каждая перестановка остаётся в истории и видна в ленте | Выбор владельца. Соответствует сценарию «менеджер пришёл на смену и знает, сколько сегодня людей»: одно действие вместо обхода станций. Обратная сторона принята: первый выбравший решает за всю пиццерию, и удерживает от произвола только видимость перестановок (D052) | owner |
| D056 | 2026-09-07 | Три уровня пункта (критичный, важный, обычный) и три режима смены (обычная, с ограничениями, критичная) зашиты в продукт жёсткой матрицей; справочника режимов нет и настраивать нечего. В сокращённом режиме лишние пункты на экране не показываются вовсе, а не помечаются необязательными | Выбор владельца. Отсутствие справочника — прямое исполнение D051: переливать при появлении пространств стран нечего, потому что перечисление живёт в коде, а не в таблице. Обратная сторона названа: сотрудник не видит, что стандарт требует больше, чем ему показали | owner |
| D057 | 2026-09-07 | Продукт называется **MERIDIUS**. Меняется только бренд-токен (ключи `brand` / `brandMuted`, заголовок публичной главной, `metadata.title`, где сейчас стоит служебное `dodo_qr_service`). Слово «чек-лист» как название раздела и сущности остаётся везде: в каталоге строк бренд и предмет уже разведены разными ключами. Напечатанные наклейки бренда не несут и остаются действительными | Владелец: «пока полностью под 0 не переименовываем, только визуальные части при контакте с пользователем. MAXIMUS — ПНЛ 🏛️, DECIMUS — Аудиты, MERIDIUS — Чек-листы». Разбивка по местам и открытые вопросы (что остаётся от связки «Чек-листы»+«Dodo», нужен ли свой знак) — issue #15 | owner |
| D058 | 2026-09-09 | D057 расширено ДЛЯ ЭТОГО ПРОЕКТА (не для MAXIMUS/DECIMUS/FURCA — у них своя площадка вопроса): переименование доведено до конца, а не только до видимой части. Меняются `package.json`, репозиторий (`GarroV/dodo_qr_service` → `GarroV/meridius`), локальная папка, имя compose-проекта и БД (`dodo-qr`/`dodo_qr` → `meridius`), а также площадка MUSPELHEIM (папка, compose, БД, задачи планировщика). Общая рамка issue #15 «имена репозиториев/модулей/схемы/env не трогаем» для этого проекта больше не действует; открытый вопрос issue #15 про знак/связку с Dodo — не решён, брендовые ключи заполнены буквально по D057 (`brand: MERIDIUS`, `brandMuted: Dodo` без изменений) | Владелец: «пока у нас только старт разработки, надо сразу применять новое название»; на уточнение объёма — «давай наверное все сразу переделаем. у нас же не так много еще написано в этом проекте». Раз разработка только началась, дешевле переименовать сейчас, чем тащить `dodo_qr_service`/`dodo-qr` через весь проект и разводить его с публичным именем MERIDIUS годами | owner |
| D059 | 2026-09-13 | Проверяющей роли и приёмочному смоуку даётся отдельный стенд с заведомо тестовыми учётными данными из `e2e/admin-credentials.ts`, переданными переменными окружения в самой команде запуска (`ADMIN_PASSWORD_HASH`, `SESSION_SECRET`), без правки `.env` рабочей копии. Вариант (б) из Q008 | Владелец на Q008 ответил «реши сам эту проблему, мне надо чтобы был готовый продукт». Эти значения уже лежат в репозитории открытым текстом и секретом не являются по своему назначению; отдельный стенд не ослабляет рабочий и не трогает его настройки — именно поэтому сторож отклонял прежние попытки, правившие `.env` | auto |
| D060 | 2026-09-13 | Сломанный часовой пояс пиццерии ловится при сохранении в справочнике; публичный маршрут заполнения оставляет громкий отказ, второй (мягкий) режим отказа не заводится | Владелец ответил «ок» на разъяснение по Q006 от 11.09, где это и было рекомендацией: тихий отказ на кухне хуже громкого, а проверка при сохранении убирает причину вместо того, чтобы сглаживать следствие. Сопоставление ответа «1. ок» с Q006 сделано по времени и содержанию — журнал исходящих канала прочитать не удалось (боевая база, чтение отклонено), и это записано здесь честно, а не выдано за подтверждённое | auto |
| D061 | 2026-09-13 | T108 (планшет на станции и уведомления) выносится за горизонт MVP — этап 3; в готовность MVP не входит, из трекера (#17) не убирается | Спека относит уведомления к nice и перечисляет их в «Не делаем», а требование must про планшет (вывод QR станции на экран) уже выполнено блоком qr. Сверх этого задача упирается в развилку, которую нельзя закрыть дефолтом: уведомлению нужен адресат, а продукт сознательно не знает, кто на смене (D010, D014), и опознание прямо исключено D001. Строить по догадке здесь дороже всего — владелец 11.09 сказал «мне надо чтобы был готовый продукт», а не «добавь уведомления» | auto |
| D062 | 2026-09-13 | Публичный адрес показа остаётся на `tailscale funnel` (подтверждает D045); именованный туннель Cloudflare не берём | Владелец на Q007: «Домена нет на данный момент, делаем через туннель». Без своего домена именованный туннель Cloudflare невозможен по определению, а случайный `*.trycloudflare.com` Chrome и помечает Dangerous — то есть вариант, из-за которого вопрос и возник | owner |
| D063 | 2026-09-13 | Три вида тревоги на ленте различаются только текстом; третий «плохой» цвет тега в дизайн-систему не заводится | Владелец на Q009: «Хватит текста». Решение принято против рекомендации автора вопроса (различать визуально) и записано именно так, чтобы следующая сессия не переоткрыла развилку, увидев «один красный стиль на три смысла» | owner |
| D064 | 2026-09-13 | Префикс публичного маршрута заполнения `"/s/"` живёт в `core/public-routes`; `src/security-headers.ts` и `src/blocks/qr/scan-url.ts` берут его оттуда. Импорт в `security-headers.ts` — относительным путём, а не через алиас `@/` | Канонического владельца у факта не было: маршрут принадлежит `fill`, строит адрес `qr`, а политику заголовков держит корневой файл вне границ блоков, и границы (`.dependency-cruiser.cjs`) не дают `qr` импортировать `fill`. Общее знание живёт в `core` по той же причине, что адреса разделов кабинета (D-решение T074). Относительный путь — не небрежность: `next.config.ts` импортирует этот файл как `./src/security-headers`, то есть вне TS-алиасов, и `@/…` роняет сборку с «Cannot find module» — поймано прогоном | auto |
| D065 | 2026-09-14 | Сетка часов в бумажном чек-листе — артефакт печати, а не модель продукта. Регулярность настраивается интервалом, а экран показывает состояние проверки (сделана в 11:00 / пора / пропущена), а не таблицу часов. Таблица остаётся только в отчётах, где она и нужна | «В бумажном виде ты не можешь по-другому никак отобразить регулярность. Но в IT-системе мы можем дать возможность настраивать интервалы обходов и в самом чек-листе интерактивно отображать» | owner |
| D066 | 2026-09-14 | Отметка периодической проверки всегда встаёт в ТЕКУЩИЙ интервал; пропущенные интервалы остаются пропущенными и задним числом не закрываются. Отказа сотруднику нет — он отмечает, отметка просто ложится туда, куда положено | «В девять пропустили, в десять пропустили. Время сейчас одиннадцать с копейками, мы отмечаем, и отметка ставится на одиннадцать часов, те два мы в системе оставляем, что они пропущены». Пропуски нужны статистике: «разбирать, почему так получается — нагрузка была большая, людей недостаточно» | owner |
| D067 | 2026-09-14 | Уровень пункта и режим смены (D056) действуют на периодические проверки поштучно: проверка, выпавшая по уровню, сегодня не даёт ни дедлайнов, ни сигналов, ни тревог | «Если мы ставим этому пункту вариант с некритичным, при выборе сегодняшнего дня настройки, что у нас сегодня только критично, значит, этот пункт отметается и сегодня не работает» | owner |
| D068 | 2026-09-14 | Оповещение о пропущенной проверке настраивается методистом: каждые 10 / 20 / 60 минут или не оповещать. Сигнал повторяется, пока просрочка держится, и гаснет с отметкой | «Нужно сделать какую-то регулярность сигнала, что вот пропущенный вот такой пункт… Нужна вот эта возможность гибкой настройки нам дать» | owner |
| D069 | 2026-09-14 | Справочника продуктов со сроками годности, журнала выставления на линию и учёта списаний НЕ будет. Отслеживание маркировки на стадии заготовки — не наша задача. Q014 снимается вместе с половиной дизайна от 14.09 | «Мы не ведём никакой файлик со сроком… Это сервис только про чек-листы в онлайне, всё, не больше. Какие-то дебри учёта мы в рамках этого сервиса сто процентов не будем» | owner |
| D070 | 2026-09-14 | «Будильники» — блок чек-листа, а не сущность продукта: сотрудник вручную вносит время, планшет в это время звонит. Разовый, регулярности у него нет и не будет. **Срок жизни уточнён решением D090:** будильник живёт до конца окна времени чек-листа, а не до конца местных суток — формулировка «на сегодня» из этой строки D090 перекрыта | «Речь именно про просто доступный будильник под рукой, который в рамках чек-листа работает… Это ручная операция, и ежедневно здесь нет возможности какую-то регулярность сделать» | owner |
| D071 | 2026-09-14 | Оси смены (утро / день / вечер) для круглосуточных пиццерий не заводим: окна времени у чек-листа достаточно. Q011 снимается | «Насколько нам важна жёсткая привязка к утро-вечер-день. Сейчас реализовано, что мы можем задать время, мне кажется, этого достаточно… Мы же можем по времени выставлять, когда чек-лист работает» | owner |
| D072 | 2026-09-14 | Второй подписи («проверил менеджер») не будет. Q010 снимается | «По поводу подписи это плевать» | owner |
| D073 | 2026-09-14 | Эскалации наружу (почта, Telegram) не будет: сигнал звонит на месте, а пропуски собираются в отчётах. Сигнал о подходящем сроке на линии управляющему тоже не нужен. Q012 снимается | «Звонит будильник на месте, в отчётах это всё собирается, если вдруг где-то была просрочка… А по поводу просрочки на линии и сигнализация о том, что по времени что-то подходит — это по фигу» | owner |
| D074 | 2026-09-14 | Замес теста в пилоте нужен, но без углубления: берём то, что есть в исходном файле, и не строим вокруг него учёт — клонирование замесов и вынос живут в отдельном сервисе. Q013 отвечен частично: свободный список списаний из пилота выпадает вместе с D069 | «Замес теста также необходим. Единственное, на нём можно сильно прям не акцентироваться, потому что у нас есть отдельный сервис, который позволяет клонировать замесы, вынос и всякое такое» | owner |
| D075 | 2026-09-14 | Регулярность живёт на самом пункте и задаётся НАБОРОМ ОТРЕЗКОВ, а не одним интервалом и не списком времён: чип в строке редактора открывает окно, где методист пишет «с 8 до 16 каждый час, с 16 до 23 каждые два часа». Неравномерная сетка выражается вторым отрезком, а не вторым чек-листом. Настройка секции как отдельного понятия не заводится; «применить ко всей секции» — кнопка в том же окне, то есть удобство редактора, а не сущность модели | «В редакторе строки надо просто дать возможность чипом открыть окно настройки регулярности и там уже дать возможность настроить. И там же по сути можно задать, что, условно, с 8 до 16 обходим каждый час, с 16 до 23 обходим каждые два часа» | owner |
| D076 | 2026-09-14 | Обход показывается одной свёрнутой строкой на чек-лист — с БЛИЖАЙШИМ временем обхода, а не со счётом сделанного, — и разворачивается списком отметок: когда обходы делались | «В рамках чек-листа это одна строчка с ближайшим временем обхода, но можно это развернуть и списком просмотреть, когда обходы делались» | owner |
| D077 | 2026-09-14 | Разворот отметок текущей смены доступен НА ЭКРАНЕ СТАНЦИИ, без входа в кабинет. Этим правится первая мера D021 («ссылка не отдаёт историю станции»): публичная ссылка отдаёт отметки обхода за текущие местные сутки по тому чек-листу, что привязан к этой станции, — без комментариев, без других чек-листов, без других станций и без выхода за сутки. Ограничение частоты отправок и размещение наклейки в рабочей зоне (остальные две меры D021) не трогаются | «Чтобы можно было проверять текущую смену без захода в админку». Риск принят и назван: код станции — публичный адрес, и отметки обхода по нему становятся видны любому, кто отсканировал QR. Содержание отметки анонимно и беднее того, что человек и так видит, стоя у станции; ротация кода (D006) остаётся | owner |
| D078 | 2026-09-14 | Отдельного файла по замесу теста не будет: источник — вкладки `Dough mixing, production` и `Dough mixing, fridge` уже присланного «Чеклисты для сервиса чеклистов (14.09).xlsx». Ожидание второго файла снято | «Файла про замес теста не будет пока, базируемся на той таблице, что я тебе скинул» | owner |
| D079 | 2026-09-14 | Временный порядок для обходов сделан НЕ так, как предложил владелец. Предложено было «разбить на пункты подряд: уборка 8:00, уборка 9:00 … до 22:00» внутри одного чек-листа; сделано иначе — каждый обход разложен на почасовые чек-листы и вынесен на СВОЮ станцию («Горячий цех · обход» рядом с «Горячий цех»). Причина в двух фактах кода, а не во вкусе. Первый: `getPublishedVersionForStation` отдаёт станции РОВНО ОДИН чек-лист (`limit(1)`), поэтому почасовой обход на общей станции молча перекрыл бы открытие смены. Второй: один чек-лист на день из 100+ пунктов ломает уже работающую тревогу — продукт принимает неполное заполнение, и каждая почасовая отправка давала бы 90+ критичных пунктов без ответа, то есть тревогу «критичный пункт оставлен без ответа» на каждом часе. Почасовые чек-листы, наоборот, заставляют существующую тревогу работать правильно: непройденный час = незаполненный чек-лист с закрывшимся окном (D054). Побочно вышло лучше задуманного: вращающаяся зона («проверены сроки годности» — в 8:00 линия начинения, в 12:00 склад) выражается прямо в тексте пункта своего часа | Владелец: «громоздко, но в рамках временного костыля пойдёт… потом уже доработаем нормальный функционал». Громоздкость принята им заранее; выбор конкретной формы костыля остался за исполнителем, и он сделан по двум измеренным свойствам продукта, а не по предпочтению | auto |
| D080 | 2026-09-14 | Стенд показа обновляется с коммита 7f63698 (09.09) до main СЕЙЧАС, а не в ночное окно 23:00-05:59. Окно на эту раскатку не соблюдается по прямому решению владельца | «ну так давай ебаш, правь» — сказано после того, как окно было названо вслух и предложено дождаться его. Повод: на стенде показа переход по любому пункту меню кабинета даёт ошибку, потому что в версии 09.09 меню сделано обычным `<a href>`, которому Next не приставляет базовый путь `/qr`, и клик уводит на корень порта, где живёт MCP сервиса аудита (дефект T088/D046, в main уже починен). То есть без обновления показывать коллегам нечего. Такое же отступление от окна владелец делал 09.09 при переименовании | owner |
| D081 | 2026-09-14 | Станция, на которой в одну минуту открыто НЕСКОЛЬКО чек-листов, показывает выбор, а не первый по началу окна. Один открытый — открывается сразу, выбора нет. Выбранный чек-лист живёт в адресе (`?c=<id>`), чужой или закрывшийся идентификатор возвращает к выбору и не подставляет свой молча. В списке — только название и окно каждого: ровно то, что сотрудник увидит, открыв любой из них, и ничего сверх (D021). Порядок задаёт слой данных (начало окна, затем идентификатор) и экран его не пересортировывает: список обязан выглядеть одинаково при каждом сканировании. Это снимает дефект #60, на котором держался костыль D079: пересечение окон на боевом пакете (обход менеджера 08:00–23:00 накрывает приём смены 15:00–18:00) молча съедало второй чек-лист | Не развилка владельца, а исправление дефекта: продукт терял данные методиста без единого следа. Форма выбора — ссылками, а не кнопкой с обработчиком: наклейку сканируют чужим телефоном в подсобке, и переход обязан работать до того, как доедет клиентский код | auto |
| D082 | 2026-09-15 | На станции ОДИН чек-лист, а не по одному на период суток. Периоды («открытие», «приём смены», «закрытие») становятся его секциями с часами в заголовке, обход — такой же секцией, только его пункты несут расписание и уезжают в панель обходов сами (D075). Окно чек-листа накрывает все периоды; считается перебором опор, потому что периоды идут через полночь и наивная склейка вывернула бы сутки. Боевой пакет: 9 чек-листов вместо 24, те же 175 пунктов | «что за чеклисты у нас, где 1 или 3 пункта? какой в них смысл? Нам нужен 1 станция — 1 чеклист. В нем внутри должно быть разделение на утро и вечер, если надо». Мелкие чек-листы были честным отражением таблицы (в закрытии холодного цеха одна строка), но как единица работы бессмысленны: сотрудник сканирует наклейку станции, а не «период суток» | owner |
| D083 | 2026-09-15 | Продукт показывается только на английском, вся русская обвязка из данных убрана: страна «Россия» и пиццерия «Пиццерия — пилот» удалены, пакет заведён в стране Demoland (пиццерия «Demoland, Pilot»). Имя станции в базе одно и берётся по языку страны; переводы пунктов внутри версий остаются, их выбирает язык браузера | «давай все русское выкинем, оставляем демоленд… у нас весь интерфейс сейчас на инглише. насколько помню сейчас смена языка не поддерживается» | owner |
| D084 | 2026-09-15 | Пароль кабинета на стенде показа — `admin`, вопреки требованию длины: адрес открыт из интернета, и это названо вслух дважды. Короткий пароль на время показа предусмотрен D047 | «мне продукт тестить надо, им никто не пользуется, его никто не сломает» | owner |
| D085 | 2026-09-15 | Библиотека получает ШЕСТЬ блоков, и ни один из них импорт никуда не вставляет: «Наличные: приём кассы», «Наличные: сдача и инкассация», «Допуск к смене», «Dodo IS: остатки, стоп-лист, списания», «Закрытие зоны: полная уборка», «Сроки годности и FEFO». Температуры блоком СОЗНАТЕЛЬНО не делаются | «Подумай насчет блоков. у нас есть такой функционал. Подготовь логически блоки заранее из того что у нас уже есть». Нарезка сделана НЕ по совпадению строк — дословных повторов между станциями ноль, 175 пунктов написаны каждый под свою станцию. Блок оправдан двумя другими поводами: текст стоит на нескольких станциях и обязан звучать одинаково, либо текст меняется от внешней причины (регламент, банк, закупка оборудования) и обязан меняться разом. Деньги — сильнейший случай обоих: одна процедура описана в пакете СЕМЬЮ разными формулировками (где «X-report is printed», где «generated», где есть сверка суммы, где нет), и кассир с менеджером сегодня читают про одно и то же разное. Температуры выглядят таким же кандидатом и им не являются: у каждой точки своя норма (зал +17…+27, цех ниже 22, холодильник 9…11), и блок с числовыми границами пришёл бы в чужую зону с чужой нормой — молча; это тот случай, когда неверная нарезка дороже отсутствия блока. Вставка блока в чек-лист остаётся за методистом: блок, вставленный импортом, менялся бы в чужих черновиках при каждой перезаливке пакета | auto |
| D086 | 2026-09-15 | Стенд показа раскатан ДНЁМ, вне ночного окна 23:00–05:59: код догнан до 03ccbca и перезалит боевой пакет, чтобы на площадке появились шесть блоков библиотеки (D085). Окно на эту раскатку не соблюдается по прямому решению владельца; стенд показа пользователей не имеет (D084), поэтому цена отступления — только собственный показ | «продолжи работу по стрйоке. раскатай что та мнадо было», а на прямой вопрос про остановленную сторожем команду — «Разрешаю — повтори сейчас». Прецедент D080 | owner |
| D087 | 2026-09-15 | Демо-пиццерии «Demoland, Central Square» и «Demoland, Riverside» решено снести ЦЕЛИКОМ, а не наполнять демо-набором. Исполнение упёрлось в принцип продукта: продукт отказывает удалению («Submissions reference this record — deletion is not allowed»), потому что на их станциях лежат заполнения — 10 на Kitchen у Central Square и 3 на Front counter у Riverside. История неприкосновенна (принцип 3, D002), обходить её через SQL не стали. T151 возвращена владельцу с фактом | «Снести пиццерии целиком» — ответ на развилку T151. Отказ продукта проверен на стенде живым экраном, а не по коду | owner |
| D088 | 2026-09-15 | Демо-историю двух демо-пиццерий снимаем руками, сами пиццерии сносим: принцип неприкосновенности истории (D002) для выдуманных демо-заполнений отступает. Правило продукта при этом НЕ меняется — путь сноса вместе с историей в кабинете не заводится, чистка разовая и внешняя | «сделай все что требуется чтоб убрать», затем «ебаш» — ответ на отказ продукта по T151/D087 | owner |
| D089 | 2026-09-16 | Миграции этого проекта пишутся руками, а `drizzle-kit generate` выключен: `npm run db:generate` теперь печатает отказ с порядком ручного добавления миграции и падает. Снимки схемы в `migrations/meta` остаются отставшими с 0006 и не восстанавливаются | Так уже есть по факту: 0006 и 0007 написаны руками — с комментариями и обратным файлом, которых генератор не пишет. При отставших снимках `generate` выдал бы миграцию, заново создающую `store_shift_modes`, `submissions.mode` и `checks`, — накат от этого не страдает, ломается генерация, и ломается молча и правдоподобно. Развилка закрыта дефолтом: практика уже сложилась, восстановление снимков означало бы поддерживать генератор, которым никто не пользуется | auto |
| D090 | 2026-09-16 | Будильник станции живёт до конца окна времени своего чек-листа, а не до местной полуночи. Поэтому время после полуночи ставится свободно, пока оно попадает в настроенное окно: пиццерия работает до 02:00 — в 23:40 будильник на 00:30 ставится и срабатывает. Прошедшее время внутри окна по-прежнему отвергается вслух, выдумывать «значит, завтра» не надо. Это закрывает Q015 вариантом (б) и уточняет D070: «до конца местных суток станции» читалось буквально и запрещало ровно тот случай, ради которого будильник заводят | «давай сделем что чеклист живет до конца настроенного времени. мы же выбираем часы когда чеклист работает - занчит и будильник должен жить до этого времени» (владелец, 16.09.2026, канал) — окно чек-листа уже есть в продукте (D004), второй сущности «смена» заводить не пришлось | owner |
| D091 | 2026-09-16 | Пересекающиеся отрезки расписания периодического пункта ЗАПРЕЩЕНЫ: правило `overlappingSegments` живёт одно в `src/blocks/data/schedule.ts`, и его зовут обе стороны — отказ на записи (импорт пакета и разбор редактора) и окно настройки, которое называет оба отрезка и гасит «Готово». Смежные отрезки пересечением не считаются. Следствие: `assertValidSchedule` зовётся и на чтении, поэтому расписание с пересечением теперь отказывает громко, а не врёт пропуском в отчёте | Два наложенных отрезка на одном пункте почти наверняка опечатка, а не замысел. Вторая ветка развилки (разрешить пересечения и научить отметку закрывать ВСЕ активные интервалы) потребовала бы переписать правило отметки и её хранение — сейчас отметка это строка с одним `intervalStart`, то есть пришлось бы менять схему, а схему в эту волну ведёт другой блок. Решено блоком editor по поручению диспетчера, развилка была отдана ему явно | auto |
| D092 | 2026-09-16 | Кабинет методиста на телефоне обязан быть НЕ полноценным, а пригодным для правки: человек должен суметь зайти и поправить, полной работы с телефона от продукта не ждут. Практически это значит схлопывающееся меню и читаемые заголовки, списки и кнопки на 375 px — но не переработку экранов под телефон: таблицы и сетки остаются со своей прокруткой, сложные разборы остаются настольными. Граница `--page-fold: 768px` из эталона остаётся, меняется её смысл: ниже неё кабинет не исчезает, а ужимается | «Должна быть хоть какая-то возможность, чтобы человек мог поправить. Про полноценную работу речи не идёт» (владелец, 16.09.2026, канал). Это средний вариант между теми, что я предложил: ни настоящего адаптива со всеми экранами, ни снятия ожидания вовсе | owner |
| D093 | 2026-09-16 | У полей панели будильника возвращаются видимые надписи над полем — «Во сколько» и «Зачем». Подсказки внутри пустого поля достаточной не считаются. Панель становится на строку выше, и это принято. | Дословно: «Давай вернем» — ответ на вопрос Q017, вариант (а). Замер живьём показал, что поле времени не названо на экране вообще ничем: `<label>` у обоих полей `sr-only`, а `placeholder` у времени пустой, то есть подсказки внутри поля там нет даже до ввода | owner |
| D094 | 2026-09-16 | К полям будильника добавляются пиктограммы, и владелец смотрит результат живьём, прежде чем признать его окончательным. Это отступление от экранного эталона: ни один ввод эталона иконок не несёт — поэтому после показа оно либо переносится в эталон, либо записывается признанным отступлением в `docs/furca/design/map.md`. | Дословно: «Сделай пиктограммы , потом посмотрим» — указание владельца, пришло следом за ответом на Q017. «Потом посмотрим» прочитано буквально: решение не окончательное, показ обязателен | owner |
| D095 | 2026-09-17 | Отозванный код станции и код, которого никогда не было, продукт не различает и различать не будет в MVP: экран отказа говорит нейтрально — сперва «проверьте код и наберите ещё раз», затем плашкой про возможную замену наклейки. | Следа отозванного кода в данных нет, и завести его — это миграция плюс таблица отозванных кодов, то есть хранилище, которое перебору кодов отвечает по-разному и тем самым спорит с D021. Цена ошибки несимметрична: сотруднику с опечаткой нужно перенабрать, и об этом он узнаёт первым; тому, у кого код действительно отозван, вторая строка про наклейку остаётся видна. | auto |
| D096 | 2026-09-17 | Чип регулярности показывается и на экране правки блока библиотеки: блок в админке выглядит так же, как тот же блок внутри чек-листа. Вариант (а) «оставить как есть и объяснить словами» отклонён. | Дословно: «Я предполагаю что блок будет также отображаться также визуально как блок в чек листе. Соответственно в админке блоков он также должен быть» — ответ на Q018 | owner |
| D097 | 2026-09-17 | Отрезок в чипе блока библиотеки показывается относительным («каждые N часов»), без привязки к часам суток. Собственного окна времени у блока не заводится: окно остаётся принадлежностью чек-листа, который блок подключает. | Владелец ответил про видимость чипа, а не про источник окна времени — эту часть развилки закрыл диспетчер. Вариант (б) «дать блоку своё окно» создал бы второй источник правды об окне: один блок живёт в нескольких чек-листах с разными окнами, и расписание разъехалось бы с тем, что видит заполняющий. Относительный отрезок — то же, что блок несёт на деле: периодичность без часов | auto |
| D098 | 2026-09-17 | Окно времени чек-листа остаётся как построено: список готовых окон для частых случаев ПЛЮС два поля своего времени рядом, своё окно сильнее списка, когда заполнены обе границы. Список не убирается и пресетами не дополняется. | Дословно: «вариант а» — ответ на Q019, где (а) и была рекомендация: она уже работает, покрывает и частый случай, и боевые окна 05:00–17:00 и 06:00–23:00, которых в списке нет, и ничего не отнимает | owner |
| D099 | 2026-09-17 | Панель будильника остаётся как есть — и видимые надписи над полями, и пиктограммы у них. К оформлению панели возвращаемся позже, отдельным заходом. | По существу: «оставь надписи… потом поправим». Реплика была ответом на раздражение повторными вопросами об этой панели — их к тому моменту было задано три (Q017, D093, Q020). Приведена по существу, а не дословно: репозиторий публичный, а брань к решению ничего не добавляет. Указание «потом поправим» означает, что пиктограммы (D094) закрываются признанным отступлением в карте соответствия, а не остаются висеть открытым вопросом | owner |
| D100 | 2026-09-17 | Сотруднические экраны состояний цветом ошибку не выделяют: остаются монохромными, различие типа проблемы даёт текст. Окрашивание (`notice--err`) остаётся принадлежностью кабинета. | Сверка T216 увидела в эталоне противоречие: админская карточка «Ошибка сохранения» окрашена, а на сотруднической стороне ни одно из пяти состояний, включая «Could not send» — ближайший аналог сбоя базы, — цветом не выделено. Противоречия нет: это разные поверхности с разными правилами, и для сотруднической эталон отвечает прямо. Сотрудник видит экран раз в смену и на телефоне, где цветовое кодирование без легенды читается хуже текста; управляющий в кабинете видит десятки состояний подряд, и там цвет экономит ему внимание. | auto |
| D101 | 2026-09-17 | Проект принимает правила проверок соседнего проекта `decimus` (его решение номер 129): планка числа выполненных тестов краснеет сама, пропуск по причине «нет окружения» считается падением, у всех целей прогона одинаковые доступы, ядро названо поимённо, файлы блоков — состояние с потолком 120 строк, тесты-налог удаляются, полный набор гоняется один раз на закрытии волны, а не на каждой приёмке блока. | Замер на живом проекте `decimus`: из 30 дефектов, доехавших до `fix`-коммита за неделю, юнит-тестами пойман ноль, а гейт трижды был зелёным при невыполнявшихся проверках — 339 тестов блока базы молча пропускались из-за незаданной переменной с доступами. Правила FURCA применяются сами при следующей стройке, артефакты проекта — нет: их надо привести руками, иначе гейт продолжит показывать зелёное там, где проверок не выполнялось. Источники: `GarroV/decimus` — решение 129 и `docs/05-calibration.md`, правки `GarroV/furca` a327083 и bd136f6. | auto |
| D102 | 2026-09-17 | Стройка meridius остановлена владельцем; заход закрывается, новые блоки не запускаются до отдельного слова. Планка числа выполненных тестов (T225) обязательной не считается — она противоречит T229 (удалить тесты-налог), разбор идёт в `GarroV/furca` #131. Фокус проекта возвращается к продуктовым задачам, разбор самой системы ведётся отдельной сессией. | Дословно владелец: «тормози работу, и запускай рефактор фурки. надо жестко пройтись и проанализировать по настройкам агента. понять где работа сбоит, и почему мы превратились в конвейер тестов, вместо того чтобы быть конвейером по сборке проектов. фокус явно сместился, и мы теряем время в угоду неизвестно чего». Замер, стоящий за решением: в meridius за неделю прибавилось 16431 строка тестов против 12193 строк продукта, `chore`-коммитов 164 из 416; волна 4 строила планку числа тестов — ровно то же, что соседний проект сделал часом раньше независимо, а продуктовые задачи (T211, T213-T215, T219) не двигались. | owner |
| D103 | 2026-09-17 | **Парадигма системы: пока идёт работа, проект строится.** Пять пунктов, принятых владельцем для FURCA и обязательных для этого проекта. (1) Каждая волна даёт кусок продукта, который можно открыть и показать; заход, не сдвинувший продукт, — сбой захода, два подряд — остановка и вопрос владельцу. (2) Служебные задачи (оснастка, форматы, журналы, гейты, переезды на новые правила системы) не берутся молча: копятся списком и выносятся владельцу — что накопилось, что предлагается, насколько подождёт продукт. Без жести: мелочь по дороге к продуктовой задаче делается походя и упоминается строкой в отчёте; сломанное (красный прогон, упавший стенд, битая миграция) чинится сразу как блокер стройки. (3) Тесты — там, где ошибка дорогая, и сразу; ядро названо поимённо в разделе «Ядро и обвязка» этих принципов, обвязка (маршруты, `ui/**`, хендлеры, блоки qr, demo, feed, catalog) проверяется живым запуском и сверкой экрана. Тест, не способный поймать ошибку, не пишется, найденный удаляется. **Число выполненных тестов не является мерилом прогресса** и в отчёт первой строкой не идёт. (4) Отчёт открывается тем, что пользователь теперь может сделать, чего не мог; числа прогона ниже, как подтверждение «не сломали». (5) Мерило волны — работающий сквозной сценарий («управляющий создал чек-лист и сотрудник его заполнил»), а не принятые блоки и зелёный прогон. Уточняет D101: планка числа выполненных тестов из T225 обязательной не является | Владелец дословно: «основной акцент что ПРОЕКТ ДОЛЖЕН СТРОИТЬСЯ. все время работы фурки ПРОЕКТ ДОЛЖЕН СТРОИТЬСЯ. Ради этого мы собирали его», «давай договоримся что СЛУЖЕБНЫЕ задачи идут через запрос пользователя, если это чтоб-то важное», «не жестим, конечно. в рамках разумного чиним сами себя, но фокус на сборку продукта», «Про тесты я согласен. но опять же, мы не должны строить тесты ради тестов». Внесено в систему коммитами GarroV/furca 7657320 и 26d6f0a, разбор — GarroV/furca#132. Правила системы применяются сами, артефакты проекта — нет (D101), поэтому решение записано здесь | owner |
| D104 | 2026-09-18 | Накопленное служебное разбирается по рекомендации диспетчера из Q021: (1) эталон редактора — отдельная волна, он разблокирует пять незакрытых сверок экранов (заведена T235); (2) T225, планка числа выполненных тестов, снимается — `cancelled` со ссылкой на это решение; (3) T226 (сверить доступы у целей прогона) вытаскивается из неслитой ветки `feat/core-w4` отдельным коммитом, без спорной T225; (4) T228 (срезать файлы блоков до потолка) и (5) T229 (удалить тесты-налог) откладываются до конца MVP; (6) сквозной смоук доведён до одного прохода в ночь на 18.09 и отдельной работы больше не требует | Владелец ответил на Q021 дословно: «Сделай ка креоуиендуешь» (набрано с опечатками, смысл однозначен — делать по приложенной рекомендации; иных прочтений у фразы в контексте вопроса нет, вариантов в вопросе было ровно три и рекомендация была одна). Записываю дословно, потому что пересказ теряет оговорки | owner |
| D105 | 2026-09-18 | Волна 7 запускается немедленно, не дожидаясь сброса пятичасового окна в 01:40. Остаток окна (менее 19% при длительности блоков 56–60 минут) основанием для паузы не считается: диспетчер предупреждает о риске и стартует. Уточняет решение прошлого захода, остановившего стройку по остатку окна. | Владелец выбрал вариант «Запустить сейчас» из трёх предложенных (ждать сброса — рекомендация диспетчера; запустить сейчас; остановиться). Риск был назван до выбора дословно: блок с высокой вероятностью упрётся в лимит посередине и оставит копию репозитория с несведённой работой, разбирать её придётся отдельным заходом. | owner |
| D106 | 2026-09-18 | Тёмная тема входит в MVP и делается двумя способами сразу: автоматически по системной настройке И явным переключателем, который сохраняет выбор пользователя и перебивает автоматику. Рекомендация диспетчера (вариант «а» — за горизонт MVP) отклонена. Заведена T236, отдельной волной по всем экранам сразу; пункт про тёмную тему в T216 остаётся в силе. | Владелец дословно: «23 - добавляем возможность настройки + автомат» — ответ на Q023. Это варианты (б) и (в) вместе: «автомат» — включение по `prefers-color-scheme`, «возможность настройки» — переключатель, которого эталон требует атрибутом `[data-theme="dark"]`. За решением стоит факт из вопроса: смена на кухне идёт 17:00–05:00, то есть продуктом пользуются ночью. | owner |
| D107 | 2026-09-18 | Три расхождения с эталоном на новых экранах отказа закрываются по рекомендации диспетчера: (1) неподсвеченный раздел меню на экране отказа кабинета остаётся как есть до жалобы — подсветка требует своей границы ошибки на каждый раздел, это дороже пользы; (2) карточка отказа в кабинете, смешавшая виды «пусто» и «ошибка», и (3) отсутствующий образец «база недоступна» для сотрудника считаются пробелом эталона, а не дефектом продукта, и закрываются той же волной, что правка эталона редактора (T235) — заведена T237. | Владелец ответил на Q022 дословно: «22 - реши сам». По правилу такой ответ означает принятие рекомендации, приложенной к вопросу, и решение записывается как принятое диспетчером. Рекомендация была ровно одна и приведена в самом вопросе. | auto |
| D108 | 2026-09-18 | Блок data по T221 принимается, хотя сверяющий тест закрывает половину заявленного риска: он ловит пропажу ограничения по имени, но не расхождение текста условия при сохранённом имени. Расширение сверки на тексты вынесено отдельной задачей T238, а не доделкой в эту волну. | Фактическое состояние верно и проверено накатом с нуля: разбор свежим взглядом поднял базу, накатил все миграции, сверил `pg_get_constraintdef` с объявленным в схеме и подтвердил, что тексты совпадают буквально, имена взяты конечные (после переименования 0011), а генератор после правки добавляет ровно две строки CONSTRAINT и ничего не снимает. То есть дыра — про будущую правку, а не про сегодняшнее расхождение. Против доделки в эту волну: канонизация текста ограничения с обеих сторон (суффиксы `::text`, скобки, квотирование регулярных выражений) — работа с собственным отрицательным прогоном, а недельное окно подписки на момент решения ушло к 91–100% при сбросе через ~40 часов. Размен записан явно, а не замолчан: принять известное ограничение теста ревизор назвал допустимым исходом наравне с починкой. | auto |
| D109 | 2026-09-18 | Поле `timeZone` остаётся в успешном ответе экрана заполнения: D021 оно не нарушает, переносить форматирование времени на сервер не требуется. | Развилку поднял сам блок `fill`, и разбор свежим взглядом (security-reviewer) её закрыл фактически: поле объявлено только в исходе «код настоящий» (`FillTargetReady`), а ветки неизвестного, отозванного и чужого по формату кода возвращаются раньше, чем код вообще обращается к этому полю, — и это закреплено строгим равенством в тесте, то есть случайное поле в отказе уронит прогон. Отдельный тест фиксирует точный список полей успешного ответа. Заголовки и набор запросов к базе у обоих исходов не изменились, нового различия по времени ответа правка не создаёт. Сведением сверх уже раскрытого пояс не является: в том же ответе сотрудник получает название пиццерии, название станции и содержимое чек-листа. Не проверено сетевым замером (время и байтовый состав живьём) — вывод по коду и тестам, и это названо честно. | auto |
| D110 | 2026-09-18 | У числового пункта появляется единица измерения полем в данных, и её задаёт методист в редакторе, а не разработчик в коде: перечня зашитых единиц не будет. Выводится она у поля рядом с границами («-22…-10 °C»), эталон в этой части остаётся как есть. | Ответ владельца на Q024, дословно: «Значит давай добавим единицы тоже, но надо уметь их настраивать, чтобы поле было гибким». Рекомендация диспетчера была противоположной (признать нынешний вид без единицы) — владелец её отклонил и добавил требование гибкости, которого в вариантах вопроса не было. | owner |
| D111 | 2026-09-18 | Тёмная тема (T236) строится блоком `core`, а не новым блоком `ui`: отдельный блок для неё не заводится. | T236 была заведена на блок `ui`, которого в пакете документов нет вовсе — проверка целостности состояния падала, и раздать эту задачу было нельзя ни одной волной. Токенами темы (`@theme inline` в Tailwind) и глобальными стилями владеет `core`, переключатель темы живёт в каркасе приложения и по всем экранам сразу — то есть работа и так целиком внутри его границ. Заводить блок ради одной задачи дороже: у него не было бы ни контракта в техплане, ни истории. | auto |
| D112 | 2026-09-18 | Модель работы с чек-листом: сотрудник подходит к нему раз в час-два и отмечает несколько пунктов сразу, а не держит телефон в руках весь день. Поэтому время отдельного пункта не означает момент выполнения этого пункта — оно означает момент, когда человек подошёл, — и на карточке управляющего его показывать как время отметки нельзя. Исключение — чек-листы циклических действий (уборки, обходы): там значимо не поштучное время, а попадание проверки в положенное ей окно, и это механизм расписания, а не отметки. | Владелец, дословно: «Человек действительно подходит раз в час-два и отмечает несколько пунктов, ане ходит с телефоном в постоянном режиме решения задач.» и «исключение только чеклисты циклычных действий: уборки, обходы. тут важна отметка как раз в районе того времени, когда должна быть проверка чек лита». Сказано в ответ на Q025, где разбиралось, что делать с недостоверным поштучным временем: модель работы снимает сам вопрос — поштучное время не нужно ни доказательству, ни картине, а доказательство циклической проверки даёт окно расписания. | owner |
